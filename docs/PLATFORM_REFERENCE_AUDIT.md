# تدقيق مراجع المنصة السابقة

آخر مراجعة: 2026-09-09
النتيجة: صفر اعتماد تشغيلي أو بنائي على Lovable داخل نسخة العمل الأساسية

## نطاق الفحص

شمل الفحص أسماء الملفات والمجلدات، كود الواجهة، إعدادات البناء والاختبار، سكربتات التشغيل، Edge Functions، الحزم وlockfile، ملفات SQL والبناء الناتج.

كما شمل أسماء القالب والمستودع والمعرفات القديمة التالية:

- Lovable ونطاقاتها وحزمها وأسماء متغيراتها.
- `gptengineer`.
- `creative-flame-keeper`.
- `vite_react_shadcn_ts` وأسماء القالب المشتقة منه.
- معرف مشروع Supabase القديم `ejfoidwbeygzzkbonbmq`.

## النتيجة التشغيلية

- لا مجلد `.lovable` ولا ملف منصة مخفي.
- لا حزمة `lovable-tagger` أو حزمة Lovable أخرى.
- جميع قيم `resolved` وعددها 97 في lockfile تستخدم `registry.npmjs.org` فقط؛ تحقق منها parser مستقل وليس بحثاً نصياً فقط.
- اسم المشروع في `package.json` و`supabase/config.toml` هو `store-accounting-app`.
- لا يظهر اسم المستودع أو القالب أو معرف المشروع القديم في الملفات التشغيلية أو البناء.
- لا يظهر أي نطاق Lovable أو نطاق إنتاج حقيقي داخل البناء الوهمي.

## المراجع المسموح بها

| الملف | السبب |
|---|---|
| `docs/LOVABLE_DECOUPLING_PLAN.md` | سجل عملية الانفصال وقراراتها وأدلتها |
| `docs/DEPENDENCY_SECURITY_PLAN.md` | نتائج تاريخية تقارن الأبنية المعزولة بالمشروع القديم |
| `docs/SALES_REPORT_IMPROVEMENT_TRACKER.md` | سجل حادثة الاتصال بالمشروع القديم وخطوات علاجها |
| `docs/MASTER_EXECUTION_PLAN.md` | الخطة الرئيسية التي تضبط إغلاق الانفصال ومراحل L3 |
| `docs/L3_STAGING_RESTORE_RUNBOOK.md` | سجل نقل بيانات واقعية تمهيداً لاختبار انتقال سجل الترحيلات |
| `docs/L3_STAGING_FARIDA_DATA_REHEARSAL.md` | أدلة تجربة البيانات واسم جدول المصدر التاريخي المطلوب نقله |
| `supabase/migrations/20260419170758_1e4296a7-5cfa-429f-86d4-8cd8fe59158e.sql` | تعليق تاريخي داخل migration مطبق؛ لا يجوز تعديل migration مطبق |
| `supabase/migrations/20260907091000_neutral_migration_tracking.sql` | يحتاج اسم جدول التتبع القديم لنقل بياناته وإنشاء View رجوع مؤقت؛ ليس اتصالاً بخدمة منصة |

هذه قائمة سماح مغلقة تجمع السجلات التاريخية وخطة التنفيذ الحالية. يحتوي ملف التدقيق الحالي وسكربت الحاجز أيضاً كلمات المنع اللازمة للشرح والتنفيذ، وهما مستثنيان بوصفهما أداتي رقابة. أي ظهور جديد خارج الملفات الثمانية وأداتي الرقابة يعد تراجعاً ويجب أن يفشل في فحص المستودع.

## حاجز منع العودة

وسع `scripts/check-repository-safety.sh` ليرفض:

- أي مسار جديد باسم Lovable أو `.lovable` أو `gptengineer` خارج سجل الخطة المسموح.
- أي ذكر جديد للمنصة خارج الملفات الثمانية المسموح بها أعلاه و`docs/archive`.

لا تطبع أداة الفحص قيماً سرية عند فشلها.
