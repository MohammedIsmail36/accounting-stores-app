# خط أساس Staging قبل مصدر تشخيص مطابقة المخزون

> التاريخ: 2026-09-13
>
> البيئة: Staging فقط — مشروع `dunzfxurefzlaamgghys`
>
> الحالة: نسخة محفوظة ومتحقق منها؛ طُبقت Migration لاحقاً على Staging فقط واجتازت التحقق

## النسخة المعتمدة

- المسار المحمي: `/backups/staging/inventory-reconciliation-before-20260913-192242`.
- صلاحية المجلد: `0700`، والملفات: `0600`.
- تشمل مخطط وبيانات `public` فقط؛ لا تشمل بيانات `auth` أو `storage` أو `realtime`.
- PostgreSQL: `17.6`، وSupabase CLI: `2.116.0`.
- يحتوي المخطط `39` جدولاً، ويحتوي ملف البيانات `39` كتلة `COPY`.
- العلاقات الدائرية المعروفة في `accounts` و`product_categories` تتطلب استخدام ترتيب الاستعادة المعتمد؛ لا يشغّل ملف البيانات منفرداً على بيئة فعلية.

## البصمات

| الملف | الحجم بالبايت | SHA-256 |
|---|---:|---|
| `public-schema.sql` | 316804 | `2c36d26bd72fe6288aa9c235bed68876dad7179297e8debe9d0538fb1df77eb9` |
| `public-data.sql` | 1334831 | `17fb3690706d05b3022eec8acb85b2003ccc2dac4fbb6dbbbca21e6ad6437151` |
| `baseline-query.sql` | 3546 | `2c6c509d8ed9bcf8190c43fe84a07b28cd4b3a971efc25d10044c42b4b4759a1` |
| `baseline.json` | 1046 | `8a03e91c697a26dd98ca565d4ee418ad07711eff3c7364feca16ae6191167183` |
| `manifest.txt` | 1068 | `aa1b002c4e6c7d1b1d155412a934d7a56ff81de5917852becad6d2aede1174a6` |

نجح فحص `SHA256SUMS` قبل النقل وبعده. كما طابقت أعداد الصفوف المقروءة من كتل `COPY` خط الأساس الحي لجميع الجداول العشرة محل الفحص.

## خط الأساس الحي

نُفذ الاستعلام عبر معاملة `BEGIN TRANSACTION READ ONLY` وانتهى بـ`ROLLBACK`:

| المؤشر | القيمة |
|---|---:|
| المنتجات | 613 |
| حركات المخزون | 1354 |
| فواتير المبيعات | 98 |
| فواتير المشتريات | 31 |
| مرتجعات المبيعات | 10 |
| مرتجعات المشتريات | 5 |
| تسويات المخزون | 2 |
| القيود | 310 |
| سطور القيود | 827 |
| القيود المرحلة بلا سطور | 3 |
| المفاتيح الأجنبية | 60 |
| مشغلات المستخدم | 37 |
| إصدارات السجل المحايد | 96 |

بصمة سجل الترحيلات المحايد: `634e981f2f7363bcdc1b546a1e9c37c0`.

## خط المخزون

| المؤشر | القيمة |
|---|---:|
| كمية البطاقات | 3880 |
| صافي كمية الحركات | 3880 |
| القيمة الدفترية للحركات | 483717.1190909091 |
| رصيد حساب `1104` | 483717.1390909091 |

يوجد فرق إجمالي قدره `0.02` بين قيمة الحركات ورصيد `1104`. لا يُخفى هذا الفرق ولا يعالج في خطوة النسخ؛ ستصنفه دالة التشخيص حسب مصادره بعد اختبارها على Staging.

## بوابة الأمان

- الدالة الجديدة لم تكن موجودة وقت التقاط الخط: `diagnostic_exists=false`.
- لم تُطبق Migration ولم يُنفذ أي تعديل على Staging.
- لم تُلمس Farida أو Alibea الإنتاجيتان.
- الخطوة التالية هي تجربة Migration ودالة التشخيص على Staging داخل معاملة تنتهي بـ`ROLLBACK`، قبل أي تطبيق دائم.

## تجربة Migration ذات الرجوع الكامل

اكتملت بعد حفظ النسخة، باستخدام المشغل المقيد `scripts/staging/rehearse-inventory-reconciliation-diagnostic.mjs`:

- تحقق المشغل محلياً أن المشروع المرتبط هو `dunzfxurefzlaamgghys`.
- أنشأ الدالة وشغّل الملخص وصفحة المنتجات وصفحة المصادر على بيانات Staging الفعلية.
- طابقت الكمية والقيمة ورصيد `1104` خط الأساس أعلاه، ونجح حاجز الحد الأقصى للصفحات.
- انتهت المعاملة بـ`ROLLBACK`.
- أكد استعلام مستقل بعد الرجوع أن الدالة غير موجودة وأن أعداد الجداول ما زالت مطابقة.
- النتيجة: `STAGING_DIAGNOSTIC_TRANSACTIONAL_REHEARSAL_OK`.
- الدليل المحمي: `/backups/staging/inventory-reconciliation-before-20260913-192242/diagnostic-transactional-rehearsal`.

بذلك أصبحت Migration متوافقة مع بيانات Staging الحقيقية من دون إبقاء أي تغيير في تجربة الرجوع.

## التطبيق الدائم على Staging

بعد الاعتماد الصريح:

- أكد `migration list --linked` أن الإصدار `20260913160000` هو الملف المحلي الوحيد غير المسجل في Staging.
- طبق `supabase@2.116.0 db push --linked` الملف `20260913160000_inventory_reconciliation_diagnostic.sql` وحده؛ لم يطبق seeds أو roles.
- أكد تحقق مستقل داخل معاملة قراءة فقط وجود الإصدار في سجل Supabase ووجود الدالة بالخصائص `STABLE` و`SECURITY DEFINER` و`search_path` الثابت.
- تأكد منع `PUBLIC` و`anon` من التنفيذ، ومنح التنفيذ لـ`authenticated` و`service_role` مع استمرار فحص دور التطبيق داخل الدالة.
- بقيت أعداد بيانات الأعمال مطابقة تماماً لخط الأساس.
- أعادت الدالة: كمية بطاقة `3880`، كمية حركات `3880`، قيمة حركات `483717.12`، ورصيد `1104` بقيمة `483717.14`.
- الحالة `rounding_only`: لا توجد مشكلة منتج، ولا حركة أو قيد غير مرتبط؛ توجد حالتا تقريب موثقتان بإجمالي `0.02`.
- حُفظ دليل التطبيق في `/backups/staging/inventory-reconciliation-before-20260913-192242/diagnostic-post-apply` واجتاز `SHA256SUMS`.

لم تُطبق الدالة على Farida أو Alibea، ولم تُربط بها الواجهة بعد. الخطوة التالية مقارنة ظل read-only بين المصدر الحالي والدالة الجديدة على Staging.

## مقارنة الظل مع الشاشة الحالية

نُفذت مقارنة قراءة فقط على النطاق `all_recorded_stock_effects` وانتهت بـ`ROLLBACK`:

- طابقت الدالة الجديدة قيمة الحركات في الشاشة الحالية: `483717.12`.
- طابقت تقييم WAC الذي تحسبه الشاشة فعلياً: `483378.34`، والفارق التحليلي عن قيمة الحركات `-338.78`.
- طابقت `489` صفاً غير صفري من أصل `489` صفاً بين الشاشة والدالة، دون اختلاف كمية أو قيمة في أي منتج.
- تستبعد الدالة الجديدة `124` منتجاً صفرياً من الصفوف لتقليل النقل والازدحام، مع بقائها محسوبة ضمن تعريف نطاق المنتجات.
- عدد فروق كمية البطاقة عن الحركات: صفر.
- دالة التقييم القديمة `get_inventory_valuation` أعادت WAC بقيمة `483378.71`، أي أعلى `0.37`، لأنها تقرّب WAC لكل منتج إلى منزلتين قبل الضرب. لا تستخدم هذه النتيجة بديلاً عن صيغة الشاشة الجديدة.
- تسمّي الشاشة الحالية `21` فرقاً بين WAC وقيمة الحركات «منتجات غير متطابقة». هذا وصف تحليلي مضلل: الدالة الجديدة أثبتت عدم وجود مشكلة كمية أو رابط، وتعرض فروق WAC منفصلة عن تصنيف السلامة.
- مصدر فرق الأستاذ البالغ `0.02` فاتورتا شراء مرحلتان رقما `17` و`24`، ولكل منهما باقي تقريب `0.01` موثق.
- النتيجة: `STAGING_INVENTORY_RECONCILIATION_SHADOW_OK`.
- الدليل المحمي: `/backups/staging/inventory-reconciliation-before-20260913-192242/diagnostic-shadow-comparison`.

بوابة مقارنة الظل مكتملة. الخطوة التالية ربط واجهة Staging بالدالة الجديدة، مع إزالة وصف «غير متطابق» عن فرق WAC ومنع زر المزامنة المباشر، دون دمج أو نشر إنتاجي.
