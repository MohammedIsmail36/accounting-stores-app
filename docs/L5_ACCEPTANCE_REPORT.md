# تقرير قبول L5 — الجودة والأمان

> بدأ في: 2026-09-07
> النطاق: النسخة النظيفة فقط
> الحالة: مكتملة ومقبولة؛ نُشرت حزمة القبول على Staging فقط، دون نشر أو اتصال بقواعد الإنتاج

## بيئة التشغيل

- Node المختبر: `22.23.2`، والسياسة المعتمدة `>=22.0.0 <23`.
- npm المختبر في هذه الدورة: `10.9.8`، والسياسة المعتمدة `>=10.9.0 <13`.
- نجح `npm ci --offline`، ولم تتغير أي خانة اعتماد عند مزامنة سياسة التشغيل.

## بوابة أمان الاعتماديات والمستودع

- `npm audit --offline`: صفر ثغرات في جميع الاعتماديات.
- `npm audit --offline --omit=dev`: صفر ثغرات في اعتماديات الإنتاج.
- نجح حاجز الأسرار والملفات الحساسة والمراجع التشغيلية المحظورة دون نتائج.
- جميع قيم `resolved` وعددها 97 تستخدم `registry.npmjs.org`، وسجل npm الفعلي هو `https://registry.npmjs.org/`.

## بوابة التحليل الساكن

- نجح TypeScript عبر `tsc --noEmit` دون أخطاء.
- لم يجتز ESLint البوابة الحالية: 1295 خطأ و119 تحذيرًا. لم يُستخدم `--fix` ولم يُعدل الكود آليًا.
- الفصل حسب النطاق: الأرشيف 284 خطأ/38 تحذيرًا، و`src` التشغيلي 914/81، وEdge Functions عدد 96/0، وملف إعداد جذري واحد 1/0.
- القاعدة الغالبة هي `@typescript-eslint/no-explicit-any` بعدد 1281 خطأ. توجد أيضًا 6 أخطاء `prefer-const`، وخطآن لقواعد Hooks، وأربعة أخطاء أخرى موزعة على قاعدتين.
- هذه ديون سابقة وليست ناتجة عن تثبيت Node أو تنظيف الاعتماديات، لكنها تمنع إعلان L5 مكتملة. يجب أولًا إخراج الأرشيف من نطاق ESLint، ثم معالجة أخطاء المصدر التشغيلي على دفعات مختبرة بدل تعطيل القواعد أو استخدام إصلاح شامل.
- صُحح نطاق ESLint ليستبعد `docs/archive/**` فقط إلى جانب `dist`؛ لم تُستبعد ملفات التطبيق أو Edge Functions ولم تتغير القواعد.
- خط الأساس التشغيلي بعد التصحيح: 1011 خطأ و81 تحذيرًا في 113 ملفًا متأثرًا، مع صفر ملفات أرشيف مفحوصة. التوزيع: `src` عدد 914/81، وEdge Functions عدد 96/0، وإعداد جذري واحد 1/0.
- من أخطاء المصدر 997 نتيجة `no-explicit-any`، وتبقى 14 نتيجة من قواعد أخرى؛ تبدأ المعالجة بالقواعد الأخرى الأعلى خطورة، خاصة Rules of Hooks، قبل مشروع تحسين الأنواع الواسع.
- عولجت الأخطاء الأربعة عشر الأخرى يدويًا دون `--fix`: ثوابت قابلة للتصحيح، نوعان فارغان، كتلتان لالتقاط خطأ متوقع، اختبار تعبير شرطي، استيراد Tailwind بصيغة ESM، وخطآ Rules of Hooks في `LookupManagement`.
- كان إصلاح Hooks وظيفيًا: نُقل اشتقاق القائمة والإحصاءات قبل الخروج الشرطي حتى يبقى ترتيب Hooks ثابتًا بين جميع عمليات العرض.
- بعد الدفعة نجح TypeScript، ونجح 76/76 اختبارًا مستهدفًا في ملفين، وأصبح ESLint يعرض 997 خطأ فقط كلها `no-explicit-any` مع 81 تحذيرًا وصفر أخطاء من أي قاعدة أخرى.
- بعد تثبيت هذا الخط الأساسي أصبحت `no-explicit-any` تحذيرًا مرئيًا لا خطأً مخفيًا، وبقيت قواعد مستوى الخطأ الأخرى حاجبة. يشغل `npm run lint` الفاحص `scripts/check-eslint-baseline.mjs`، بينما يعرض `npm run lint:details` التفاصيل الكاملة.
- تفشل البوابة إذا ظهر أي خطأ، أو زادت تحذيرات `any` عن 997، أو زادت التحذيرات الأخرى عن 81. يسمح الحاجز بخفض الأعداد ولا يسمح بزيادتها.
- نجح الاختبار السلبي: أضيف استخدام `any` مؤقتًا فارتفع العداد إلى 998 ورفضه الحاجز بالحالة 1؛ حُذف الملف المؤقت ثم عادت البوابة إلى النجاح عند 997/81، ونجح حاجز أمان المستودع.

## بوابة الاختبارات الآلية

- نجح Vitest بالإصدار `3.2.7`: عدد 50/50 ملف اختبار و565/565 اختبارًا، دون تخطٍ أو فشل.
- استغرقت الدورة 74.79 ثانية، وكان زمن تنفيذ الاختبارات نفسها 1.05 ثانية؛ الباقي إعداد وتحويل وبيئة jsdom.
- ظهر تحذيران فريدان غير حاجبين من React Router 6 عن تغييرات مستقبلية في الإصدار 7 (`startTransition` ومسارات splat). لا يؤثران في نتيجة الاختبار ويُسجلان لدورة ترقية React Router اللاحقة.

## بوابة بناء الإنتاج المعزول

- نجح Vite `5.4.21` بقيمة Supabase وهمية `https://l5-production.invalid` وفي مجلد مؤقت فقط: 3935 وحدة خلال 59.58 ثانية.
- نتج 178 ملفًا بحجم إجمالي 6,402,962 بايت. يستخدم `index.html` أصولًا جذرية من `/assets/` ويحمل سياسة `noindex, nofollow, noarchive`.
- لم يظهر أي نطاق Farida أو Alibea أو Staging، ولا معرف مشروع Staging أو المشروع القديم، ولا اسم المستودع القديم أو مرجع تشغيلي للمنصة السابقة.
- ظهر النص العام `*.supabase.co` مرة داخل مكتبة Supabase ضمن قائمة المضيفين المعروفة؛ ليس عنوان مشروع ولا يحتوي معرفًا. ظهرت القيمة الوهمية في ملف واحد كما هو متوقع.
- لم تعد ملفات `links.html` و`full-schema.sql` و`placeholder.svg` إلى البناء.
- بصمة `index.html`: `f30394a70b336759d237c33e941e5742346b8f8fdebb017c75139022b19b92c2`.
- الأصل الرئيسي `/assets/index-B1YMOUBj.js` وبصمته `d62b101f6f99fe55b4b4f488d7c00fe8aedb24eea6ae89c096bcd7e5617e4ab2`.
- بقي تحذير قديم عن استيراد `expense-posting.ts` ديناميكيًا وثابتًا، وثلاثة أجزاء أكبر من 800KB: PDF وTelegram وExcel. البناء ناجح، وهذه ديون تحسين أداء غير حاجبة وليست تسريبًا أو خطأ استقرار.

## بوابة بناء Staging المعزول

- جُلب المفتاح العام فقط من مشروع Staging عبر Supabase CLI `2.116.0` إلى ذاكرة العملية، دون طباعته أو حفظه في المستودع، ثم أزيل من متغيرات العملية بعد البناء.
- نجح Vite `5.4.21`: عدد 3935 وحدة خلال 48.21 ثانية، و178 ملفًا بحجم 6,404,469 بايت.
- ظهر عنوان مشروع Staging `dunzfxurefzlaamgghys` في ملف واحد فقط. لم يظهر أي نطاق إنتاج، أو API تابع للنطاق الفرعي، أو معرف المشروع القديم، أو القيمة الوهمية المستخدمة في بناء الإنتاج.
- قورنت المفاتيح آليًا دون طباعة قيمها: وجد مرشح عام واحد وطابق البناء في ملف واحد، ووجد مرشح `service_role` واحد لكنه لم يظهر في أي ملف.
- فحص الأسرار العام أبلغ عن JWT واحد لأنه مفتاح `anon` العام المتوقع في تطبيق المتصفح؛ أثبت فحص الهوية السابق أنه المفتاح العام وليس `service_role`. لا يُعامل هذا الاستثناء المحدد كتسريب.
- ظهرت علامة «بيئة تجريبية» فعليًا في ملف بناء واحد، وكانت مراجع `index.html` جذرية من `/assets/`، ولم تعد الملفات العامة المحذوفة.
- بصمة `index.html`: `b49854bc3e6a9b8bc8e4979372c0b3656fa8e5c1ea1ad4bbca4ab45ad8fdbf50`.
- الأصل الرئيسي `/assets/index-CzIozxOm.js` وبصمته `546cf1a7a96453bd1511a61c5d038b6bd3fc5b811a91a10f73578a2727e23d14`.
- تكررت تحذيرات الأداء نفسها المسجلة في بناء الإنتاج ولم يظهر تحذير جديد خاص بـStaging.

## بوابة dev وpreview المحلية

- شُغل خادم Vite `dev` على `127.0.0.1:41731` فقط بقيم وهمية. أعادت `/` و`/auth` و`/reports/sales` و`/src/main.tsx` الحالة 200 ومحتوى غير فارغ.
- أُوقف خادم `dev` قبل بدء `preview`، ثم بُنيت نسخة حالية مؤقتة بقيم وهمية وشُغلت على `127.0.0.1:41732` فقط.
- أعادت `/` و`/auth` و`/reports/sales` من `preview` الحالة 200. أعيد الأصل الرئيسي `/assets/index-DyQXPTj8.js` بالحالة 200 وحجم 686,583 بايت وطابق ملف البناء بايتًا ببايت.
- خلا الأصل المقدم من نطاقات الإنتاج وStaging ومعرف المشروع القديم ومراجع المنصة السابقة.
- أُوقف `preview`، وأثبت طلب لاحق أن المنفذين 41731 و41732 مغلقان. حُذف بناء smoke المؤقت بعد الاختبار.

## نشر حزمة القبول على Staging

- أُنشئت نسخة رجوع قبل التغيير في `/var/backups/accounting-app/staging-frontend/20260907T154504Z`، وتحتوي 183 ملفًا مطابقًا للواجهة السابقة؛ بقيت محفوظة للاستخدام أثناء الاختبار اليدوي.
- أعادت أول محاولتي تحقق بعد النسخ الحالة 403، فعمل الرجوع التلقائي في المرتين وأعاد `index.html` السابق بالبصمة `f86be6682086cb363d0de838f158d8a3c43859b123f3b86a28f178cdd0465440` وعدد 183 ملفًا.
- أثبتت محاكاة معزولة السبب: مجلد البناء المؤقت كان 700، و`rsync -a` نقل صلاحية جذر المصدر إلى جذر موقع Nginx فصار 700 ومنع المستخدم `www-data` من المرور. لم يكن الخطأ من DNS أو التطبيق أو قاعدة البيانات.
- ثُبتت صلاحيات الحزمة إلى 755 للمجلدات و644 للملفات، ثم أعيد النشر بـ`--delete-delay --delay-updates`. أصبح جذر الموقع 755 ونجحت طلبات `/` و`/auth` و`/reports/sales` بالحالة 200 محليًا وخارجيًا.
- الواجهة المنشورة تحتوي 178 ملفًا، وأصلها الرئيسي `/assets/index-CzIozxOm.js`. طابق الأصل المنشور حزمة البناء، وبصمته `546cf1a7a96453bd1511a61c5d038b6bd3fc5b811a91a10f73578a2727e23d14`، وبصمة `index.html` هي `b49854bc3e6a9b8bc8e4979372c0b3656fa8e5c1ea1ad4bbca4ab45ad8fdbf50`.
- حُصن `scripts/deploy-all.sh` ليطبق صلاحيات الأصول نفسها ويستخدم `--delete-delay` مستقبلًا. لم يُشغل السكربت، ولم يتغير Nginx أو أي قاعدة بيانات أو واجهة إنتاج.

## الاختبار الوظيفي اليدوي على Staging

- المصادقة بحساب المدير: ناجحة بتأكيد المستخدم.
- فتح لوحة التحكم بعد الدخول: ناجح.
- ظهور علامة «بيئة تجريبية»: ناجح، بما يؤكد هوية الواجهة وعدم التباسها مع الإنتاج.
- توجيه حساب البائع بعد الدخول إلى صفحة «فواتير المبيعات»: ناجح بتأكيد المستخدم.
- منع حساب البائع من فتح `/expenses`: ناجح؛ ظهرت صفحة «غير مصرّح بالوصول» بدل صفحة المصروفات، ما يؤكد تطبيق الحماية على المسار المباشر.
- إجراءات فاتورة المبيعات المرحّلة لحساب البائع: ناجحة؛ ظهر إجراء الطباعة فقط، ولم تظهر إعادة المسودة أو الإلغاء أو التعديل.
- إنشاء فاتورة مبيعات جديدة بحساب البائع وحفظها كمسودة: ناجح؛ أُنشئت الفاتورة رقم 5 بتأكيد المستخدم.
- تعديل مسودة فاتورة المبيعات رقم 5: ناجح؛ بقي رقم المستند ثابتًا، وحُفظت الكمية الجديدة وظهرت بعد إعادة الفتح.
- ترحيل مسودة المبيعات رقم 5: ناجح؛ أصبحت `INV-0005` مرحّلة، واختفت إجراءات التعديل والإلغاء وإعادة المسودة من حساب البائع وبقي زر الطباعة فقط.
- تقرير المبيعات بحساب المدير: ناجح؛ ظهرت `INV-0005` عند اختيار «هذا اليوم» وعرض «الفواتير»، ما يؤكد دخول المستند المرحّل في التقرير.
- تصدير تقرير المبيعات إلى PDF: ناجح؛ ظهرت قائمة التصدير كاملة، ونُزل الملف وفتح بنجاح بتأكيد المستخدم.
- تصدير تقرير المبيعات إلى Excel: ناجح؛ فُتح الملف وظهرت بيانات `INV-0005` داخله.
- إنشاء فاتورة مشتريات بحساب المدير وحفظها كمسودة: ناجح؛ أُنشئت المسودة رقم 1 بتأكيد المستخدم.
- تعديل مسودة المشتريات رقم 1: ناجح؛ بقي رقمها ثابتًا وحُفظت الكمية الجديدة وظهرت بعد إعادة الفتح.
- ترحيل مسودة المشتريات رقم 1: ناجح؛ أصبحت `PUR-0001`، وظهرت للمدير إجراءات «إعادة كمسودة» و«إلغاء» و«طباعة».
- طابقت الإجراءات سياسة الكود: إعادة المسودة للمدير فقط، والإلغاء المالي للمدير والمحاسب، والطباعة للمستخدم المصرح. لم يُنفذ إلغاء أو إعادة مسودة أثناء اختبار القبول.
- إنشاء مصروف بحساب المدير: ناجح؛ أُنشئ المستند رقم 1 بنوع «مصروف تجربة» المثبت على حساب المصاريف الإدارية، والدفع من الصندوق، والبيان «اختبار L5».
- حالة المصروف رقم 1 بعد الإنشاء: مسودة، وظهرت إجراءات «تعديل» و«ترحيل» و«حذف» داخل قائمة الصف؛ وهي إجراءات المسودة المتوقعة.
- ترحيل المصروف رقم 1: ناجح؛ أصبح `EXP-0001` مرحّلًا، وظهرت للمدير إجراءات «إعادة تعيين كمسودة» و«إلغاء». لم يُنفذ إجراء عكسي أثناء الاختبار.
- قيد `EXP-0001`: ناجح ومتوازن بقيمة 100.00 EGP؛ مدين `5105 - مصاريف إدارية` ودائن `1101 - الصندوق (النقدية)`، مع وصف يطابق المستند ونوع المصروف وبيان L5.
- غطت الاختبارات اليدوية المطلوبة: المصادقة، وتوجيه الأدوار، ومنع المسار المباشر، والمبيعات، والمشتريات، والمصروفات، والتقارير، وتصدير PDF وExcel.

## نتيجة الإغلاق الموحد

- الحالة النهائية: ناجحة في 2026-09-07.
- تطابقت سياسة التشغيل مع Node `22.23.2` وnpm `10.9.8`، وجميع 97 وصلة حزمة من السجل الرسمي.
- نجح فحص الأسرار والمراجع المحظورة، ونجح فحص بنية جميع سكربتات Shell وNode.
- التدقيق الكامل والإنتاجي: صفر ثغرات.
- ESLint: صفر أخطاء، ونجح حاجز عدم زيادة خط الأساس 997/81. TypeScript: صفر أخطاء.
- Vitest: عدد 50/50 ملفًا و565/565 اختبارًا خلال 99.93 ثانية.
- نجح بناء الإغلاق الوهمي: 178 ملفًا بحجم 6,402,951 بايت، دون عنوان بيئة حقيقية أو ملف عام محذوف.
- بصمة `index.html` في بناء الإغلاق: `382932e890d068423b7eec66095b97d08a9af7c68cbd963355f029ecff57dcba`.
- الأصل الرئيسي `/assets/index-Cz5gOuvK.js` وبصمته `3b71f377b87eccf0a4658fbaebddf91203b8a8e14448d515a990c31fd424b99e`.
- أعاد Staging الحالة 200 في فحص الإغلاق، وحُذف مجلد البناء المؤقت تلقائيًا.
- الديون غير الحاجبة والمراقبة: 997 استخدام `any` و81 تحذيرًا آخر دون سماح بالزيادة، تحذيرا React Router 7 المستقبليان، أجزاء PDF وTelegram وExcel الكبيرة، والاستيراد المختلط لوحدة ترحيل المصروفات.
- قرار القبول: L5 مكتملة؛ لا توجد بوابة آلية أو يدوية معلقة قبل L6.
