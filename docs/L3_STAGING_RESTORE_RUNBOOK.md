# خطة استعادة بيانات Farida إلى Staging

> التاريخ: 2026-09-08
> الحالة: نُفذت الاستعادة ونجح اختبار الصلاحيات؛ اختبارات المنتجات والمخزون والفواتير والتقارير متبقية

## القرار التنفيذي

لن تستخدم الاستعادة الكاملة لقاعدة Farida، ولن تنقل مخططات Supabase الداخلية أو مستخدمي الإنتاج. ستستبدل بيانات الأعمال في `public` على Staging داخل معاملة واحدة، مع الحفاظ على مستخدمي Staging وجداول الصلاحيات الخاصة بهم.

وسيلة التنفيذ المعتمدة هي `supabase db query --linked --file ...` عبر مشروع Staging المرتبط `dunzfxurefzlaamgghys`. أثبت استعلام قراءة فقط أن الاتصال يعمل بدور `postgres` غير superuser، لكنه مالك جميع جداول `public` وعددها 38 ولديه `BYPASSRLS`.

## خط الأساس المحفوظ في Staging

- PostgreSQL: 17.6.
- `auth.users`: ثلاثة صفوف.
- `profiles`: ثلاثة صفوف.
- `user_roles`: ثلاثة صفوف.
- `products`: صفان.
- `sales_invoices`: خمسة صفوف.
- نسخة الرجوع المتحققة: `/backups/staging/before-farida-copy-20260908-1435`.

## ما سيبقى وما سيتغير

- تبقى `auth.users` و`profiles` و`user_roles` و`telegram_settings` دون استبدال.
- تفرغ `telegram_post_log` ولا تستورد سجلات Farida؛ لأنها تشير إلى منتجات Staging القديمة ويجب ألا تبقى بعد استبدال المنتجات.
- تستبدل بيانات 34 جدولاً بالقائمة الموجودة في `EXECUTION_TABLES.txt` داخل حزمة النقل. يظل `RESTORE_TABLES.txt` سجلاً لحزمة المصدر ذات 35 جدولاً.
- يفرغ `audit_log` ولا يستورد من Farida؛ فهو سجل تشغيلي للإنتاج وليس من بيانات الأعمال اللازمة للاختبار، ونسخته محفوظة في تفريغ Farida الكامل.
- ينشأ `lovable_schema_migrations` بمفتاحه الأساسي ثم تستورد سجلاته الـ96 دون إعادة تسمية في هذه المرحلة.
- لا ينسخ `auth` أو `storage` أو `realtime` من Farida.

## سبب عدم تشغيل ملف البيانات الحالي مباشرة

يستخدم `public-data.sql` أوامر PostgreSQL `COPY ... FROM stdin`. لم يثبت بعد أن واجهة Management API التي يستخدمها `db query` تدعم بروتوكول COPY. لذلك يعد الملف مصدراً متحققاً للحزمة، لكنه ليس ملف التنفيذ النهائي.

قبل الاستعادة يجب إنشاء ملف forward بصيغة `INSERT` وملف rollback مماثل لبيانات Staging الحالية، ثم فحص الحجم والتقسيم من دون فقد الذرية. يمنع إرسال ملف COPY إلى `db query` على سبيل التجربة مع بيانات حقيقية.

## ملفات التنفيذ المكتملة

| الوظيفة | المسار | الحجم | SHA-256 |
|---|---|---:|---|
| تقديم بيانات Farida | `/backups/farida/staging-transfer-20260908-1425/forward-execute.sql` | 1,411,254 | `36b5ada9b0a3ca656bf7eeba5b15d93f8bb4640a0e694573785a601fc414a6a8` |
| إدخالات التقديم دون audit | `/backups/farida/staging-transfer-20260908-1425/forward-data-inserts-no-audit.sql` | 1,386,142 | `318576e3b451d4bad5b949ab758545e81785ecde55f943fd4b22dc06e87adc1f` |
| رجوع Staging | `/backups/staging/before-farida-copy-20260908-1435/rollback-execute.sql` | 148,452 | `370cf4a41f42a7faed478a7dc30e18f30422fd411fd09cf8a91192957f688ff6` |
| إدخالات الرجوع | `/backups/staging/before-farida-copy-20260908-1435/rollback-data-inserts.sql` | 125,097 | `b94f938bee9275bbc664a53894da32a4187c0761793989b80935b43cbd61d045` |

## طريقة بناء ملف التنفيذ

1. تحويل بيانات Farida المجمدة إلى `INSERT` باستخدام قائمة TOC الحالية، دون قراءة جديدة من الإنتاج.
2. إنشاء ملف رجوع بصيغة `INSERT` من نسخة Staging الحالية.
3. إنشاء SQL ديناميكي موثق لإسقاط المفاتيح الأجنبية التي تتداخل مع الجداول المستهدفة وإعادتها بالتعريف نفسه.
4. تعطيل `TRIGGER USER` على الجداول المستهدفة فقط أثناء الإدخال؛ لا يمكن الاعتماد على `DISABLE TRIGGER ALL` لأن دور الاتصال ليس superuser.
5. يحيط ملف التنفيذ بكل العمليات بين `BEGIN` و`COMMIT`، ويستخدم استثناءات قبل `COMMIT` عند فشل أي عدد أو شرط.
6. تعاد تسلسلات الأرقام من أوامر `setval` الموجودة في الحزمة، ثم تفحص مقابل أكبر رقم فعلي.

يحافظ ملف التقديم على دقة مبالغ Farida بتحويل أعمدة مبالغ القيود الأربعة إلى `numeric` غير مقيد داخل Staging فقط. كما يصفر `journal_entries.created_by` إلى `NULL` عندما تكون UUID قادمة من مستخدم إنتاج غير منقول؛ العمود اختياري، ولا تتغير القيم المحاسبية. يعيد ملف الرجوع أنواع الأعمدة إلى `numeric(15,2)` وحالة بيانات Staging الأصلية.

## القيود والعلاقات

- يحتوي Staging على 60 مفتاحاً أجنبياً داخل `public` و37 مشغلاً للمستخدم.
- الجداول كلها مملوكة لدور `postgres`، ولذلك يمكن إسقاط القيود وإعادتها وتعطيل مشغلات المستخدم داخل المعاملة.
- `profiles` مرتبط بـ`auth.users`، و`user_roles` مرتبط بـ`auth.users`؛ كلاهما محفوظ.
- `telegram_post_log` مرتبط بـ`profiles` و`products`؛ لذلك يفرغ قبل استبدال المنتجات ولا يستورد من Farida.
- إعادة إضافة المفاتيح الأجنبية قبل `COMMIT` هي اختبار سلامة؛ أي مرجع مكسور يفشل المعاملة ويرجع كل التغييرات تلقائياً.

## بوابات ما قبل التنفيذ

- تطابق SHA-256 لنسخة Farida ونسخة Staging وحزمة النقل.
- بقاء `git status` نظيفاً وعدم وضع ملفات بيانات داخل Git.
- تطابق project ref مع `dunzfxurefzlaamgghys`.
- نجاح ملف تجريبي صغير يستخدم `BEGIN` و`ROLLBACK` عبر `db query --file` دون أثر دائم.
- نجاح فحص صيغة ملف forward وملف rollback، وعدم احتوائهما بيانات `auth` أو Telegram الإنتاجية.
- إيقاف الاختبار اليدوي على Staging خلال نافذة الاستعادة لمنع كتابة متزامنة.

## تحقق ما قبل COMMIT

- أعداد صفوف جداول Farida تطابق خط الأساس في وثيقة التجربة.
- `products=613` و`sales_invoices=97` و`journal_entries=308` و`journal_entry_lines=821`.
- `lovable_schema_migrations=96`.
- تبقى أعداد `auth.users=3` و`profiles=3` و`user_roles=3`.
- `telegram_post_log=0`، وتبقى إعدادات Telegram الخاصة بـStaging دون استبدال.
- لا يوجد رأس قيد غير متوازن.
- تبقى القيود 114 و118 و137 بلا سطور لأنها جزء من حالة Farida الفعلية، وتوثق كعيب معروف لا كفشل نقل.
- يعالج القيد 152 وفق قرار صريح يمنع التقريب الصامت؛ إما مواءمة نوع العمود في Staging مع Farida أو توثيق التقريب المتماثل قبل التنفيذ.
- تعاد المفاتيح الأجنبية والمشغلات، وتنجح جميع اختبارات القيود.

## نتيجة الاختبار المعزول

نُفذت الدورة الكاملة في حاوية PostgreSQL مؤقتة بلا شبكة وبحدود 512 MiB للذاكرة ومعالج واحد، اعتماداً على النسخ المجمدة فقط:

1. نجح التقديم: `products=613`، و`sales_invoices=97`، و`journal_entries=308`، و`journal_entry_lines=821`، و`lovable_schema_migrations=96`.
2. بقيت هوية Staging: `auth.users=3`، و`profiles=3`، و`user_roles=3`.
3. نجح الرجوع: عادت `products=2` و`sales_invoices=5`، وبقيت أعداد الهوية الثلاثة كما هي.
4. حذف الرجوع جدول `lovable_schema_migrations`، وأعاد نوع `journal_entries.total_debit` إلى `numeric(15,2)`.
5. عادت جميع المفاتيح الأجنبية وعددها 60 وجميع مشغلات المستخدم وعددها 37.

هذا يثبت سلامة ملفي التقديم والرجوع منطقياً، لكنه لا يغني عن اختبار قناة التنفيذ الفعلية على Staging بمعاملة تنتهي بـ`ROLLBACK` قبل النقل الحقيقي.

## نتيجة اختبار قناة Staging الفعلية

- project ref المتحقق: `dunzfxurefzlaamgghys`.
- أداة التنفيذ: `supabase@2.116.0 db query --linked --file`، وهي الأداة والإصدار المقرر استخدامهما في الاستعادة.
- أنشأ الاختبار جدولاً باسم فريد داخل معاملة، تحقق من صف الفحص، ثم نفذ `ROLLBACK`.
- أكد ناتج المعاملة أن الجدول لم يبقَ، وأكد استعلام مستقل لاحق النتيجة نفسها.
- بقيت الأعداد `products=2` و`sales_invoices=5` و`auth.users=3` و`profiles=3` و`user_roles=3`.
- النتيجة: قناة التنفيذ تدعم معاملة متعددة التعليمات و`ROLLBACK` دون أثر، وبوابة ما قبل التنفيذ ناجحة.

## نتيجة الاستعادة الفعلية

- نُفذت الاستعادة بعد اعتماد صريح، باستخدام ملف التقديم ذي البصمة الموثقة وعلى project ref الخاص بـStaging فقط.
- نجحت المعاملة، وأكد استعلام مستقل: 613 منتجاً، و97 فاتورة مبيعات، و747 بند فاتورة، و308 قيود، و821 سطر قيد، و96 سجل ترحيل قديم.
- بقي مستخدمو Staging وملفاتهم وأدوارهم الثلاثة، ولم تبقَ مراجع `created_by` معلقة.
- عادت المفاتيح الأجنبية الـ60 ومشغلات المستخدم الـ37، ولا توجد رؤوس قيود غير متوازنة.
- بقيت القيود 114 و118 و137 دون سطور باعتبارها عيباً معروفاً من بيانات المصدر، وليست فشلاً في الاستعادة.
- أكد المستخدم أن بيانات Farida تظهر في واجهة Staging.

## نتيجة قبول الصلاحيات

- المدير: نجح فتح `/users` وظهرت حسابات Staging الثلاثة.
- البائع: نجح فتح `/sales` وظهرت فواتير Farida، بينما أعادت `/users` و`/expenses` منع وصول `403`، ومُنع تقرير المبيعات `/reports/sales`.
- إجراءات الفاتورة المرحلة للبائع اقتصرت على «طباعة» فقط؛ لم تظهر إجراءات التعديل أو إعادة المسودة أو الإلغاء.
- النتيجة: صلاحيات المدير والبائع مطابقة لنطاق العمل المعتمد بعد الاستعادة.

## الرجوع

- أي خطأ قبل `COMMIT` يؤدي إلى rollback للمعاملة نفسها ولا يحتاج استعادة يدوية.
- إذا نجحت المعاملة وفشل الاختبار الوظيفي، ينفذ ملف rollback المتحقق لإرجاع بيانات `public` في Staging إلى نسخة ما قبل النقل.
- لا تستخدم نسخة Farida للرجوع عن Staging، ولا يلمس الرجوع Farida أو Alibea.
- يمنع بدء الاستعادة حتى يصبح ملف rollback قابلاً للتنفيذ بالطريقة نفسها التي سيستخدمها ملف forward.

## ترتيب الاعتماد

1. ~~إنشاء ملفي forward وrollback وفحصهما دون تنفيذ.~~ مكتمل، مع نجاح دورة التقديم والرجوع المعزولة.
2. ~~تجربة معاملة صغيرة تنتهي بـ`ROLLBACK` على Staging والتحقق من عدم بقاء أثر.~~ مكتمل دون أثر.
3. ~~عرض نتيجة الفحص للمستخدم والحصول على اعتماد صريح.~~ مكتمل.
4. ~~تنفيذ الاستعادة في Staging فقط.~~ مكتمل مع تحقق مستقل.
5. اختبار الدخول والصلاحيات والمنتجات والفواتير والتقارير. قيد التنفيذ؛ اكتمل الدخول والصلاحيات، وتبقت المنتجات والمخزون والفواتير والتقارير.
6. بعد الاعتماد تبدأ L3 على Staging؛ يظل الإنتاج خارج النطاق.
