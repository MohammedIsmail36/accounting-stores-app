# خطة الانفصال الكامل عن Lovable

> تاريخ الإنشاء: 2026-09-06
> الحالة: اكتملت L0 وL1 وL2 وL4 وL5 وL6 وL7؛ اكتملت بوابة بيانات Staging الواقعية، وبقي تنفيذ L3 على القواعد وفق `docs/MASTER_EXECUTION_PLAN.md`
> نقطة الأساس: الفرع `main` عند الالتزام `3f63708`

## الهدف

إزالة الاعتماد التشغيلي والبنائي على Lovable، ثم إنشاء مستودع GitHub جديد بتاريخ نظيف، من دون تغيير مصدر نشر الإنتاج الحالي أو قواعد بيانات Farida وAlibea قبل نجاح دورة Staging كاملة واعتماد المستخدم.

المقصود بالانفصال هو ألا يحتاج التثبيت أو البناء أو المصادقة أو الترحيلات أو النشر إلى نطاق أو خدمة أو حساب تابع لـLovable. لا تُحذف الإشارات التاريخية التي تلزم لفهم أصل البيانات أو قرارات الترحيل قبل نقل نسخة مختصرة ومدققة منها إلى سجل أرشيفي مناسب.

## نتيجة الحصر الأولي

| المجال | الموجود حالياً | الأثر | القرار المبدئي |
|---|---|---|---|
| تثبيت الحزم | `package-lock.json` يحتوي 111 رابط `resolved` إلى مخزن Lovable | قد يفشل `npm ci` خارج بيئة Lovable؛ npm 12 رفضها فعلاً بـ`EALLOWREMOTE` | استبدال المصدر بـnpm الرسمي مع تثبيت الإصدارات و`integrity` ثم اختبار نظيف |
| مصادقة الواجهة | `src/integrations/supabase/client.ts` يستورد `previewAuthStorage.ts` الذي يتواصل مع نطاقات محرر Lovable | لا يعمل المسار الخاص على نطاقاتنا، لكنه كود تابع للمنصة داخل حزمة التطبيق | استبداله بتخزين مصادقة محلي مستقل واختبار الدخول/التحديث/الخروج وتعدد التبويبات |
| إعداد Drizzle | `drizzle.config.ts` يقرأ `LOVABLE_DB_MIGRATION_URL` | اسم متغير مرتبط بالمنصة وقد يربك التشغيل المستقل | الانتقال إلى `DATABASE_URL` مع فحص السكربتات والوثائق وعدم تضمين أسرار |
| تتبع الترحيلات | سكربتا النشر يستخدمان `public.lovable_schema_migrations` | الاسم موجود فعلياً في قواعد الإنتاج؛ تغييره المباشر قد يعيد تنفيذ migrations أو يكسر التتبع | ترحيل منفصل ومحمي إلى اسم محايد بعد نسخ السجلات والتحقق؛ يمنع الحذف المباشر |
| Migration قديم | تعليق في migration بتاريخ 2026-04-19 يذكر Lovable Cloud | تاريخي فقط ولا يغير التنفيذ | عدم تعديل migration مطبق؛ توضيح سياقه في سجل الترحيل إن لزم |
| ملفات الإرشاد | إشارات في `LOCAL_SETUP_GUIDE.md` و`ROADMAP.md` و`PRODUCTION_READINESS_AUDIT.md` وخطط التتبع | بعضها تعليمات قديمة وبعضها سجل تدقيق | تحديث التعليمات الحية، وحفظ الوقائع التاريخية بوضوح دون تقديمها كاعتماد حالي |
| Git | `origin` الحالي هو `creative-flame-keeper.git` والفرع `main` نظيف | تغييره على checkout الإنتاج قد يؤثر في `git pull` داخل سكربتات التحديث | إبقاؤه ثابتاً حتى اكتمال المستودع الجديد وStaging؛ لا force-push ولا استبدال مبكر |
| ملفات المنصة | كان `.gitignore` يستبعد `.lovable/`، وكشف الفحص التفصيلي لاحقاً 14 خطة Markdown متعقبة داخل `.lovable/plan` | الخطط مفيدة تاريخياً لكن مسارها مرتبط بالمنصة | نقلها مع تحقق المحتوى إلى `docs/archive/plans` ثم حذف مسار المنصة من cleanroom |

## مستوى التعقيد والخطر

- التعقيد: متوسط؛ أغلب العمل في lockfile والمصادقة والتوثيق، بينما انتقال جدول تتبع الترحيلات يحتاج عناية أعلى.
- خطر التنظيف داخل مساحة مستقلة: منخفض جداً، لأنه لا يغير الملفات التي تخدم النطاقات ولا قواعد البيانات.
- خطر إنشاء مستودع جديد: منخفض، ما دام `origin` الحالي وسكربتات الإنتاج لم تتغير.
- خطر نشر الواجهة المنظفة: منخفض إلى متوسط ويعالج بنشر Staging أولاً ونسخة رجوع وحواجز API.
- خطر تغيير جدول تتبع الترحيلات: متوسط إلى مرتفع إن نُفذ مباشرة؛ لذلك هو دفعة قاعدة بيانات مستقلة بعد نسخة احتياطية وdry-run والتحقق على Staging.

## ضوابط غير قابلة للتجاوز

1. لا يُنفذ التنظيف داخل checkout الإنتاج `/opt/accounting-app` قبل نجاحه في مساحة مستقلة.
2. لا تتغير قواعد Farida أو Alibea خلال تنظيف الملفات أو إنشاء GitHub الجديد.
3. لا تُحذف migration مطبقة ولا يُعاد ترتيب تاريخ migrations.
4. لا يُعاد إنشاء lockfile عشوائياً؛ تثبت الإصدارات الحالية ثم تُغير المصادر فقط، وبعدها تطبق ترقية Vite المحددة كتغيير مستقل قابل للمراجعة.
5. لا تُنسخ ملفات `.env` أو مفاتيح أو نسخ قواعد أو `node_modules` أو `dist` أو مجلدات النسخ الاحتياطية إلى المستودع الجديد.
6. لا يُغير `origin` في checkout الإنتاج قبل اعتماد Staging ووجود نسخة رجوع واختبار أمر التحديث الجديد دون تنفيذ نشر.
7. لا يُستخدم `npm audit fix --force`.
8. كل دفعة لها فرق ملفات محدود، فحوص مستقلة، توثيق، والتزام Git منفصل.

## مراحل التنفيذ

### L0 — تثبيت خط الأساس ومساحة التنظيف

- إنشاء مساحة دائمة مستقلة خارج `/opt/accounting-app` وبلا `.git` من المستودع القديم.
- تسجيل بصمات `package.json` وlockfile والإعدادات وبصمات أصول Staging وFarida وAlibea.
- إعداد قائمة سماح للملفات المنقولة وقائمة منع للأسرار والمخرجات والنسخ الاحتياطية.
- لا نشر ولا تغيير remote في هذه المرحلة.

بوابة القبول: checkout الإنتاج نظيف، البيئات الثلاث HTTP 200، والخدمات والبصمات الحالية لم تتغير.

### L1 — فصل سجل الحزم

- تحويل كل `resolved` تابع لمخزن Lovable إلى `https://registry.npmjs.org/` مع إبقاء الاسم والإصدار و`integrity`.
- تشغيل `npm ci` نظيف عبر npm حديث، ثم فحص عدم بقاء أي نطاق Lovable في lockfile أو npm config المشروع.
- تشغيل التدقيق الكامل والإنتاجي ومقارنة شجرة الحزم قبل/بعد لمنع تحديثات عرضية.

بوابة القبول: صفر روابط Lovable، تثبيت نظيف، ولا اختلاف إصدارات غير مصرح به.

### L2 — فصل مصادقة المعاينة

- إزالة `previewAuthStorage.ts` وربطه من عميل Supabase، أو استبداله بوحدة محايدة إذا كانت هناك حاجة سلوكية مثبتة.
- الحفاظ على إعدادات Supabase الحالية لكل بيئة من متغيراتها الصريحة.
- اختبار تسجيل الدخول، استمرار الجلسة بعد refresh، تسجيل الخروج، انتهاء الجلسة، تعدد التبويبات، ودوري المدير والبائع على Staging.

بوابة القبول: لا نطاقات أو رسائل `postMessage` تابعة لـLovable داخل الحزمة، وكل اختبارات المصادقة ناجحة.

### L3 — تحييد أدوات قاعدة البيانات

- استبدال `LOVABLE_DB_MIGRATION_URL` باسم محايد مثل `DATABASE_URL` في الإعدادات والتعليمات، دون تسجيل قيمة سرية.
- تصميم migration مستقل ينشئ جدول تتبع محايداً وينسخ سجلات الجدول القديم دون فقد أو تكرار.
- تجربة migration وdry-run على Staging أولاً، والتحقق من أن جميع migrations تعتبر مطبقة ولا يعاد أي منها.
- يبقى الجدول القديم دون حذف حتى اعتماد الشركتين ومرور فترة رجوع متفق عليها.

بوابة القبول: تطابق سجل الترحيلات القديم والجديد، وdry-run بلا migrations قديمة معلقة، وعدم تغير بيانات الأعمال.

### L4 — تنظيف السكربتات والوثائق والهوية

- تحديث سكربتات النشر والترحيل لاستخدام الأسماء المحايدة بعد نجاح L3.
- إزالة تعليمات الربط بـLovable من دليل الإعداد وتحديث خريطة المشروع.
- إبقاء الوقائع التاريخية الضرورية في سجل واضح، مع حذف التعليمات المنتهية أو الملفات الترويجية/المولدة إن وجدت.
- فحص نصي شامل، مع تصنيف أي ذكر متبقٍ هل هو تاريخ مطلوب أم اعتماد يجب إزالته.

بوابة القبول: صفر اعتماد تشغيلي أو بنائي على Lovable، وأي ذكر متبقٍ تاريخي ومبرر صراحة.

### L5 — بوابات الجودة والأمن

- TypeScript.
- 50 ملف اختبار و565 اختباراً على الأقل، وفق خط الأساس الحالي بعد اكتمال L4.
- بناء production ومعاينة محلية وdev smoke test.
- `npm audit` كامل وإنتاجي، وفحص أسرار وتوكنات وعناوين قواعد الإنتاج داخل الملفات المتعقبة والبناء.
- بناء Staging بمتغيراته فقط والتحقق من غياب عناوين Farida وAlibea وأي عنوان Lovable.
- اختبار وظيفي يدوي للمصادقة والصلاحيات والمبيعات والمشتريات والمصروفات والتقارير والتصدير.

بوابة القبول: كل الفحوص ناجحة أو أن أي دين قديم موثق ومثبت أنه مطابق لخط الأساس وليس تراجعاً.

### L6 — مستودع GitHub الجديد

- إنشاء مستودع خاص جديد وفارغ باسم يحدده المستخدم.
- إنشاء تاريخ Git جديد من قائمة الملفات المنظفة، بدلاً من دفع تاريخ المستودع القديم بما قد يحويه من آثار أو أسرار قديمة.
- تنفيذ فحص أسرار أخير قبل الالتزام الأول، ثم دفع الفرع `main` وحمايته.
- إبقاء المستودع القديم أرشيفاً خاصاً وعدم حذفه أثناء الانتقال.

بوابة القبول: clone جديد ثم `npm ci` والفحوص والبناء ينجحون دون وصول إلى Lovable أو ملفات خارج المستودع.

الحالة: مكتملة في 2026-09-08. المستودع الجديد هو `accounting-stores-app`، وتفاصيل التاريخ النظيف والفحوص وRuleset موثقة في `docs/L6_REPOSITORY_BOOTSTRAP_REPORT.md`.

### L7 — Staging ثم الإنتاج والرجوع

- ربط Staging أولاً بالمستودع الجديد أو نشر بنائه يدوياً، مع نسخة رجوع وبصمات وحواجز API.
- بعد اعتماد المستخدم، تغيير مصدر checkout الإنتاج في نافذة مستقلة مع اختبار fetch/dry-run قبل pull أو build.
- نشر Farida ثم اختبارها، وبعد الاعتماد نشر Alibea؛ لا نشر متزامن للشركتين.
- عدم تعديل قواعد البيانات ضمن دفعة الواجهة؛ ترحيل جدول التتبع له نافذة منفصلة.

بوابة القبول: النطاقات الثلاثة سليمة، البصمات موثقة، Nginx وDocker وقواعد البيانات مستقرة، وخطة الرجوع مجربة.

## خطة الرجوع

- يظل `/opt/accounting-app` وremote الحالي بلا تغيير حتى L7.
- تحفظ نسخة نشر لكل نطاق قبل استبدال الملفات.
- عند فشل الواجهة: استعادة مجلد النشر السابق وإعادة تحميل Nginx بعد `nginx -t`.
- عند فشل ترحيل تتبع migrations: إيقاف الدفعة وعدم حذف الجدول القديم؛ استخدامه كمصدر الحقيقة حتى تحليل السبب.
- لا rollback لبيانات أعمال عبر Git، ولا حذف لمستودع أو قاعدة أو جدول ضمن خطة الرجوع الآلية.

## تنفيذ L0 وخط الأساس — 2026-09-06

- أُنشئت مساحة التنظيف الدائمة `/opt/accounting-app-cleanroom` بملكية `deploy:deploy` وصلاحية `0750`، ونُقلت إليها الملفات المتعقبة فقط دون `.git` أو `node_modules` أو `dist` أو `coverage` أو `supabase/.temp`.
- تطابقت checksums الملفات المنقولة مع الالتزام `80ab2e6ceb418d00cba636fcccca865a9f5af7c5`، وبقي checkout الإنتاج نظيفاً ومتطابقاً مع `origin/main` عند إنشاء المساحة.
- كشف فحص أسماء الملفات أن `.env` متعقب في المستودع الحالي. لم يُقرأ محتواه ولم يُعرض؛ حُذفت نسخته من مساحة التنظيف فقط، وبقي ملف checkout الإنتاج دون تغيير. يمنع إدراجه في المستودع الجديد، وتراجع قيمه لاحقاً بفحص أسرار لا يطبعها قبل تقرير الحاجة إلى تدويرها.
- قائمة المنع للمستودع الجديد: `.git`، `.env` و`.env.*`، المفاتيح `*.pem` و`*.key`، ملفات service-role والتوكنات، dumps ونسخ قواعد البيانات، مجلدات النسخ الاحتياطية، `node_modules`، `dist`، `coverage`، `supabase/.temp`، logs وملفات المحررات. لا يسمح بقالب `.env.example` إلا بعد مراجعته والتأكد أنه أسماء متغيرات وقيم وهمية فقط.
- شُدد `.gitignore` داخل cleanroom فقط ليمنع ملفات البيئة و`.npmrc` و`.netrc` والمفاتيح وملفات credentials/secrets ونسخ قواعد البيانات والمخرجات والحزم وبيانات التشغيل. أزيل منه بند `.lovable/` وتعليقه كي لا يخفي عودة ملفات المنصة مستقبلاً؛ تأكد عدم بقاء اسم Lovable في الملف. لم يتغير `.gitignore` في checkout الإنتاج.
- أُضيف `scripts/check-repository-safety.sh` إلى cleanroom؛ يفحص أسماء الملفات الحساسة وأنماط المفاتيح المعروفة محلياً، ولا يطبع القيمة عند الاشتباه. كشف أول تشغيل أربعة JWTs لمفاتيح Supabase العامة: مفتاحا Farida وAlibea داخل `scripts/deploy-all.sh` وتكرارهما في `docs/DEPLOYMENT_WORKFLOW.md`.
- أزيلت القيم الأربع من cleanroom فقط. أصبح سكربت النشر يطلب `FARIDA_SUPABASE_PUBLISHABLE_KEY` و`ALIBEA_SUPABASE_PUBLISHABLE_KEY` من بيئة النشر ويفشل قبل نشر الشركة عند غياب متغيرها، وأصبحت الوثيقة تستخدم أسماء المتغيرات بلا قيم. اجتاز Bash syntax ثم اجتاز cleanroom فحص الأسرار بلا نتائج. حُذفت ملفات التحرير المؤقتة المحددة من `/tmp` بعد تثبيت النسخ النظيفة.
- لم تُعدل النسخ التشغيلية من `scripts/deploy-all.sh` أو `docs/DEPLOYMENT_WORKFLOW.md` في checkout الإنتاج ضمن هذه الدفعة؛ سيأتي المستودع الجديد من cleanroom النظيف، بينما يبقى المصدر الحالي ثابتاً حتى اكتمال التحقق.

### بصمات ملفات البناء المرجعية

| الملف | SHA-256 |
|---|---|
| `package.json` | `529d244013ec82e9f47cad1586efaf46921e0594206fbb53a1dc66f03f640c91` |
| `package-lock.json` | `cca7687a37b83e70be963113d4fdb5436b86f5bc8952287a9871bb89d6526d5c` |
| `vite.config.ts` | `aef2deda85c5c704ce6989a916e06d1831fc920adf08349f661bc6ba76b01641` |
| `vitest.config.ts` | `2c64550d05cdec49d120f28afd53056372945035b13d54cbd16f8027329f2a84` |

### بصمات النشر المرجعية

| البيئة | `index.html` | الأصل الرئيسي | SHA-256 للأصل |
|---|---|---|---|
| Staging | `f86be6682086cb363d0de838f158d8a3c43859b123f3b86a28f178cdd0465440` | `/assets/index-DeU0Y8Mc.js` | `eee96a4a927d10fb20477fea8731bf0b5c5725f5bffb95ad0bd79b4846ca940f` |
| Farida | `81b00744793d06a2641ef6b2380dec5a9eb56e8ef29418b88173653fa4cda388` | `/assets/index-JPiGv4kC.js` | `822e13108224e6bf738f853aa632aebeb41d94303c94e67495794aec212bf37d` |
| Alibea | `d3700a9392d163b17ffa547fe22b93643066a008f887dd607e5a31c4020d3c83` | `/assets/index-CmoYbaBh.js` | `8c2139a17e7c0f6321b0234fa8e67f7a849046f9210cc14c1e11614c856aa4ba` |

- أعادت النطاقات الثلاثة HTTP `200` و`text/html` وقت التسجيل. كان Nginx وDocker بحالة `active`، وجميع حاويات Farida وAlibea عاملة، وكل حاوية لها health check ظاهرة بحالة `healthy`.
- لم تتغير ملفات الويب أو إعدادات Nginx أو Docker أو قواعد البيانات أو remote أثناء L0.

## تنفيذ L1 وفصل سجل الحزم — 2026-09-07

- استُبدلت 111 قيمة `resolved` تابعة لمخزن Lovable داخل lockfile في cleanroom بنظائرها على `https://registry.npmjs.org/`. أثبتت مقارنة JSON أن هذا كان التغيير الوحيد أولاً، دون تغيير اسم أو إصدار أو `integrity`.
- كشف npm 12 أن lockfile القديم ناقص بيانات حزم SWC وRollup الاختيارية لأنظمة التشغيل الأخرى. أكمل lockfile بإضافة 33 تابعاً اختيارياً بالإصدارات نفسها وإزالة `yaml@2.9.0` الاختياري غير المطلوب، مع صفر تغييرات إصدار. أضيف `resolved` و`integrity` الرسميان لإدخالي `@swc/core@1.15.46` و`esbuild@0.21.5` الرئيسيين.
- سُجلت سياسة npm 12: رفض سكربت `core-js` والسماح بسكربتي `@swc/core` و`esbuild`. يسجل npm السماح بالاسم بسبب سلوك `install-scripts`، بينما يثبت `npm ci` الإصدار وSHA-512 عبر lockfile؛ لذلك يمنع تغيير lockfile دون مراجعة فرق مستقلة.
- نجح `npm ci` من الصفر من npm الرسمي: 651 حزمة، بلا رابط Lovable وبلا تدخل يدوي أو سكربت غير مراجع. عمل SWC `1.15.46` وesbuild `0.21.5`، واجتاز ماسح الأسرار بلا نتائج.
- لأن `.env` لم يُنقل، كشفت أول دورة اختبار اعتماد سبعة ملفات على متغيرات Supabase. أضيفت قيم وهمية محصورة في `vitest.config.ts` (`supabase-test.invalid` ومفتاح واضح أنه اختباري)، فلا تتصل الاختبارات بقاعدة حقيقية ولا تدخل القيم بناء الإنتاج. بعدها نجح TypeScript و48/48 ملف اختبار و563/563 اختباراً.
- نجح بناء Vite 5 المرجعي بقيم وهمية صريحة: 3936 وحدة خلال 58.37 ثانية، و183 ملفاً بحجم 7,312,389 بايت. بصمة `index.html` هي `1b00c1f98fdb6e3727e6a4c46dcd52c62fcaafef1536638c7941b09e6f644162`، والأصل `/assets/index-BAzPacr4.js` ببصمة `6a5e3dd1c9cb90cc46ce36cced4daba1a94187d166d3aa266fc5dc2b3d812c08`.
- احتوى البناء قيم الاختبار فقط، ولم يحتو نطاق Farida أو Alibea أو Staging ولا عنوان `supabase.co`. بقيت كلمة `lovable` وحدها بسبب `previewAuthStorage.ts` المستخدم حالياً؛ إزالتها هي L2 ولا تمنع إغلاق فصل سجل الحزم L1.
- نتيجة التدقيق بقيت خط الأساس المتوقع قبل ترقية Vite: 8 متوسطة و1 عالية. لا `audit fix --force`؛ ترقية Vite تبقى دفعة مستقلة بعد إزالة الاعتماد التشغيلي على Lovable.

### بصمات L1 داخل cleanroom

| الملف | SHA-256 |
|---|---|
| `package.json` | `5fec4868863008025b6012c7092b3df52c7d69a455c3f813de60a8b37270f843` |
| `package-lock.json` | `b7178f424149236d658c25dbf74b3426b70724494f50aa328e052afa6bd66983` |
| `vitest.config.ts` | `250a8dd7cf7f3a2c6fc6407b690569bb7d6f6d3a0f02900165d1cc61e2e2e2b4` |
| `scripts/deploy-all.sh` | `cb5600e7ecb62a6ebbf5f260d30af6a02e02891ecd9d6f6ff3143ca16bf4af85` |
| `scripts/check-repository-safety.sh` | `8459bca37cbac51c95af6f4d6f576fd95d3923f0cc68c6db69548a470eb67877` |

## تنفيذ L2 وفصل مصادقة المعاينة — 2026-09-07

- أثبتت مراجعة `previewAuthStorage.ts` أنه وسيط خاص بنطاقات معاينة Lovable فقط، وأنه يعيد `localStorage` مباشرة على نطاقات Farida وAlibea وStaging. كما أثبتت مراجعة Supabase JS `2.110.8` أن التخزين الافتراضي مع `persistSession: true` يستخدم `localStorage` في المتصفح ويوفر مزامنة التبويبات عبر `BroadcastChannel`.
- ثُبت عقد السلوك القديم أولاً بثلاثة اختبارات ناجحة لاستمرار قيمة الجلسة واستعادتها وإزالتها خارج نطاقات Lovable. بعد ذلك حُذف `previewAuthStorage.ts` واستيراده وخيار `storage` من عميل Supabase داخل cleanroom فقط.
- أضيف اختبار يمنع إعادة وسيط تخزين خاص بمنصة إلى إعداد عميل Supabase، واختبار تكاملي يثبت أن التخزين الافتراضي لـSupabase يستخدم `localStorage` ويستمر ويستعيد ويحذف قيمة الجلسة. نجح الاختباران المستهدفان، ثم نجح TypeScript بلا أخطاء، ثم نجح كامل الاختبارات: 50/50 ملفاً و565/565 اختباراً.
- اجتاز ماسح الأسرار بلا نتائج. اختفت إشارات Lovable من كود الواجهة؛ بقي اسم متغير Drizzle واسم جدول تتبع migrations في سكربتين فقط، وهما ضمن L3 ولا يجوز تغيير جدول التتبع مباشرة.
- نجح بناء Vite 5 بقيم Supabase وهمية: 3935 وحدة، و183 ملفاً بحجم 7,310,399 بايت. لم تحتو الحزمة Lovable أو نطاقات Farida أو Alibea أو Staging أو أي مشروع Supabase حقيقي، واحتوت قيم الاختبار الوهمية فقط.
- بصمة `index.html` هي `a4d263f2ce2926d390adb2918e10a2fd3fd667e34d370a6769e06232dc84fa1e`، والأصل الرئيسي `/assets/index-J1s2QPsP.js` ببصمة `0d8f74e63b12f361bd4846d6919af8d225030f75661bac15ef0fbf3fef968727`.
- أكدت مقارنة checksum مع المصدر أن الفروق محصورة في L0 وL1 وL2. لم تُنقل `.env` أو بيانات ربط Supabase أو بيانات المحرر أو ملفات TypeScript المؤقتة، ولم يحدث نشر أو تغيير قاعدة بيانات أو تعديل كود في checkout الإنتاج.

### بصمات L2 داخل cleanroom

| الملف | SHA-256 |
|---|---|
| `src/integrations/supabase/client.ts` | `ba148436047edc5c5e624aa5513e9fb1ab76f7a4a48b5fb9efa45267a1398872` |
| `src/integrations/supabase/client.test.ts` | `a8e9ece909ae790834273595b02c390053d0df0b134493ef7c8097ae22c4b33c` |
| `src/integrations/supabase/defaultAuthStorage.test.ts` | `3c2f8aa9849ea30e6a42c7b8ad19fb66a270034221fe3bc99ac14c6ef1b993a0` |
| `src/integrations/supabase/previewAuthStorage.ts` | محذوف من cleanroom |

## تجهيز L3 وفصل إعدادات وتتبع قاعدة البيانات — 2026-09-07

بدأت في 2026-09-08 تجربة L3 ببيانات Farida واقعية على Staging. اكتملت الاستعادة واختبارات الصلاحيات والمخزون والفواتير والتقارير؛ التفاصيل والحدود والمشكلات المكتشفة موثقة في `docs/L3_STAGING_FARIDA_DATA_REHEARSAL.md`.

- استُبدل متغير Drizzle القديم بـ`DATABASE_URL` داخل cleanroom، وأضيف إيقاف صريح إذا كانت القيمة مفقودة أو فارغة. نجح تحميل الإعداد بقيمة وهمية دون اتصال، وفشل عمداً عند غيابها.
- أصبح الاسم التشغيلي لسجل ترحيلات النشر الذاتي `public.app_schema_migrations` في السكربتين، وأصبح مسار المشروع ومجلد migrations يُستنتجان من موقع السكربت بدلاً من الاعتماد على `/opt/accounting-app`. بقي اسما حاويتي Farida وAlibea الافتراضيان كما هما، مع السماح بضبطهما عبر متغيرات لاختبارات معزولة.
- أضيف migration ذري `20260907091000_neutral_migration_tracking.sql`: يعيد تسمية الجدول القديم مع بياناته ومفتاحه الأساسي، ويرفض الحالة المتعارضة التي يوجد فيها جدولان فعليان، وينشئ View مؤقتاً بالاسم القديم لتبقى نسخة السكربت السابقة قابلة للرجوع خلال نافذة الأمان. الإشارات المتبقية إلى الاسم القديم محصورة في هذا migration الانتقالي والتوثيق، وليست اعتماداً تشغيلياً.
- يجهز `migrate-all-companies.sh --prepare-tracking` السجل المحايد قبل فحص التاريخ، ويستدعيه `update-everything.sh` قبل تنظيف سجل migration المخزون. أُصلح خيار `--help` في السكربتين ليكون مستقراً ولا يعتمد على أرقام الأسطر.
- نجحت ثماني بوابات SQL داخل PostgreSQL 15 مؤقت: حفظ السجل، إعادة التسمية، المفتاح الأساسي، View الرجوع، التكرار الآمن، القراءة والكتابة والحذف من النسختين، قاعدة جديدة، والتوقف الآمن عند التعارض. ثم نجح اختبار السكربت الحقيقي من البداية للنهاية وحافظ على سجلين عبر تشغيلين. حُذفت الحاويتان المؤقتتان بعد الاختبارات.
- نجح فحص الأسرار، وصفر إشارات Lovable تشغيلية خارج migration التاريخي، وصفر روابط Lovable في lockfile. نجح TypeScript و50/50 ملفاً و565/565 اختباراً.
- أعيد البناء بالقيم الوهمية وكانت بصمتا الحزمة مطابقتين تماماً لـL2: `index.html` ببصمة `a4d263f2ce2926d390adb2918e10a2fd3fd667e34d370a6769e06232dc84fa1e`، والأصل الرئيسي ببصمة `0d8f74e63b12f361bd4846d6919af8d225030f75661bac15ef0fbf3fef968727`. بقي العدد 183 ملفاً والحجم 7,310,399 بايت، وصفر عناوين بيئات حقيقية أو Lovable في الحزمة.
- **لم يُطبق migration على Staging أو Farida أو Alibea، ولم يُنشر أي بناء.** إغلاق L3 تشغيلياً مشروط بتجربته لاحقاً على Staging وأخذ لقطة قبلها واعتماد خطة الرجوع.

### بصمات كود L3 داخل cleanroom

| الملف | SHA-256 |
|---|---|
| `drizzle.config.ts` | `935cd94b5b730a800ba8fc1e16ed16dc5055995f9dca4b6d78426af9cd4526fc` |
| `scripts/migrate-all-companies.sh` | `ebe2ed0be413191c43d5e1ac05e4e927e4af2e52f4d817471250cb5b1926cf64` |
| `scripts/update-everything.sh` | `df04f6ca10fc233e1223d32e1f7b262baceae1a4ab8106834f31ce3dfb05f92d` (بصمة L3؛ أزيل لاحقاً في L4) |
| `supabase/migrations/20260907091000_neutral_migration_tracking.sql` | `9a6eb211ec127cb8b125d08f71c2aafe4fcd93f721460cd0f0329fa0d9e2ed70` |

## تنفيذ L4 حتى الآن — 2026-09-07

- كشف الفحص التفصيلي 14 خطة متعقبة في `.lovable/plan` وصحح الاستنتاج الأولي في هذا السجل. نُقلت داخل cleanroom إلى `docs/archive/plans`، وتطابق محتوى الملفات الأربعة عشر قبل النقل وبعده دون اختلاف، ثم أزيل مسار `.lovable` الفارغ.
- أعيدت كتابة `docs/LOCAL_SETUP_GUIDE.md` ليعتمد Node 22 وnpm 12 وSupabase CLI مثبت الإصدار، ويجعل migrations مصدر بنية القاعدة، ويمنع خلطها مع `public/full-schema.sql` أو وضع مفاتيح خادمية في `VITE_*`.
- وُسم `docs/PRODUCTION_READINESS_AUDIT.md` بوضوح كسجل تاريخي مع الحفاظ على نتائج التدقيق الأصلية.
- أصبح الاسم التقني المحايد `store-accounting-app` في `package.json` وlockfile و`supabase/config.toml` وREADME، دون تغيير أسماء الشركات أو النطاقات التشغيلية.
- استُبدل favicon القديم بأيقونة محاسبية محايدة متعددة المقاسات، وأضيف تعريفها الصريح في `index.html`، وحذف `public/placeholder.svg` غير المستخدم. طابق favicon الناتج ملف المصدر الجديد ونجح البناء دون تغيير بصمة JavaScript الرئيسية عن L3.
- عُيّن `docs/DEPLOYMENT_WORKFLOW.md` دليلاً تشغيلياً وحيداً و`scripts/deploy-all.sh` نقطة التنفيذ الرسمية. أزيلت منه خطوات النشر اليدوي المتعارضة وأوامر حذف ملفات الويب، ووثقت حدود الرجوع الحالية والنشر المتتابع Farida ثم Alibea.
- نُقلت الأدلة القديمة إلى `docs/archive/deployment`، ونُقل دليل ترقية Node المكتمل إلى `docs/archive/runbooks`. تحمل الملفات الثلاثة تنبيهاً صريحاً بأنها تاريخية وترتبط بالدليل الحالي.
- صُنّف `scripts/update-everything.sh` كسكربت انتقالي لإصلاح مخزون محدد وليس مسار نشر دوري. حُفظ وصف وظيفته وسبب إخراجه في `docs/archive/runbooks/UPDATE_EVERYTHING_2026-09.md` ثم حُذفت نسخته التنفيذية من cleanroom.
- اكتملت مراجعة ملفات `scripts` السبعة وسُجلت في `docs/SCRIPT_INVENTORY.md`: ثلاثة سكربتات تشغيل/أمان دائمة، وسكربتان مقيدان بـStaging، واختبار PostgreSQL يحتاج نقلاً وتحـصيناً، وسكربت انتقالي واحد يجب إخراجه من المسار التشغيلي.
- أثبت البحث أن `update-everything.sh` لا تستهلكه أي أداة أو `package.json`، وأن منطقه الإضافي الوحيد تنظيف سجل `20260831190000_inventory_reports_catchup` عند غياب دالة محددة؛ لذلك لا ينبغي حمله كمسار نشر عام في المستودع الجديد.
- كشف التدقيق أن `test-atomic-sales-documents.sh` لا يلمس بيانات الأعمال لكنه كان يفترض حاوية `farida-db` وينشئ ويحذف قاعدة مؤقتة داخلها. نُقل إلى `scripts/tests/run-atomic-sales-documents.sh` باسم قاعدة محايد، وأصبح يتطلب حاوية صريحة وتأكيد `ALLOW_ISOLATED_DB_TEST=yes` ويرفض اسمي حاويتي الإنتاج المعروفين.
- اكتملت مراجعة SQL خارج `supabase/migrations` وسُجلت في `docs/SQL_INVENTORY.md`. نُقلت لقطة `public/full-schema.sql` القديمة إلى `docs/archive/sql/full-schema-snapshot-2026-02.sql` حتى لا تنشر كملف عام داخل الواجهة، ونُقل إصلاح الضريبة اليدوي إلى الأرشيف نفسه. يحمل الملفان تحذيراً يمنع التنفيذ، وبقي `supabase/migrations` مصدر البنية الوحيد.
- وُحّد اسم قاعدة اختبار المستندات الذرية إلى `store_accounting_atomic_test` داخل المشغل وSQL، واستُبدلت أسماء دالة ومشغل الاختبار المؤقتة بأسماء محايدة. لم يُشغل الاختبار ولم تُنشأ قاعدة.
- أثبت استعلام القراءة الذي نفذه المستخدم أن دالة ومشغلي سلامة القيود المنفردة في Drizzle غير موجودة في Farida ولا Alibea. بناءً عليه نُقلت ملفات Drizzle التجريبية إلى `docs/archive/sql/drizzle-experiments`، وحُذف الإعداد والتعريف الفارغ، وأزيلت حزم `drizzle-kit` و`drizzle-orm` و`postgres` غير المستخدمة.
- حدث lockfile محلياً بلا أي تغيير إصدار مقارنة بالمصدر: أزيلت شجرة Drizzle وملحقاتها فقط، بينما الإضافات الاختيارية لمنصات Rollup وSWC هي إصلاح L1 السابق. نجح `npm ci --offline`، وأظهر التدقيق المتاح صفراً في جميع درجات الثغرات بعد خروج شجرة Drizzle القديمة.
- نجح TypeScript، ثم 50/50 ملف اختبار و565/565 اختباراً. نجح بناء وهمي مستقل وأنتج 181 ملفاً بحجم 7,309,515 بايت؛ غاب `full-schema.sql` و`placeholder.svg` عن `dist`، وطابق favicon بصمته المعتمدة، ولم تظهر عناوين البيئات الحقيقية أو Lovable في البناء.
- بصمة `dist/index.html` بعد التنظيف هي `09a8b3f2c377662f34782699a63d5ae2a837f3a20869bc9784a8901435601e5f`، والأصل الرئيسي `/assets/index-DQ9d86fg.js` ببصمة `5a6418ec2e17d6b87f538bfe812a3fe0b1798da495fa70c1d545ef250d13e39f`. استخدم البناء مفتاحاً وعنواناً وهميين، لذلك لا تقارن بصمة الأصل ببناء ذي متغيرات مختلفة.
- اكتملت مراجعة ملفات الجذر و`public` وسُجلت في `docs/PROJECT_FILE_INVENTORY.md`. كل ملفات الجذر الحالية لها وظيفة معروفة، و`node_modules` و`dist` مستبعدان من Git، ولم توجد ملفات تحرير أو نسخ مؤقتة في الجذر.
- حُذف خطا Amiri غير المستخدمين بعد أن أثبت البحث أن توليد PDF يعتمد حصراً ملفات Tajawal الثلاثة. عُدل `robots.txt` إلى `Disallow: /` وأضيف `noindex, nofollow, noarchive` إلى مدخل التطبيق لأن النظام داخلي ولا ينبغي فهرسته.
- اكتُشف غياب إعداد Alibea من `deploy/nginx` رغم وجود إعداد Farida وStaging. أضيف `deploy/nginx/alibea.conf` من الملف المركب فعلياً على الخادم، وأثبتت المقارنة تطابق التوجيهات بعد تجاهل التعليقات والمسافات. لم يُركب الملف ولم يُعد تحميل Nginx.
- بقي `public/links.html` دون حذف لأن طلبات HEAD أكدت أنه منشور بحالة 200 على النطاقات الثلاثة. الملف غير مستدعى من التطبيق، وهو صفحتان قديمتان ملتصقتان وفيه روابط وهمية، وتاريخه مرتبط بتحرير المنصة؛ يحتاج قرار المالك إن كان رابطاً خارجياً مستخدماً قبل أن يخرج من `public`.
- نجح بناء الأصول بعد المراجعة: 3935 وحدة، و179 ملفاً بحجم 6,464,840 بايت. حُذف ملفا Amiri من المصدر والبناء، وبقيت ملفات Tajawal الثلاثة و`links.html` متطابقة، وظهر منع الفهرسة في `dist/index.html` و`dist/robots.txt`.
- بصمة بناء المراجعة: `dist/index.html` = `e13378999f8a092cd3bfe37f9cff33f95d4b037cec0b0e8bb468442c20ade8f9`، والأصل الرئيسي `/assets/index-CKxbFJxc.js` = `0ce8d2420ab9151d359058403e1b914757399f01453b0df6ef5b8b0bf8b5d8ed`. لم يحدث نشر أو تعديل Nginx فعلي.
- بعد قرار المالك أُزيل `public/links.html` ونسخته من `dist` داخل cleanroom فقط، ووُثقت إعادة بنائه لاحقاً في `docs/DEFERRED_CUSTOMER_LINKS_PAGE.md`. لم تُحذف النسخ المنشورة من النطاقات ولم يتغير Nginx.
- صُحح `docs/ROADMAP.md`: حاجز سلامة القيود المؤجل لم يكن مكتملاً؛ عناصره ظهرت فقط في تجربة Drizzle وأثبت استعلام القاعدتين غيابها. أصبح بنداً مفتوحاً يتطلب migration رسمياً منفصلاً، ولم يُنشأ هذا migration ضمن دفعة التنظيف.
- اكتمل الفحص الشامل للمنصة ووُثق في `docs/PLATFORM_REFERENCE_AUDIT.md`: صفر اعتماد تشغيلي أو بنائي، وصفر مسار منصة، وصفر اسم قالب أو مستودع أو معرف مشروع قديم خارج السجلات، وجميع 97 قيمة `resolved` في lockfile من `registry.npmjs.org`.
- بقيت خمسة ملفات فقط تذكر المنصة لسبب موثق: ثلاثة سجلات تاريخية، وتعليق migration مطبق لا يجوز تعديله، وmigration الانتقال الآمن لسجل الترحيلات الذي يحتاج الاسم القديم لنقل البيانات وView الرجوع. ملف التدقيق وسكربت الحاجز يذكران أنماط المنع بوصفهما أداتي رقابة.
- وُسع `scripts/check-repository-safety.sh` ليمنع عودة اسم أو مسار أو حزمة منصة خارج قائمة السماح المغلقة. نجح على cleanroom، ثم اختُبر سلبياً بمرجع مصطنع داخل مجلد مؤقت فرفضه كما يجب؛ حُذف المجلد المؤقت بعد الاختبار.
- لم تُعدل سكربتات الإنتاج في هذه الدفعة، ولم يُطبق migration، ولم يتغير Git remote أو Nginx أو Docker أو قواعد البيانات أو ملفات المواقع.

## الحالة الحالية والخطوة التالية

- مكتمل داخل نسخة العمل الأساسية: L0 وL1 وL2 وL4 وL5 وL6 وL7، وتجهيز واختبار كود L3 دون تطبيقه على القواعد. صفحة روابط العملاء مؤجلة ولا تعيق الإغلاق.
- بوابتا L4 وL5 ناجحتان: صفر اعتماد تشغيلي أو بنائي على المنصة، وفحوص الجودة والأمان والبناء والاختبار اليدوي مكتملة وفق `docs/L5_ACCEPTANCE_REPORT.md`.
- اكتملت استعادة بيانات Farida الواقعية إلى Staging وقبول الصلاحيات والمخزون والفواتير والتقارير.
- لم يبدأ: التطبيق المقيد لـL3 على Staging أو أي قاعدة إنتاجية. ترتيب التنفيذ الملزم في `docs/MASTER_EXECUTION_PLAN.md`.
- اكتملت L6: أُنشئ تاريخ Git النظيف ودُفع إلى `accounting-stores-app`، واجتاز clone نهائي التثبيت والتدقيق الصفري وESLint وTypeScript و50/50 ملفاً و565/565 اختباراً والبناء والمعاينة. عولجت Vite وReact Router وExcelJS/UUID في دفعات مستقلة موثقة.
- فُعل Ruleset باسم `Protect main` على الفرع الافتراضي بحالة `Active` وقائمة تجاوز فارغة، لمنع حذف `main` وforce push مع إبقاء الدفع العادي متاحاً خلال الانتقال.
- بدأت L7 واعتمدت بوابة Staging. أُخذت نسخة Farida مرجعية. أعادت محاولة النشر الأولى `401` بسبب علامات اقتباس بقيت ضمن المفتاح المستخرج كنص خام؛ وبعد إضافة حاجز الصيغة نجح البناء الجاف، لكن محاولة النشر الثانية أثبتت أن المفتاح نفسه مأخوذ من `.env` قديم ويخص مصدراً آخر رغم صحة صيغته. لم يكن الخلل في التطبيق أو قاعدة البيانات.
- استُعيدت النسخة المرجعية فوراً بعد كل محاولة، وعادت بصمتها واستجابة API إلى 200 ولم تمس Alibea أو أي قاعدة بيانات. طابق `ANON_KEY` في إعداد Supabase الذاتي لـFarida المفتاح الموجود في البناء المرجعي، وحُفظ كمصدر Farida داخل ملف نشر محمي خارج Git في `/etc/accounting-stores-app/deploy.env`.
- وُسع حاجز سكربت النشر: بعد رفض المفتاح المفقود أو المقتبس أو المشوه، يتحقق فعلياً من `company_settings` على API الخاص بالشركة ويشترط HTTP 200 قبل أي سحب أو migration أو بناء. يمنع ذلك مفتاحاً صحيح الصيغة تابعاً لقاعدة أخرى.
- اجتاز الحاجز المحدث الاختبارين السلبي والإيجابي، ثم نجح البناء الجاف والنشر المقيد لواجهة Farida من الالتزام `5d8438f`. تطابقت ملفات البناء والنشر، وأعادت الواجهة وAPI الحالة 200، وأكد المستخدم الدخول وتقرير المبيعات وغياب أخطاء Console. بقيت Nginx وDocker والحاويات والنطاقات الثلاثة سليمة، ولم تُطبق migrations أو Edge Functions.
- اعتمدت Farida على بناء المستودع الجديد بعد اجتياز بوابات المفتاح والرجوع والبناء الجاف والاختبار اليدوي. التفاصيل والبصمات موثقة في `docs/L7_PRODUCTION_TRANSITION_REPORT.md`.
- اكتملت بوابات Alibea بالتتابع: طابق مفتاحها إعداد Supabase الذاتي، وحُفظ في ملف البيئة المحمي، وأُخذت نسخة رجوع مطابقة، ثم نجح البناء الجاف والنشر المقيد دون migrations أو Edge Functions. طابقت ملفات النشر البناء وأعادت الواجهة وAPI الحالة 200، وأكد المستخدم سلامة النظام.
- أصبح `/opt/accounting-app` هو checkout النظيف الوحيد المعتمد و`origin` الخاص به `accounting-stores-app` عند `29ebc77`. نُقل checkout القديم إلى نسخة رجوع محمية، ولم يعد دفع Lovable إلى المستودع القديم مؤثراً في الخادم أو الإنتاج.
- اكتمل جرد التنظيف: 16 مجلد اختبار داخل `/tmp` بحجم إجمالي يقارب 2.6GiB، وcheckout قديم معزول بحجم 571MiB. لا تعتمد عليها عملية أو خدمة أو إعداد تشغيل، والتغييرات التجريبية في النسخ المؤقتة موثقة كمُنقولة ومثبتة في L6، بينما checkout القديم نظيف ومتزامن مع مستودعه. بعد تثبيت الجرد في Git تُحذف الأهداف المحددة فقط؛ تبقى نسختا رجوع Farida وAlibea وقواعد البيانات وملفات المواقع خارج نطاق الحذف.

## تنفيذ L5 — سياسة بيئة التشغيل

- اعتمد Node 22 فقط عبر `.nvmrc` بالإصدار المختبر `22.23.2`، وحدد `package.json` النطاق `>=22.0.0 <23` لمنع التشغيل بإصدار قديم أو القفز غير المقصود إلى إصدار رئيسي أحدث.
- اعتمد npm من `10.9.0` إلى ما قبل `13` لأن cleanroom نجح على `10.9.8` كما نجحت أعمال L1 السابقة على `12.0.2`. لم تُفرض ترقية npm على خادم الإنتاج.
- نجحت بوابة أمان L5 للاعتماديات والمستودع: صفر ثغرات في التدقيق الكامل والإنتاجي دون شبكة، ونجح حاجز الأسرار والملفات الحساسة، وجميع 97 وصلة حزمة من سجل npm الرسمي. التفاصيل في `docs/L5_ACCEPTANCE_REPORT.md`.
- نجح TypeScript في L5، بينما كشف ESLint دينًا سابقًا مقداره 1295 خطأ و119 تحذيرًا؛ منها 284/38 داخل الأرشيف و914/81 داخل `src`. لم يُنفذ إصلاح تلقائي، وسُجلت البوابة كغير مكتملة حتى تضييق نطاق الأرشيف ثم معالجة المصدر على دفعات.
- استُبعد `docs/archive/**` وحده من ESLint لأنه تاريخي وغير تشغيلي. أصبح خط الأساس الحقيقي 1011 خطأ و81 تحذيرًا في 113 ملفًا: 997 خطأ `no-explicit-any` و14 خطأ من قواعد أخرى، مع صفر ملف أرشيف داخل الفحص.
- عولجت الأخطاء الأربعة عشر غير المتعلقة بـ`any` يدويًا، ومنها خطآن حقيقيان في ترتيب Hooks بصفحة إدارة القوائم. نجح TypeScript و76/76 اختبارًا مستهدفًا، وأثبت ESLint بقاء 997 خطأ `no-explicit-any` فقط وصفر أخطاء من القواعد الأخرى.
- ثُبت دين `any` كخط أساس مراقب: يظهر كتحذير، ويفشل `npm run lint` عند أي خطأ أو عند زيادة 997 تحذير `any` أو 81 تحذيرًا آخر. نجح الاختبار السلبي عند إضافة النتيجة رقم 998، ثم حُذف الملف المؤقت وعادت البوابة وحاجز الأمان إلى النجاح.
- نجحت دورة Vitest الكاملة في L5: عدد 50/50 ملفًا و565/565 اختبارًا خلال 74.79 ثانية دون فشل. سُجل تحذيرا React Router 7 المستقبليان كدين ترقية غير حاجب.
- نجح بناء الإنتاج المعزول بقيم وهمية: 3935 وحدة و178 ملفًا بحجم 6,402,962 بايت، دون نطاقات أو معرفات بيئات حقيقية أو ملفات عامة محذوفة. وُثقت البصمات، وسُجلت أجزاء PDF وTelegram وExcel الكبيرة وتحذير الاستيراد المختلط كديون أداء غير حاجبة.
- نجح بناء Staging المعزول: 3935 وحدة و178 ملفًا بحجم 6,404,469 بايت. احتوى عنوان المشروع والمفتاح العام في ملف واحد فقط، وصفر مفتاح `service_role` وصفر عنوان إنتاج أو مشروع قديم، وظهرت علامة البيئة التجريبية في البناء. لم يُنشر الناتج.
- نجح smoke test لخادمي `dev` و`preview` على localhost فقط: أعادت مسارات الجذر والدخول وتقرير المبيعات الحالة 200، وطابق أصل `preview` ملف البناء بايتًا ببايت. أُوقفت الخوادم، وأثبت إغلاق المنفذين، وحُذف البناء المؤقت.
- نُشرت حزمة القبول على Staging فقط بعد نسخة رجوع دائمة. كشفت محاولتان مع رجوع تلقائي أن جذر `mktemp` ذي الصلاحية 700 ينتقل عبر `rsync -a` ويمنع Nginx؛ بعد تثبيت المجلدات 755 والملفات 644 نجحت المسارات الثلاثة محليًا وخارجيًا. حُصن سكربت النشر الرسمي بالصلاحيات و`--delete-delay`، ولم يتغير الإنتاج أو قواعد البيانات.
- بدأ الاختبار اليدوي على Staging: نجح دخول حساب المدير، وفتح لوحة التحكم، وظهور علامة البيئة التجريبية بتأكيد المستخدم.
- نجح توجيه حساب البائع بعد الدخول مباشرة إلى «فواتير المبيعات» بتأكيد المستخدم.
- نجح حاجز المسار المباشر لحساب البائع: فتح `/expenses` أعاد صفحة «غير مصرّح بالوصول» ولم يعرض المصروفات.
- نجح حاجز المستند المرحّل لحساب البائع: عند فتح فاتورة مبيعات مرحّلة ظهر إجراء الطباعة فقط، دون الإلغاء أو إعادة المسودة أو التعديل.
- نجح إنشاء فاتورة مبيعات جديدة بحساب البائع وحفظها كمسودة على Staging؛ أُنشئت الفاتورة رقم 5.
- نجح تعديل المسودة رقم 5 وحفظها مجددًا دون إنشاء رقم جديد، وثبتت الكمية المعدلة بعد إعادة فتح الفاتورة.
- نجح ترحيل الفاتورة إلى `INV-0005`، وبعد الترحيل بقي للبائع إجراء الطباعة فقط، ما يثبت مسار المبيعات وحاجز المستند المرحّل.
- نجح ظهور `INV-0005` في تقرير المبيعات لحساب المدير ضمن «هذا اليوم» وعرض «الفواتير».
- نجح فتح قائمة تصدير تقرير المبيعات كاملة وتنزيل ملف PDF وفتحه، ما يؤكد إصلاح مشكلة القائمة المقصوصة ومسار PDF.
- نجح تصدير Excel وفتح الملف وظهور بيانات `INV-0005` داخله.
- نجح إنشاء فاتورة مشتريات جديدة بحساب المدير وحفظها كمسودة رقم 1 على Staging.
- نجح تعديل مسودة المشتريات رقم 1 وحفظها مجددًا مع ثبات رقمها وظهور الكمية الجديدة بعد إعادة الفتح.
- نجح ترحيل المشتريات إلى `PUR-0001`، وظهرت للمدير إجراءات إعادة المسودة والإلغاء والطباعة بما يطابق شروط الصلاحيات الحالية؛ لم يُنفذ إجراء عكسي أثناء الاختبار.
- نجح إنشاء مصروف Staging رقم 1 بحساب المدير، مربوطًا بنوع مثبت على المصاريف الإدارية ومدفوعًا من الصندوق، بالبيان «اختبار L5».
- ظهر المصروف رقم 1 كمسودة مع إجراءات التعديل والترحيل والحذف المتوقعة.
- نجح ترحيل المصروف إلى `EXP-0001`، وظهرت للمدير إجراءات إعادة المسودة والإلغاء دون تنفيذ أي منهما.
- ظهر قيد `EXP-0001` متوازنًا: 100.00 EGP مدين على المصاريف الإدارية و100.00 EGP دائن من الصندوق، بوصف مطابق للمستند.
- اكتملت تغطية الاختبار اليدوي لـL5 على Staging: المصادقة والصلاحيات والمبيعات والمشتريات والمصروفات والتقارير وتصدير PDF وExcel.
- اجتازت بوابة الإغلاق الموحدة L5: سياسة Node، وفحص الأسرار، وبنية السكربتات، وصفر ثغرات، وESLint baseline، وTypeScript، و50/50 ملفًا و565/565 اختبارًا، وبناء وهمي معزول، واستجابة Staging 200. أغلقت L5 كمرحلة مكتملة.
