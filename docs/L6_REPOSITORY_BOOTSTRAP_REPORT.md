# تقرير تجهيز مستودع L6 النظيف

> التاريخ: 2026-09-07
> النطاق: `/opt/accounting-app-cleanroom` فقط
> الحالة: الالتزام الأول وربط GitHub ودفع `main` وبوابات Vite وReact Router وExcelJS/UUID مكتملة ومرفوعة ومتحققة من clone نهائي؛ حماية الفرع واختبار Staging ما زالا مفتوحين

## الهدف

إنشاء تاريخ Git جديد من النسخة المنظفة، دون حمل تاريخ المستودع القديم أو ملفاته الحساسة، مع إبقاء checkout الإنتاج `/opt/accounting-app` ومصدر نشره الحالي دون تغيير.

## ما تم

- أُنشئ مستودع Git فارغ محلياً بفرع `main` داخل cleanroom، وكان عدد الالتزامات عند الإنشاء صفراً.
- أضيفت `.gitattributes` لتوحيد الملفات النصية على LF وتعريف الأصول الثنائية الشائعة صراحة.
- رُحّلت الملفات المرشحة إلى الفهرس فقط؛ جميع الحالات كانت إضافات جديدة لأن التاريخ نظيف.
- مُنع إدراج `.env` وملفات البيئة، و`node_modules`، و`dist`، و`coverage`، و`supabase/.temp`، والمفاتيح والنسخ الاحتياطية والمخرجات التشغيلية وفق `.gitignore` وحاجز السلامة.
- أضيف `origin` داخل cleanroom فقط إلى المستودع الخاص الجديد `https://github.com/MohammedIsmail36/accounting-stores-app.git` بعد إثبات أنه متاح وفارغ تماماً، ثم دُفع فرع `main` لأول مرة مع ضبط تتبعه لـ`origin/main`.
- لم يُعدّل remote داخل checkout الإنتاج، ولم يتغير Nginx أو Docker أو أي قاعدة بيانات.

## تنظيف فرق Git

- كان العدد الأولي 498 ملفاً مرشحاً، وأصبحت الملفات المرحّلة 499 بعد إضافة `.gitattributes`.
- بعد توحيد LF كشف `git diff --cached --check` عدد 163 ملاحظة مسافات نهائية موزعة على 34 ملفاً قديماً.
- أزيلت المسافات النهائية ميكانيكياً فقط، ثم أزيل سطر فارغ إضافي من نهاية 9 ملفات.
- اجتاز فرق الفهرس بعدها `git diff --cached --check` بلا ملاحظات.

## نتائج بوابة ما قبل الالتزام

| الفحص | النتيجة |
|---|---|
| حاجز الأسرار والملفات المحظورة | ناجح؛ لا أسماء محظورة ولا أنماط أسرار معروفة |
| ESLint baseline | ناجح؛ صفر أخطاء، و`explicit_any` = 997/997، والتحذيرات الأخرى = 81/81 |
| TypeScript `tsc --noEmit` | ناجح |
| حالات Git المرحّلة قبل إضافة هذا التقرير | 499 إضافة جديدة فقط |
| روابط رمزية مرحّلة | صفر |
| ملفات مرحّلة أكبر من 5 MiB | صفر |
| أكبر ملف | `package-lock.json` بحجم 230,596 بايت |

## القيود والخطوة التالية

- هذا التقرير لا يعلن اكتمال L6؛ أُنشئ الالتزام الأول وربط `origin` ودُفع `main`، وعولج تنبيه Vite ثم React Router. اجتازت معالجة ExcelJS/UUID البوابة التقنية ونُقلت إلى cleanroom لكنها لم تُثبت أو تُدفع بعد. تبقى حماية الفرع والاختبار الوظيفي على Staging مهمتين معلقتين.
- أُعيدت بوابة فرق Git وحاجز السلامة بعد إضافة التقرير وقبل إنشاء الالتزام الأول.
- لا يُغير `origin` في `/opt/accounting-app` أثناء L6، ولا يُنشر شيء إلى الإنتاج ضمن هذه المرحلة.

## بوابة clone الجديد وترقية Vite الأمنية

- أُنشئ clone معزول من GitHub في `/tmp/accounting-stores-app-l6-clone.yNDr4Y`. طابق الالتزام المرفوع، واحتوى تاريخاً من التزام واحد و500 ملف متعقب، وكان نظيفاً دون `.env` أو `node_modules` أو `dist` أو `coverage` أو `supabase/.temp`.
- نجح `npm ci` على Node `22.23.2` وnpm `10.9.8` من `registry.npmjs.org`: ثُبتت 637 حزمة، وبقيت الملفات المتعقبة نظيفة.
- كشف التدقيق المتصل بالسجل 6 نتائج حديثة: 5 متوسطة وواحدة عالية؛ الإنتاجي منها 4 متوسطة. يفسر تحديث قاعدة التنبيهات اختلافها عن نتيجة L5 غير المتصلة بالشبكة، ولا يعني تغير كود أو اكتشاف اختراق.
- كانت النتيجتان الخاصتان بأدوات التطوير في `vite@5.4.21` و`esbuild@0.21.5`. اختير `vite@6.4.3` كأصغر إصدار خارج النطاق المتأثر ومتوافق مع Node 22 و`@vitejs/plugin-react-swc@3.11.0` و`vitest@3.2.7`، بدلاً من القفز غير الضروري إلى Vite 8.
- أثبت الفرق الدلالي للـlockfile أن تغير الإصدارات محصور في Vite وesbuild إلى `0.25.12`؛ بقية الفرق أصول esbuild الاختيارية للمنصات أو metadata بلا تحديثات وظيفية جانبية.
- بعد الترقية نجح ESLint بصفر أخطاء وخط أساس `997/81`، ونجح TypeScript، ثم نجحت 50/50 ملفات اختبار و565/565 اختباراً خلال 67.76 ثانية.
- نجح بناء إنتاج معزول بقيم Supabase وهمية: 3704 وحدات و178 ملفاً بحجم 6,424,293 بايت، دون أي عنوان Farida أو Alibea أو Staging. بصمة `index.html` هي `39445eab1db5c499279dfdadc0dfb51699042a877ff5e1ee6c23a96916a03bb3`، والأصل الرئيسي `/assets/index-DjaDY6yv.js` ببصمة `731a90da2775da651fa2223d3c3325f059ea24fcb598325ecb6300ad42f2b80a`.
- اختفى التنبيه العالي ونتيجة esbuild؛ بقيت 4 نتائج متوسطة إنتاجية في سلسلتي React Router وExcelJS/UUID، وستعالجان كتغييرين مستقلين دون `npm audit fix --force`.
- نُقلت نتيجة Vite المتحققة بالبايت إلى cleanroom وثُبتت في commit مستقل ودُفعت إلى `origin/main`: `package.json` ببصمة `6937240c68d9d04b02515211e12bdd3717c14581e162dd45bca7ec82af74d142` و`package-lock.json` ببصمة `6109d37ada63442f54f579d513c277bff54d5f4a4799cf4f74127bbddc5e0411`. لم يحدث أي نشر للتطبيق.

## إعادة التحقق من الالتزام المرفوع

- أُنشئ clone نهائي جديد من GitHub في `/tmp/accounting-stores-app-l6-final.jBQgYI` عند الالتزام `0d32c637dd51060f609b1c69b3e21e02ca3d7287`. احتوى تاريخاً نظيفاً من التزامين و500 ملف متعقب، وثبتت فيه قيمتا Vite `^6.4.3` في `package.json` و`6.4.3` في lockfile قبل التثبيت.
- نجح `npm ci` من npm الرسمي: 637 حزمة، مع Vite `6.4.3` وesbuild `0.25.12` وشجرة عمل نظيفة. أعاد التدقيق 4 نتائج متوسطة معروفة وصفر عالية أو حرجة.
- نجح ESLint بصفر أخطاء وخط أساس `997/81`، ونجح TypeScript دون أخطاء.
- نجحت مجموعة Vitest كاملة: 50/50 ملف اختبار و565/565 اختباراً خلال 65.37 ثانية، وبقيت الملفات المتعقبة نظيفة.
- نجح بناء إنتاج نهائي بقيم Supabase وهمية: 3704 وحدات و178 ملفاً بحجم 6,424,295 بايت، وصفر مراجع لبيئات Farida وAlibea وStaging.
- بصمة `index.html` النهائية هي `f315291fb6c0854475d68399175fc474599404bea667f13aa7e327cd2a115efc`، والأصل الرئيسي `/assets/index-D4UjMd6Q.js` ببصمة `d17ae3dff181cd3251f4948db6f8931575142cd2a0c5c27cefcb60ff3ff374e8`.
- بقي تحذير الاستيراد المختلط لوحدة المصروفات وتحذير الأجزاء الأكبر من 800KB كما في خط الأساس؛ لم يظهر تحذير توافق جديد من Vite 6.
- لم يُنشر هذا البناء، ولم يتصل بقاعدة بيانات، ولم تتغير أي بيئة تشغيلية. يبقى مجلدا clone المؤقتان حتى انتهاء تجارب الاعتماديات ثم يحذفان صراحةً ويوثق الحذف.

## ترقية React Router إلى 7.18.3

- راجعت تعليمات الترقية الرسمية عند الوسم `react-router@7.18.3`. التطبيق يحقق الحد الأدنى Node 20 وReact/React DOM 18، ويستخدم Declarative/Component Router لا Data Router أو SSR.
- يوجد 44 ملف تشغيل وملفا اختبار يستوردان من `react-router-dom`. أبقينا الاستيرادات مدعومة كما هي في v7 لتجنب فرق ميكانيكي واسع؛ يمكن نقلها إلى `react-router` لاحقاً قبل أي انتقال إلى v8.
- المسار متعدد المقاطع الوحيد المنتهي بـ`/*` هو `/reports/inventory-turnover/*` ويستخدم تحويلاً مطلقاً، فلا يتأثر بتغير دلالة الروابط النسبية. جميع `React.lazy` معرفة في نطاق الوحدة خارج المكونات.
- فُعلت `v7_startTransition` و`v7_relativeSplatPath` أولاً على `BrowserRouter` و`MemoryRouter` أثناء بقاء v6. نجح TypeScript و12/12 اختباراً مستهدفاً ثم 50/50 ملف اختبار و565/565 اختباراً خلال 73.18 ثانية، واختفت تحذيرات Future Flags.
- رُقي `react-router-dom` و`react-router` من lockfile `6.30.6` إلى `7.18.3`. أزيل `@remix-run/router@1.23.4`، وأضيف التابعان الرسميان `cookie@1.1.1` و`set-cookie-parser@2.7.2` فقط؛ لم تتغير حزمة أخرى دلالياً.
- انخفض `npm audit` الكامل والإنتاجي من 4 نتائج متوسطة إلى نتيجتين متوسطتين فقط في ExcelJS/UUID، مع صفر عالية أو حرجة.
- نجح ESLint بصفر أخطاء وخط أساس `997/81` ونجح TypeScript. نجحت اختبارات التنقل والحماية والمسارات الداخلية 37/37، ثم المجموعة الكاملة 50/50 ملفاً و565/565 اختباراً خلال 63.29 ثانية دون تحذير Router.
- نجح بناء إنتاج معزول بقيم وهمية: 3712 وحدة و178 ملفاً بحجم 6,440,977 بايت، وصفر عناوين Farida أو Alibea أو Staging. بصمة `index.html` هي `7ce56fef60402e474e37cbd3050a17050ee9398a6143836cf41a6746e6c3e64e`، والأصل `/assets/index-Bgfq3JTv.js` ببصمة `eb102de3699d072ad47b534be599ced81cc17dffbe07bcf05dac249fd9a80646`.
- نجح smoke test على localhost لخادمي `dev` و`preview`: أعادت `/` و`/auth` و`/reports/sales` الحالة 200، وطابق أصل preview ملف البناء بايتاً ببايت. أُغلقت المنافذ 41741 و41742 وتأكد غياب المستمعين.
- نُقلت الملفات الخمسة المطابقة بالبايت إلى cleanroom وثُبتت مع توثيقها في commit مستقل ودُفعت إلى `origin/main`: `package.json` ببصمة `62ba63fa6cacaa4037ef75bbb7e5ff91694d5f6a567f3a3884ab7a436dee2493`، وlockfile ببصمة `57f03e729376a8dc50ba9f67515b4aaaf8426f8e8c6f7aa9f5792624ba82c2f5`، و`src/App.tsx` ببصمة `02e129987a926645ced83f08facb05629f48230d065c9a79fd7146ca35e93019`. لم يحدث نشر للترقية.

## إغلاق التنبيه المتبقي ExcelJS/UUID تقنياً

- نُفذت التجربة في clone مستقل من الالتزام `20e12dff01be60d3ffa3524dd9ea1243727e6209`، ولم تمس checkout الإنتاج أو Nginx أو Docker أو قواعد البيانات.
- أضيف override متداخل تحت `exceljs` فقط يثبت `uuid@11.1.1`. بقي `exceljs@4.4.0`، ولم يُخفض الإصدار ولم يُستخدم `npm audit fix --force`.
- احتفظ lockfile بعدد 723 سجل حزمة؛ التغير الدلالي الوحيد هو UUID من `8.3.2` إلى `11.1.1`. أعاد التدقيق الكامل والإنتاجي صفراً في الحرجة والعالية والمتوسطة والمنخفضة.
- نجح اختبار مستهلك ExcelJS الداخلي واختبار roundtrip للتصدير، ثم ESLint بصفر أخطاء وخط أساس `997/81`، وTypeScript، و50/50 ملفاً و565/565 اختباراً خلال 64.21 ثانية.
- نجح بناء الإنتاج الوهمي. أصل Excel `/assets/excel-export-DKbfwOG2.js` حجمه 943,484 بايت وبصمته `320d5f4d985be64eec3a27ec29846f45105c011a4082abb925577a86d3ad3535`. لم توجد مراجع لبيئات Farida أو Alibea أو Staging أو مشروع Supabase التجريبي الحقيقي؛ ظهر عنوان الاختبار الوهمي في الأصل الرئيسي فقط كما هو متوقع.
- نجحت معاينة `/` و`/auth` و`/reports/sales` بالحالة 200، وطابقت حزمة Excel المخدومة ملف البناء بايتاً وبصمةً. أُغلق المنفذ المحلي 41743 بعد الفحص.
- نُقل ملفا package المطابقان إلى cleanroom، ثم أضيف هذا التوثيق. عند هذه النقطة لم يُنشأ commit أو push ولم يحدث نشر. البوابة التالية هي مراجعة فرق Git وحواجز السلامة ثم الالتزام والدفع، يليهما بناء Staging واختبار تصدير ملف Excel فعلي قبل التفكير في الإنتاج.
- اجتاز الفرق النهائي للملفات الأربعة `git diff --check` وحواجز المسارات الحساسة والأسرار دون نتائج. أُنشئ الالتزام المستقل `65f8688b5c932f35cdaf50d7ee0fed545e9dc356` بعنوان `fix(deps): secure exceljs uuid dependency` ودُفع إلى المستودع الجديد، ثم تطابق `HEAD` مع `origin/main` وبقي cleanroom نظيفاً.
- أُنشئ clone نهائي من GitHub في `/tmp/accounting-stores-app-l6-uuid-final.4mc422`: الفرع `main`، والالتزام مطابق، و500 ملف متعقب، ولا تغييرات محلية. نجح `npm ci` وأعاد `npm audit` الكامل و`--omit=dev` صفراً.
- نجح ESLint بصفر أخطاء وخط أساس `997` تحذير explicit-any و`81` تحذيراً آخر، ونجح TypeScript. نجحت مجموعة Vitest كاملة: 50/50 ملفاً و565/565 اختباراً خلال 68.54 ثانية، ومنها اختبار Excel.
- نجح بناء وهمي نهائي: 3712 وحدة و178 ملفاً بحجم 6,440,983 بايت. بصمة `index.html` هي `f0125935695432b77e9ced680b02e5d83d5711c8f89afac809fb9dd320024115`، والأصل الرئيسي `/assets/index-BBLat098.js` ببصمة `1a4ba10718b2a8cd13586d336f90268a1778fe19b7aa9be9911b4943593ede11`، وحزمة Excel `/assets/excel-export-BXwI0NeD.js` ببصمة `ee71c95e00a27509fb4757e653d397677c763b7ab0142dfd387aa697ad00d33d`.
- لم يحتو البناء أي مرجع لـFarida أو Alibea أو Staging أو مشروع Supabase التجريبي الحقيقي. نجحت معاينة `/` و`/auth` و`/reports/sales` والحزمة بالحالة 200، ثم أُغلق المنفذ 41744. لم يحدث نشر أو اتصال بقاعدة بيانات؛ الخطوة التشغيلية التالية هي Staging فقط واختبار تصدير Excel فعلي.
