# تقرير انتقال الإنتاج — L7

> تاريخ آخر تحديث: 2026-09-08
> الحالة: مكتملة تشغيلياً؛ Staging وFarida وAlibea معتمدة، والمسار الأساسي مرتبط بالمستودع الجديد، واكتمل حذف checkout القديم بعد التحقق

## نطاق المرحلة

تنقل L7 الواجهتين بالتتابع من المصدر القديم إلى المستودع النظيف `accounting-stores-app`. يبدأ الانتقال بـFarida وحدها بعد نسخة رجوع واختبار جاف، ولا تبدأ Alibea قبل اعتماد Farida. لا تشمل هذه الدفعة أي migration أو تعديل قاعدة بيانات أو Edge Functions.

## الحالة المرجعية قبل Farida

- بقي checkout الإنتاج القديم `/opt/accounting-app` وremote القديم بلا تغيير.
- حُفظت نسخة ملفات Farida في `/opt/backups/accounting-app/farida-l7-new-repo-before-20260908`.
- طابق عدد ملفات النسخة المرجعية عدد ملفات الموقع: 183 ملفاً.
- بصمة `index.html` المرجعية: `81b00744793d06a2641ef6b2380dec5a9eb56e8ef29418b88173653fa4cda388`.
- أثبت الفحص أن Nginx والخدمات وقواعد Farida وAlibea وEdge Functions لا تعتمد تشغيلياً على مسار checkout القديم.

## حادثة محاولة النشر الأولى

استُدعي سكربت النشر بمتغير Farida مأخوذ من ملف dotenv بواسطة استخراج نصي خام. كانت القيمة في الملف محاطة بعلامتي اقتباس؛ لذلك أصبح طول النص الخام 210 محارف، بينما القيمة التي يفسرها shell بواسطة `source` طولها 208 محارف. دخلت علامتا الاقتباس في حزمة المتصفح وأصبحت ترويسة API غير صالحة.

النتيجة الظاهرة كانت `401 Unauthorized` لطلب `company_settings` ولمسار تسجيل الدخول. هذا لا يدل على فقد بيانات أو تغيير قاعدة، ولم يكن سببه المستودع الجديد أو Supabase نفسه. أزيلت علامات الاقتباس في الاختبار التالي، لكن اتضح لاحقاً أن مصدر المفتاح نفسه غير صحيح أيضاً كما هو موثق أدناه.

## محاولة النشر الثانية واكتمال التشخيص

بعد إضافة حاجز الصيغة وتثبيته في الالتزام `a6e5420`، نجح البناء الجاف من clone مستقل. نُشرت واجهة Farida فقط بخيارات تمنع `git pull` وmigrations وEdge Functions. نجح بناء Vite ومطابقة الأصل المنشور، لكن فحص API المستقل أعاد 401؛ لذلك استُعيدت النسخة المرجعية فوراً مرة ثانية.

أثبتت مقارنة البصمات دون طباعة القيم أن المفتاح المحمّل من `/opt/accounting-app/.env` طوله 208 محارف ولا يطابق مفتاح البناء المرجعي، بينما `ANON_KEY` في `/opt/supabase-farida/docker/.env` طوله 169 محارف ويطابق المفتاح الفعلي في البناء المرجعي تماماً. إذن `.env` القديم ليس مصدراً صالحاً لنشر Farida حتى لو حُمّل بواسطة `source`.

بعد الرجوع الثاني عادت بصمة `index.html` إلى `81b00744793d06a2641ef6b2380dec5a9eb56e8ef29418b88173653fa4cda388`، وأعاد `/auth` الحالة 200 وأعاد طلب `company_settings` بالمفتاح المرجعي الحالة 200.

## الرجوع والتحقق

- أُوقف الانتقال ولم تُلمس Alibea.
- استُعيدت نسخة Farida المرجعية إلى `/var/www/farida` باستخدام `rsync --delete-delay --delay-updates`، وثُبت وضع الجذر `0755`.
- عادت بصمة `index.html` مطابقة تماماً للبصمة المرجعية أعلاه.
- أعاد `/` و`/auth` الحالة 200، وأعاد REST بالمفتاح المحمّل بصورة صحيحة الحالة 200.
- أعادت محاولة دخول ببيانات اعتماد غير صحيحة الحالة 400 بدلاً من 401، ما يثبت قبول مفتاح API بعد الرجوع.
- أكد المستخدم أن الدخول إلى Farida يعمل من جديد.
- لم تُطبق migrations، ولم تتغير Edge Functions أو Nginx أو Docker أو قواعد Farida وAlibea.

## الحماية المضافة

أضيف فحص مبكر إلى `scripts/deploy-all.sh` يعمل قبل `git pull` أو migrations أو Edge Functions أو تثبيت الحزم أو البناء. يوقف السكربت عند أي من الحالات التالية:

- متغير المفتاح غير موجود أو فارغ.
- القيمة تبدأ أو تنتهي بعلامة اقتباس مفردة أو مزدوجة.
- القيمة تحتوي مسافة أو محرف سطر.
- القيمة لا تطابق صيغة JWT العامة القديمة أو بادئة `sb_publishable_` الحديثة.
- قبول الصيغة مع رفض API المستهدف للمفتاح أو تعذر الاتصال به؛ يجب أن يعيد طلب `company_settings` الحالة 200.

نجحت اختبارات الصيغة السلبية الأربعة، ورفض اختبار الاتصال مفتاحاً صحيح الصيغة تابعاً لمصدر آخر بالحالة 401 قبل تثبيت الحزم، وقَبِل مفتاح Farida المحمي ثم أكمل البناء الجاف. نجح أيضاً `bash -n` للسكربت.

## إعادة الاختبار الجاف

حُمّل ملف البيئة القديم بواسطة `source` من دون طباعة المفتاح، ثم شُغّل بناء Farida في وضع `--dry-run` داخل clone مستقل من المستودع الجديد. أثبت هذا الاختبار سلامة التضمين فقط، واتضح بعد النشر أنه غير كافٍ قبل إضافة فحص API:

- اكتمل تثبيت 638 حزمة، وفحص npm عدد 639 حزمة بصفر ثغرات.
- أنشأ Vite حزمة الإنتاج وظهر المفتاح المحمّل في أصل واحد فقط؛ لم يكن هذا دليلاً على قبول API له.
- ظهر عنوان `https://farida.alibea2020.com/api` في أصل واحد فقط.
- لم يظهر عنوان أو معرف يخص Alibea أو Staging أو مشروع Supabase التجريبي.
- بقي checkout الاختبار نظيفاً.
- لم تتغير ملفات Farida الحية؛ بقيت بصمة `index.html` المرجعية كما هي واستجاب `/auth` بالحالة 200.

## مصدر النشر المحمي

- اعتُمد إعداد Supabase الذاتي للشركة مرجعاً للتحقق من مفتاحها، بعد مطابقته بالبناء الإنتاجي العامل دون طباعة القيمة.
- أُنشئ `/etc/accounting-stores-app/deploy.env` خارج Git ليحمل متغير Farida الصحيح.
- وضع المجلد `0750` والملف `0640`، والمجموعة `deploy` حتى يستطيع مستخدم النشر قراءته دون جعله متاحاً لبقية المستخدمين.
- لا يجوز الرجوع إلى `.env` في checkout القديم أو استخراج القيم الخام منه.

## اعتماد Farida النهائي

- ثُبت حاجز API في الالتزام `5d8438f` ودُفع إلى `origin/main`، ثم حُدث clone الاختبار إليه بتقديم سريع وبقيت نسخة العمل نظيفة.
- نجح `--dry-run --only farida` من السكربت المدفوع باستخدام `/etc/accounting-stores-app/deploy.env`: قبل API المفتاح قبل تثبيت الحزم، واكتمل `npm ci` بتدقيق 639 حزمة وصفر ثغرات، ثم اكتمل بناء Vite لـ3712 وحدة دون تعديل الإنتاج.
- نُشرت واجهة Farida وحدها مع `--no-pull --skip-db --skip-functions --only farida`. لم تُطبق migrations، ولم تُنسخ Edge Functions، ولم تُلمس Alibea.
- طابقت ملفات `/var/www/farida` ناتج `dist` بالكامل. عدد الملفات المنشورة 178 ملفاً.
- بصمة `index.html` المنشور: `ae4a258be4d98221b9a4b93dd225ad111042aee45a1ff2d15a7ab5afcb1d58a6`.
- الأصل الرئيسي المنشور `/assets/index-DKlJu6pd.js` وبصمته `311ccd870b3b3cdc30cc1bce04f2e35f56df7d99557b6070833ffdcccf21c948`.
- أعاد `/` و`/auth` وطلب `company_settings` الحالة 200. أكد المستخدم نجاح الدخول وظهور تقرير المبيعات عند اختيار «هذا الربع» وعدم وجود أخطاء 401 أو أخطاء API حمراء في Console بعد التحديث.
- بقي Nginx وDocker بحالة `active`، وظلت حاويات Farida وAlibea عاملة والحاويات ذات health check بحالة `healthy`. أعادت واجهات Farida وAlibea وStaging الحالة 200.
- بناءً على الاختبارات الآلية واليدوية اعتمدت Farida على بناء المستودع الجديد، مع بقاء نسخة الرجوع المرجعية محفوظة في مسارها.

## اعتماد Alibea النهائي

- طابق `ANON_KEY` في `/opt/supabase-alibea/docker/.env` المفتاح الموجود في واجهة Alibea المرجعية دون طباعة القيمة، ثم أضيف `ALIBEA_SUPABASE_PUBLISHABLE_KEY` إلى `/etc/accounting-stores-app/deploy.env` مع الحفاظ على مفتاح Farida.
- تحقق الفحص من وجود متغير كل شركة مرة واحدة، ومطابقة كل مفتاح لبنائها الإنتاجي، وبقاء الملف بوضع `0640` ومجموعة `deploy`.
- حُفظت نسخة رجوع كاملة في `/opt/backups/accounting-app/alibea-l7-new-repo-before-20260908`، وأكدت المقارنة الكاملة عدم وجود فروق.
- كان خط الأساس السابق 183 ملفاً؛ بصمة `index.html` هي `d3700a9392d163b17ffa547fe22b93643066a008f887dd607e5a31c4020d3c83`، والأصل `/assets/index-CmoYbaBh.js` ببصمة `8c2139a17e7c0f6321b0234fa8e67f7a849046f9210cc14c1e11614c856aa4ba`.
- حُدث clone الاختبار إلى الالتزام `29ebc77` وبقي نظيفاً. قبل حاجز API مفتاح Alibea، ثم نجح `--dry-run --only alibea`: تدقيق 639 حزمة بصفر ثغرات وبناء Vite لـ3712 وحدة دون تعديل الإنتاج.
- نُشرت واجهة Alibea وحدها مع `--no-pull --skip-db --skip-functions --only alibea`. لم تُطبق migrations، ولم تُنسخ Edge Functions، ولم تُلمس Farida.
- طابقت ملفات `/var/www/alibea` ناتج `dist` بالكامل، وأصبح العدد 178 ملفاً. بصمة `index.html` المنشور `55728b24fe4f7e438f33c6b6f20894c3f1c1c01658d1846fd1d084e76bc26138`.
- الأصل الرئيسي المنشور `/assets/index-tL2IHUBE.js` وبصمته `e24a37303422cb6fae6b932b333094b29e1538c9f70bcbc4c01aca7c7287dbec`.
- أعادت صفحة دخول Alibea وAPI الخاص بها الحالة 200، كما بقي API الخاص بـFarida وواجهة Staging بالحالة 200. أكد المستخدم أن كل شيء يعمل بطريقة صحيحة.

## تبديل checkout الإنتاج

- أثبت التدقيق عدم وجود خدمة Nginx أو systemd أو cron أو عملية عاملة تعتمد على checkout القديم أو cleanroom؛ المواقع تخدم ملفات `/var/www` وقواعد وEdge Functions من مسارات مستقلة.
- كان `/opt/accounting-app` مرتبطاً بالمستودع القديم `creative-flame-keeper`، بينما كان `/opt/accounting-app-cleanroom` نظيفاً ومرتبطاً بالمستودع الجديد `accounting-stores-app` عند `29ebc77`.
- نُقل checkout القديم نقلاً قابلاً للرجوع إلى `/opt/backups/accounting-app/legacy-checkout-before-new-repo-20260908` ووضع جذرُه `0700`، ثم أصبح cleanroom هو `/opt/accounting-app` بوضع `0750` وملكية `deploy:deploy`.
- أصبح `origin` للمسار الأساسي هو `https://github.com/MohammedIsmail36/accounting-stores-app.git` للقراءة والكتابة، وتطابق `HEAD` مع `origin/main` وبقيت نسخة العمل نظيفة. لم يعد مسار `/opt/accounting-app-cleanroom` موجوداً.
- أي دفع لاحق من أداة التطوير الخارجية إلى المستودع القديم لا يصل إلى الخادم؛ يظل المستودع القديم خارج مسار النشر، وتُنقل الإضافات المطلوبة منه يدوياً بعد المراجعة وStaging.

## فحص الاستقرار النهائي

- Nginx وDocker بحالة `active`، وجميع حاويات Farida وAlibea عاملة، والحاويات ذات health check بحالة `healthy`.
- أعادت Farida وAlibea وStaging وواجهتا API الإنتاجيتان الحالة 200 بعد اعتماد المستخدم.
- وقت الفحص كان الخادم عاملاً منذ 50 يوماً، والقرص مستخدماً بنسبة 27%، والذاكرة المتاحة نحو 1.8GiB.
- كانت مساحة swap البالغة 2GiB مستخدمة، لكن خمس عينات `vmstat` أظهرت صفراً مستمراً في `si` و`so` مع عدم وجود عمليات محجوبة؛ هذا استخدام راكد لا ضغط تبديل نشط، ولذلك لم تُفرغ swap ولم تُعد تشغيل الخدمات.

## جرد التنظيف قبل الحذف

أُجري جرد قراءة فقط بعد تثبيت المسار الأساسي. لم تظهر أي عملية عاملة أو خدمة Nginx أو systemd أو cron تشير إلى مجلدات الاختبار أو checkout القديم. بلغ مجموع مجلدات `/tmp` المستهدفة نحو 2.6GiB:

- أبنية مؤقتة: `accounting-alibea-s3-3a-build-20260906-151305` (69MiB)، و`accounting-alibea-s3-3a-build-retry-20260906-151305` (15MiB)، و`accounting-s3-3c-baseline-dist.9QUH6Y` (7.5MiB)، و`accounting-s3-3c-incremental.ePAq1s` (7.2MiB)، و`accounting-s3-3c-simulation.hSdJ4a` (552MiB).
- أبنية Staging مؤقتة: `accounting-staging-build` (7.5MiB)، و`accounting-staging-l6-uuid-build.4b5X1k` (6.7MiB)، و`accounting-staging-navbar-20260905` (7.5MiB)، و`accounting-staging-navbar-opaque-20260905` (7.5MiB)، و`accounting-staging-path-check` (7.5MiB)، و`accounting-staging-sales-catalog-20260905` (7.5MiB)، و`accounting-staging-subdomain-build` (7.5MiB).
- نسخ Git التجريبية: `accounting-stores-app-l6-clone.yNDr4Y` (489MiB)، و`accounting-stores-app-l6-excel.RQLX9O` (490MiB)، و`accounting-stores-app-l6-final.jBQgYI` (490MiB)، و`accounting-stores-app-l6-uuid-final.4mc422` (491MiB).

أظهرت ثلاث نسخ Git تغييرات تجريبية محلية: ملفان في تجربة Vite، وملفان في تجربة ExcelJS/UUID، وخمسة ملفات في تجربة React Router. يثبت `docs/L6_REPOSITORY_BOOTSTRAP_REPORT.md` أن النتائج المتحققة نُقلت إلى cleanroom وثُبتت في commits مستقلة ودُفعت إلى المستودع الجديد؛ النسخة الرابعة نظيفة عند `29ebc77`. لذلك لا توجد أعمال فريدة غير محفوظة داخل هذه النسخ.

الـcheckout القديم المعزول في `/opt/backups/accounting-app/legacy-checkout-before-new-repo-20260908` حجمه 571MiB، وحالته نظيفة، و`HEAD` مطابق لـ`origin/main` للمستودع القديم بنتيجة `0 0`. مجموع المساحة المتوقع تحريرها من الأهداف المؤقتة والقديمة نحو 3.2GiB.

لا يشمل التنظيف `/opt/accounting-app` أو `/var/www` أو `/opt/supabase-farida` أو `/opt/supabase-alibea` أو `/etc/accounting-stores-app`، ولا يشمل نسختي رجوع الواجهتين:

- `/opt/backups/accounting-app/farida-l7-new-repo-before-20260908`
- `/opt/backups/accounting-app/alibea-l7-new-repo-before-20260908`

## الحالة الحالية

- Farida تعمل ببناء المستودع الجديد من الالتزام `5d8438f` ومعتمدة وظيفياً.
- Alibea تعمل ببناء المستودع الجديد من الالتزام `29ebc77` ومعتمدة وظيفياً.
- Staging تعمل ببناء المستودع الجديد ومعتمدة وظيفياً.
- أصبح حاجز النشر يختبر قبول المفتاح من API فعلياً قبل أي تغيير، واجتاز الاختبارين السلبي والإيجابي والبناء الجاف.
- `/opt/accounting-app` هو checkout الوحيد المعتمد للنشر ومرتبط بالمستودع الجديد `accounting-stores-app`.
- حُذف checkout القديم المعزول `/opt/backups/accounting-app/legacy-checkout-before-new-repo-20260908` بعد انتهاء الجرد والتحقق من أن حالته كانت نظيفة ومتطابقة مع مستودعه القديم.
- تحقق الفحص بعد الحذف من أن المسار القديم لم يعد موجوداً، وأن فرع `main` في المسار الأساسي ما زال يتتبع `origin/main` من المستودع الجديد دون تغييرات محلية.
- بقيت نسختا رجوع الواجهتين Farida وAlibea محفوظتين ولم يشملهما الحذف، كما لم يشمل الحذف ملفات `/var/www` أو قواعد البيانات أو إعدادات النشر المحمية.

## الخطوات التالية بالترتيب

1. تثبيت توثيق اكتمال التنظيف ورفعه إلى `origin/main`، ثم التحقق من بقاء checkout الأساسي نظيفاً ومتزامناً.
2. تنفيذ فحص استقرار نهائي للواجهات وواجهات API والخدمات بعد التنظيف، دون نشر أو تعديل قواعد البيانات.
3. إبقاء نسختي رجوع الواجهتين مؤقتاً حتى اكتمال فترة المراقبة، ثم اتخاذ قرار مستقل بشأن مدة الاحتفاظ بهما.

يُمنع استخدام استخراج نصي خام لقيم dotenv، ويُمنع نشر الشركتين معاً أو تطبيق L3 ضمن دفعة انتقال الواجهة.
