# عقد معالج انحرافات المخزون

> التاريخ: 2026-09-13
>
> الفرع: `feature/inventory-control-staging`
>
> الحالة: المرحلتان 2C والبنية الأساسية لـ2D مطبقتان على Staging؛ اكتمل قبول خريطتي الشراء `1104/2101` والبيع `1103/4101 + 5101/1104` بلا ضريبة، وتبقى خرائط الضريبة والمرتجعات والتسويات قبل إغلاق 2D
>
> النطاق: فرع Staging فقط؛ لا يوجد تطبيق أو نشر إنتاجي

## 1. الهدف

يحوّل المعالج نتيجة التشخيص المقروءة إلى عملية مراجعة موثقة، ثم ينفذ نوع إصلاح محدداً بعد اعتماد المدير. لا يغيّر المعالج رقماً لمجرد وجود فرق، ولا يعد فرق WAC التحليلي انحرافاً يحتاج إصلاحاً.

يبقى الفصل واضحاً بين:

- `get_inventory_reconciliation_diagnostic`: تشخيص لحظي للقراءة فقط.
- معالج الانحراف: طلب ومراجعة واعتماد وتنفيذ موثق.
- تسوية المخزون: مستند تشغيلي لتسجيل واقع مخزني جديد.
- الجرد الفعلي: عملية رقابية مستقلة تبدأ بلقطة وعد أعمى.

## 2. قرار التنفيذ المرحلي

لا تنفذ الأنواع الخمسة في Migration واحدة. تقسم المرحلة 2 إلى:

1. **2A — عقد المعالج:** تثبيت الجداول والحالات والصلاحيات والعقود. لا SQL.
2. **2B — سجل المعالجة:** إنشاء الجداول وسياسات القراءة وعمليات إنشاء/تعديل/إرسال/اعتماد المسودة فقط. لا تغيير منتجات أو حركات أو قيود.
3. **2C — إعادة بناء كمية البطاقة:** أول منفذ إصلاح، ويعمل فقط عندما تثبت صحة الحركات وأن `quantity_on_hand` وحده خاطئ.
4. **2D — حركة صحيحة بلا قيد:** منفذ مستقل بعد تثبيت خريطة الحساب المقابل لكل نوع مستند.
5. **2E — قيد بلا حركة:** منفذ مستقل؛ لا يعكس القيد كاملاً قبل تحليل سطوره وأثره.
6. **2F — التسوية وفرق التقريب:** الربط بمستند التسوية الذري وحساب فروق التقريب بعد اعتماد إعداد الحساب.

لا تنتقل دفعة إلى التالية إلا بعد L3 معزولة ثم Staging وقبول مستقل.

## 3. أنواع الإصلاح في الإصدار الأول

| الرمز | الوصف | الأثر المسموح | حالة التنفيذ |
|---|---|---|---|
| `rebuild_product_card` | إعادة كمية بطاقة المنتج من صافي الحركات الصحيحة | تحديث `products.quantity_on_hand` فقط داخل RPC موثقة | أول نوع ينفذ في 2C |
| `create_missing_inventory_journal` | إنشاء قيد تصحيح لحركة صحيحة موثقة بلا أثر 1104 | قيد تصحيح جديد بلا حركة جديدة | مطبق على Staging؛ قُبلت خريطتا الشراء والبيع بلا ضريبة فعليًا من الواجهة |
| `reverse_unbacked_inventory_journal` | معالجة قيد 1104 غير المدعوم بحركة | قيد تصحيحي/عكسي جديد؛ لا تعديل للأصل | تصميم فقط حتى 2E |
| `create_linked_inventory_adjustment` | فرق كمية أو قيمة يمثل واقعاً مخزنياً جديداً | إنشاء مسودة تسوية مرتبطة ثم ترحيلها بمحرك التسوية | تصميم فقط حتى 2F |
| `post_rounding_adjustment` | باقي تقريب مثبت المصدر | قيد تكلفة محدود إلى حساب تقريب معتمد | محجوب حتى اعتماد الحساب والحد |

`manual_review` تصنيف قرار وليس نوع إصلاح منفذ. يستخدم عندما لا تكفي الأدلة، وتبقى الحالة مفتوحة بلا كتابة مالية.

## 4. نموذج البيانات

### 4.1 رأس عملية المعالجة

الجدول: `inventory_reconciliation_repairs`

الحقول الأساسية:

```text
id uuid primary key
repair_number bigint unique not null
status text not null
title text not null
explanation text not null
diagnostic_fingerprint text not null
diagnostic_snapshot_at timestamptz not null
source_scope text not null
version integer not null default 1
accounting_date date null
reverses_repair_id uuid null
reversed_by_repair_id uuid null
separation_override_reason text null
prepared_by uuid not null
prepared_at timestamptz not null
submitted_by uuid / submitted_at timestamptz
approved_by uuid / approved_at timestamptz
executed_by uuid / executed_at timestamptz
cancelled_by uuid / cancelled_at timestamptz / cancellation_reason text
reversed_by uuid / reversed_at timestamptz / reversal_reason text
created_at / updated_at timestamptz
```

الحالات المسموحة:

```text
draft -> ready_for_review -> approved -> executed -> reversed
  |             |              |
  +-----------> cancelled <----+
```

- لا توجد حالة `executing` دائمة؛ التنفيذ يتم داخل معاملة واحدة.
- فشل متوقع داخل المنفذ يسجل حدث `execution_failed` وتبقى العملية `approved` قابلة للمراجعة، من دون أثر أعمال جزئي.
- لا تعاد العملية المنفذة إلى مسودة.
- لا يحذف أي رأس معالجة؛ المسودة غير المطلوبة تصبح `cancelled` للحفاظ على الأثر الرقابي.

### 4.2 بنود الانحراف

الجدول: `inventory_reconciliation_repair_items`

```text
id uuid primary key
repair_id uuid not null
line_number integer not null
axis text not null                 -- product أو source
issue_key text not null
classification text not null
repair_type text not null
product_id uuid null
source_type text null
source_id uuid null
source_number text null
original_journal_entry_id uuid null
before_card_quantity numeric null
before_movement_quantity numeric null
before_movement_book_value numeric(18,2) null
before_ledger_1104_value numeric(18,2) null
proposed_card_quantity numeric null
proposed_movement_book_value numeric(18,2) null
proposed_ledger_1104_value numeric(18,2) null
after_card_quantity numeric null
after_movement_quantity numeric null
after_movement_book_value numeric(18,2) null
after_ledger_1104_value numeric(18,2) null
before_state jsonb not null
proposed_state jsonb not null
after_state jsonb null
precondition_hash text not null
result_status text not null default 'pending'
result_message text null
created_at / updated_at timestamptz
```

القيود:

- `unique (repair_id, line_number)`.
- `unique (repair_id, axis, issue_key)` لمنع تكرار الانحراف داخل الطلب.
- لا يسمح بأكثر من عملية نشطة (`draft` أو `ready_for_review` أو `approved`) لنفس `axis + issue_key`. يتحقق RPC من ذلك داخل قفل استشاري خاص بمفتاح الانحراف لمنع سباق طلبين متزامنين، مع إبقاء العمليات التاريخية `cancelled` و`executed`.
- يجب وجود `product_id` لمحور المنتج، و`source_type/source_id` لمحور المصدر المعروف.
- القيم الكمية والمالية التي تقود القرار تحفظ في أعمدة `numeric` قابلة للفحص، ولا تدفن داخل JSON فقط. تستخدم حقول JSON للأدلة والسياق الإضافي.
- لا تحفظ قيمة «بعد» إلا بواسطة منفذ قاعدة البيانات.
- `before_state` و`proposed_state` لقطتان غير قابلتين للتعديل بعد `ready_for_review`.
- لا تقبل بنود `matched`، ولا يقبل فرق WAC وحده كبند إصلاح.

### 4.3 آثار التنفيذ

الجدول: `inventory_reconciliation_repair_effects`

```text
id uuid primary key
repair_id uuid not null
repair_item_id uuid not null
effect_type text not null
table_name text not null
record_id uuid not null
before_data jsonb null
after_data jsonb null
created_at timestamptz not null
```

يحفظ روابط السجلات التي أنشئت أو تغيرت فعلاً: المنتج، مستند التسوية، الحركة، القيد أو قيد العكس. وجود جدول آثار مستقل يمنع ضغط عدة حركات أو قيود داخل أعمدة مفردة في البند.

### 4.4 سجل الأحداث والأوامر

الجدول: `inventory_reconciliation_repair_events`

```text
id uuid primary key
repair_id uuid not null
event_type text not null
from_status text null
to_status text null
request_id uuid not null
actor_id uuid not null
event_data jsonb not null default '{}'
created_at timestamptz not null
unique (request_id)
```

يكون `request_id` فريداً على مستوى جدول الأحداث كله، وليس داخل العملية فقط، حتى تكون إعادة إرسال أمر الإنشاء نفسه آمنة أيضاً ولا تنشئ عملية ثانية.

## 5. الصلاحيات

- المدير والمحاسب يقرآن الرأس والبنود والآثار والأحداث.
- المحاسب والمدير ينشئان المسودة ويعدلانها ويرسلانها للمراجعة.
- المدير وحده يعتمد وينفذ ويلغي بعد الاعتماد ويعكس عملية منفذة.
- إذا كان المُعد والمدير المنفذ الشخص نفسه، يجب ملء `separation_override_reason` وتسجيل حدث صريح بأن فصل المهام لم يتحقق.
- لا يملك `anon` أو دور البائع أي صلاحية.
- لا توجد سياسات `INSERT/UPDATE/DELETE` مباشرة للمستخدمين على الجداول الأربعة.
- جميع الكتابات تمر عبر دوال `SECURITY DEFINER` ذات `search_path` ثابت وفحص صريح لـ`auth.uid()` والدور.
- `service_role` لا يتجاوز قواعد الأعمال؛ يسمح به فقط للأدوات الإدارية المقيدة ويُسجل الفاعل التشغيلي صراحة.

## 6. واجهات قاعدة البيانات

### 6.1 عمليات دورة المستند

```text
create_inventory_reconciliation_repair(..., p_request_id uuid) -> jsonb
update_inventory_reconciliation_repair(..., p_expected_version integer, p_request_id uuid) -> jsonb
submit_inventory_reconciliation_repair(p_id, p_expected_version, p_request_id) -> jsonb
approve_inventory_reconciliation_repair(p_id, p_expected_version, p_separation_override_reason, p_request_id) -> jsonb
cancel_inventory_reconciliation_repair(p_id, p_reason, p_expected_version, p_request_id) -> jsonb
```

في 2B تنفذ هذه العمليات دورة الاعتماد فقط ولا تلمس جداول الأعمال.

### 6.2 التنفيذ والرجوع

```text
execute_inventory_reconciliation_repair(p_id, p_expected_version, p_request_id) -> jsonb
reverse_inventory_reconciliation_repair(p_id, p_reason, p_expected_version, p_request_id) -> jsonb
```

تضاف كل حالة `repair_type` إلى المنفذ في دفعتها الخاصة. أي نوع غير مفعل يعيد `REPAIR_TYPE_NOT_ENABLED` قبل أي كتابة.

تعيد أخطاء دورة المستند رموزاً ثابتة قابلة للعرض والترجمة في الواجهة، ومنها:

- `REPAIR_DUPLICATE_ISSUE` عند تكرار الانحراف داخل الطلب نفسه.
- `REPAIR_ISSUE_ACTIVE` عند وجود عملية نشطة أخرى للانحراف نفسه.
- `REPAIR_VERSION_CONFLICT` عند إرسال نسخة قديمة.
- `REPAIR_PRECONDITION_CHANGED` عند تغير السجل منذ المعاينة.
- `REPAIR_TYPE_NOT_ENABLED` عند طلب تنفيذ نوع لم تعتمد دفعته بعد.

## 7. التزامن ومنع القرار القديم

البصمة العامة من شاشة التشخيص تحفظ كدليل إنشاء، لكنها لا تستخدم وحدها لمنع التنفيذ؛ لأنها تتغير عند أي حركة مخزون غير مرتبطة بالإصلاح.

عند التنفيذ:

1. يقفل رأس المعالجة وبنودها `FOR UPDATE`.
2. يقفل المنتج أو المستند أو القيد المتأثر فقط.
3. يعيد حساب الانحراف المحدد من المصدر الحالي.
4. يقارن `precondition_hash` والقيم الأساسية في `before_state`.
5. يرفض بـ`REPAIR_PRECONDITION_CHANGED` إذا تغير السجل المتأثر.
6. لا يرفض بسبب تغير منتج آخر غير داخل العملية.
7. يزيد `version` في كل انتقال حالة لمنع اعتماد نسخة واجهة قديمة.

هذا قفل متفائل على المستند وقفل صفوف عند التنفيذ، وليس قفلاً عاماً للمخزون.

## 8. قواعد التنفيذ الذري

- تبدأ جميع الآثار وتنتهي داخل استدعاء RPC واحد ومعاملة واحدة.
- يمنع التنفيذ في تاريخ يساوي أو يسبق `locked_until_date` عندما ينشأ قيد أو مستند مؤرخ.
- لا يعدل قيداً أو حركة مرحلة؛ التصحيح بسجل جديد مرتبط بالأصل.
- الاستثناء الوحيد هو `rebuild_product_card`: يعدل حقل البطاقة لأنه نسخة مشتقة، بعد إثبات أن الحركات نفسها سليمة، ويسجل القديم والجديد في الآثار والتدقيق.
- لا ينشئ `rebuild_product_card` حركة أو قيداً؛ إن كان الواقع المخزني تغير فالمسار الصحيح تسوية مخزون.
- بعد الكتابة يعاد التشخيص داخل المعاملة نفسها على البنود المتأثرة.
- لا تعتمد العملية `executed` إلا إذا تحقق الأثر المتوقع ولم تظهر مشكلة جديدة.
- عند الفشل ترجع آثار الأعمال كلها. يسجل فشل متوقع في حدث مستقل داخل معالجة استثناء مضبوطة، من دون إخفاء خطأ غير متوقع.
- العكس عملية جديدة أو قيد/مستند عكس؛ لا يحذف الأثر السابق ولا يعيد كتابة تاريخه.

## 9. عقد أول منفذ: إعادة بناء كمية البطاقة

لا يقبل التنفيذ إلا إذا تحققت جميع الشروط:

- التصنيف الحالي `product_balance`.
- المنتج نفسه هو المحدد في البند.
- لا يوجد `unresolved_source_reference`.
- لا توجد `zero_quantity_nonzero_value` أو `nonzero_quantity_zero_value` أو تكلفة دفترية سالبة.
- صافي الحركات الحالي يساوي القيمة المقترحة المخزنة في البند.
- كمية البطاقة الحالية تساوي `before_state.card_quantity`.
- لا توجد عملية أخرى منفذة أو معتمدة لنفس `issue_key` وشرط البداية.

الأثر:

```text
products.quantity_on_hand = current_movement_quantity
```

ثم يسجل:

- القيمة القديمة والجديدة.
- عدد الحركات وتاريخ آخر حركة.
- البصمة الموضعية قبل التنفيذ.
- نتيجة إعادة التشخيص.
- المنفذ و`request_id` والزمن.

## 10. الأنواع المحجوبة حتى استكمال متطلباتها

### حركة بلا قيد

**قرار 2D المعتمد بتاريخ 2026-09-21:** لا ينشأ طرف مقابل ثابت من قيمة حركة المخزون وحدها، ولا يستخدم حساب معلق عام لإخفاء الانحراف. يعيد مخطط قراءة فقط تكوين القيد الكامل المتوقع من المستند والحركات والإعدادات، ويجمع القيد الفعلي حسب الحساب، ثم يقترح قيداً تصحيحياً يمثل فرق المتجهين فقط. إذا لم يكن الفرق حتمياً أو دخل فيه حساب غير متوقع تتحول الحالة إلى `manual_review` دون كتابة مالية.

خريطة القيد الكامل المتوقع:

| المصدر | أثر المخزون | الأطراف الأخرى التي يجب إثباتها |
|---|---|---|
| فاتورة بيع | `1104` دائن بقيمة تكلفة الحركة | `5101` مدين، مع إثبات `1103 / 4101 / 2102` من إجمالي المستند وضريبته |
| مرتجع بيع | `1104` مدين بقيمة تكلفة الحركة | `5101` دائن، مع إثبات عكس `1103 / 4101 / 2102` |
| فاتورة شراء | `1104` مدين بقيمة الحركة | `2101` دائن بإجمالي المستند و`1105` مدين بالضريبة إن وجدت |
| مرتجع شراء | `1104` دائن بقيمة WAC للحركة | `2101` مدين، و`1105` دائن، و`5108` باتجاه فرق سعر المرتجع |
| تسوية عجز | `1104` دائن بصافي العجز | `5201` مدين |
| تسوية فائض | `1104` مدين بصافي الفائض | `4201` دائن |

قواعد الأهلية والتنفيذ:

1. إذا كان `journal_entry_id` فارغاً، يجوز إنشاء القيد الكامل المتوقع فقط بعد تطابق حالة المستند `posted` وقيمه مع الحركات، ثم يربط القيد الجديد بالمستند ويُسجل الربط في أثر المعالجة.
2. إذا كان القيد المرتبط `posted` لكنه لا يحتوي أثر `1104`، يحسب الفرق بين القيد الكامل المتوقع والفعلي. لا ينشأ قيد تصحيحي إلا إذا كانت كل حسابات الفرق ضمن الخريطة أعلاه.
3. إذا كان القيد المرتبط `draft` أو غير متوازن أو يحوي فرقاً في حساب غير متوقع، يرفض التنفيذ الآلي ويصنف `manual_review`.
4. لا يعدل أو يحذف القيد القديم. التصحيح قيد جديد `posted` مرتبط ببند المعالجة، ويعرض القيد الأصلي والتصحيحي معاً في التدقيق.
5. يضاف رابط أجنبي صريح من بند المعالجة إلى القيد التصحيحي؛ لا يعتمد التشخيص على نص الوصف لاكتشافه.
6. يوسع التشخيص ليجمع أثر `1104` من القيد الأصلي ومن القيود التصحيحية المنفذة فقط، دون احتساب المسودات أو المعالجات الملغاة.
7. تاريخ القيد هو تاريخ المصدر إذا كانت فترته مفتوحة. إذا كانت مقفلة يصبح `accounting_date` داخل فترة مفتوحة إلزامياً، مع حفظ تاريخ المصدر ورقمه في الوصف والأثر.
8. التنفيذ للمدير فقط، ويقفل الرأس والبنود والمستند والقيد والحسابات المستعملة، ويعيد بناء الخطة ومقارنتها بـ`precondition_hash` قبل الكتابة.
9. `request_id` والربط الفريد بالقيد التصحيحي يمنعان إنشاء قيد ثان عند إعادة المحاولة أو انتهاء مهلة الواجهة.
10. بعد الكتابة يعاد التشخيص داخل المعاملة؛ لا تصبح المعالجة `executed` إلا إذا أصبح فرق المصدر صفراً، ولم تتغير المنتجات أو الحركات أو مبالغ المستند.
11. الرجوع لا يحذف القيد التصحيحي؛ ينشئ معالجة عكس بقيد معاكس في فترة مفتوحة ويحفظ السلسلة كاملة.
12. كل قيد يصبح `posted` يجب أن يحمل `posted_number` موجبًا وفريدًا، ويُحجز الرقم داخل المعاملة بقفل ذري لمنع التكرار عند التوازي.
13. لا تخزن بادئة رقم القيد في القيد نفسه؛ تقرأ طبقة العرض `company_settings.journal_entry_prefix` وتركبها مع `posted_number`. لا يجوز تثبيت `JV-` أو أي بادئة أخرى في منفذ المعالجة.

ينفذ 2D على دفعات فرعية: مخطط القراءة وعقد الاختبار أولاً، ثم منفذ قاعدة البيانات داخل L3، ثم Staging. لا تضاف الواجهة ولا يفعّل النوع قبل قبول المنفذ والرجوع مستقلاً.

اكتملت دفعة 2D-A للقراءة فقط بتاريخ 2026-09-21. أضيفت الدالة `get_inventory_reconciliation_journal_plan(text, uuid, date)` التي تقبل المدير والمحاسب فقط، وتقرأ المصدر والحركات والقيد والإعدادات، ثم تعيد القيد المتوقع والفعلي وفرق الحسابات وبصمة حتمية وأهلية التنفيذ. لا تستدعي بوابة إنشاء القيود ولا تحتوي أي كتابة إلى جداول الأعمال. غطى العقد ستة عشر سيناريو: القيد الكامل للبيع، فرق زوج التكلفة، مرتجع البيع، الشراء، مرتجع الشراء وفرق `5108`، عجز وفائض التسوية، القيد المسودة، الحساب غير المتوقع، النوع غير المدعوم، تغير البصمة، الفترة المقفلة وتاريخ المعالجة المفتوح، الحتمية، القراءة بلا كتابة، ومنع الحساب المعلق. نجحت Migration والعقد داخل L3 مع `ROLLBACK` كامل، ونجح ملف الرجوع الصريح في تجربة مستقلة، وتطابقت بصمات كل بيانات الأعمال قبل الاختبار وبعده. حُفظت التقارير والملفات وبصماتها في `/backups/staging/inventory-missing-journal-plan-l3-20260921`. ما زال `create_missing_inventory_journal` محجوباً ولم تُنشأ قيود أو تُطبق Migration على Staging أو الإنتاج.

بدأت دفعة 2D-B بعقد TDD أحمر مستقل `inventory_reconciliation_missing_journal_executor_contract.sql`. يثبت العقد أربعة عشر شرط قبول للمنفذ الذري: إنشاء القيد الكامل وربطه بالمصدر، إنشاء قيد فرق مستقل مع إبقاء القيد الأصلي دون تعديل، مطابقة السطور للخطة واتزانها، رفض تغير المستند أو الحركة، قصر التنفيذ على المدير، منع التكرار بمفتاح الطلب، رفض إعادة التنفيذ، إعادة فحص قفل الفترة، قفل الشرط المسبق على المصدر المستهدف دون التأثر بمصدر آخر، عدم تغيير المنتجات أو الحركات، تسجيل الأثر والحدث، وضم قيد الفرق إلى التشخيص اللاحق حتى تصبح الخطة `NO_CORRECTION_REQUIRED`، ومنع أي حساب معلق أو خارج الخريطة. نجح اختبار `TDD_MISSING_INVENTORY_JOURNAL_EXECUTOR_RED_OK` داخل L3 وأثبت أن المنفذ ما زال محجوباً وأن ملفات Migration التنفيذ والرجوع غير موجودة، دون تشغيل عينات العقد أو تغيير L3 أو Staging أو الإنتاج. الخطوة التالية كتابة Migration المنفذ وملف رجوعه ثم تشغيل الحالات داخل معاملة L3 تنتهي بـ`ROLLBACK`؛ لا تطبيق على Staging في هذه الخطوة.

اكتملت دفعة 2D-B داخل L3 بتاريخ 2026-09-22. يوزع المنفذ المشترك النوع الجديد إلى مسار ذري مستقل ويبقي تنفيذ `rebuild_product_card` المقبول في 2C دون نسخ. يعيد قفل سجل المعالجة والمصدر وفحص التشخيص والخطة والبصمة وتاريخ المعالجة، ثم ينشئ قيدًا مرحلًا جديدًا عبر بوابة `create_journal_entry`. في نمط القيد الكامل يربط القيد الجديد بالمصدر؛ وفي نمط الفرق يبقي القيد الأصلي دون تعديل ويسجل القيد الجديد في `inventory_reconciliation_repair_effects`. توسع مخطط القراءة القيود الفعلية بهذه الآثار، لذلك يجب أن تصبح النتيجة بعد التنفيذ `NO_CORRECTION_REQUIRED` وإلا تُرجع المعاملة كلها. كشف العقد خللاً في تأهيل اسم رمز الحساب داخل فحص 2D-A وصُحح؛ فأصبح غياب أي حساب من الخريطة يعيد `ACCOUNT_MAPPING_MISSING` ولا يسمح للمنفذ بالتخمين. نجحت الحالات الأربع عشرة وملف الرجوع الصريح داخل L3، وعاد منفذ 2C ومخطط 2D-A بعد الرجوع مع ثبات بيانات الأعمال. حُفظ دليل الاختبارات في `/backups/staging/inventory-missing-journal-executor-l3-20260922`. لم تتغير Staging أو قواعد الإنتاج.

اعتمدت حسابات التسوية في 2D كحسابات نظام ذات دلالة ثابتة، وليست إعدادات عامة قابلة للاستبدال: `4201` لفائض تسوية المخزون ضمن الإيرادات و`5201` لعجز تسوية المخزون ضمن المصروفات. تضيف Migration ‏`20260921213000_inventory_reconciliation_system_accounts.sql` الحساب المفقود، وتطبّع الحساب القديم الموافق للدلالة دون تغيير اسمه أو وصفه، وتجعله نشطًا ومحميًا، كما تضيف حارسًا عامًا في قاعدة البيانات يمنع حذف أي حساب يحمل `is_system=true`. يبقى تعديل هوية الحساب المحمي ونوعه وأبيه ممنوعًا بالحارس القائم. يتحقق المخطط كذلك من الرمز والنوع والأب وحالة النشاط وصفة النظام ويرفض التنفيذ بالرمز `ACCOUNT_MAPPING_INVALID` عند أي انحراف. لا تُعرض هذه الهوية كخيار حر في الإعدادات؛ وإذا احتاج النظام لاحقًا مرونة تنظيمية فتكون عبر ملف ترحيل محاسبي مضبوط لا عبر اختيار حساب اعتباطي.

ملف الرجوع يزيل الحارس والحسابات التي أنشأتها Migration فقط بعد التحقق من عدم وجود قيود عليها ومن إزالة اعتماد المخطط. ولا يعيد الحساب القديم إلى حالة غير محمية أو أب غير صحيح؛ فالتراجع لا يجوز أن يعيد ثغرة الهوية المحاسبية القديمة. أما تجربة الرجوع الكاملة فتُنفذ داخل معاملة خارجية يعيدها `ROLLBACK` إلى خط الأساس حرفيًا.

اجتازت حزمة الحسابات المحمية ثمانية سيناريوهات داخل L3، ثم أعيد تشغيل عقد المخطط بعد تقويته فنجح في ثمانية عشر سيناريو، وأعيد تشغيل عقد المنفذ فنجح في أربعة عشر سيناريو مع نجاح الرجوع الصريح وثبات بيانات الأعمال. الدليل المصحح للحسابات في `/backups/staging/inventory-system-accounts-l3-20260922-corrected`، ودليل الحزمة المحصنة كاملة في `/backups/staging/inventory-missing-journal-hardened-l3-20260922`.

أُخذ خط أساس Staging قبل 2D وحُفظ في `/backups/staging/inventory-missing-journal-before-20260922-012321`. بعد ذلك شُغلت حسابات النظام و2D-A و2D-B والرجوع الصريح داخل معاملات على Staging انتهت بـ`ROLLBACK` كامل. نجحت السيناريوهات الأربعون، وتطابقت بيانات الأعمال والمعالجات والتشخيص مع خط الأساس، ولم يُنشأ `5201` أو أي حساب دائم أثناء التجربة. حُفظ دليل التجربة وبصماته في `/backups/staging/inventory-missing-journal-before-20260922-012321/transactional-rehearsal-hardened`.

بعد اعتماد المستخدم، أكد `dry-run` أن الملفات الثلاثة وحدها منتظرة بلا Seeds أو Roles، ثم طُبقت بالترتيب الرسمي على Staging: حسابات النظام `20260921213000`، المخطط `20260921220000`، والمنفذ `20260921233000`. نجح فاحص قراءة فقط بعد التطبيق في إثبات تسجيل الإصدارات وصحة `4201/5201` وحارس الحذف والدوال والصلاحيات، مع تطابق بيانات الأعمال والمعالجات والتشخيص مع خط الأساس. أكد `dry-run` الختامي أن القاعدة محدثة ولا توجد Migrations معلقة. حُفظ التقرير والاستعلام والملفات المطبقة وملفات الرجوع وبصماتها في `/backups/staging/inventory-missing-journal-before-20260922-012321/post-apply-verification`. لم تتغير أي قاعدة إنتاجية. الخطوة التالية هي بناء واجهة عرض الخطة وتنفيذ النوع للمدير، ثم حالة قبول Staging مضبوطة مستقلة.

كشفت مراجعة الواجهة أن مسار إنشاء المسودة العام كان يحفظ `proposed_state` فارغة لنوع `create_missing_inventory_journal`، بينما يشترط المنفذ خطة خادمية مثبتة وتاريخًا محاسبيًا؛ لذلك كان زر واجهة مباشر سيرفض التنفيذ دائمًا. عولجت الفجوة في دفعة 2D-C بجسر داخلي يعمل قبل حفظ بند المسودة: يعيد اشتقاق الخطة من الخادم، يتجاهل سطور القيد والبصمة الآتية من المتصفح، يثبت التاريخ المحاسبي في الرأس، ويعيد حساب `precondition_hash`. يرفض الفترة المقفلة دون تاريخ مفتوح ولا يكتب قيدًا أثناء الإعداد. نجح TDD الأحمر أولًا، ثم نجحت سبعة سيناريوهات L3 تشمل الخطة الخادمية، تجاهل التلاعب، التاريخ المقفل، تحديث المسودة، والدورة حتى التنفيذ الذري. نجح ملف الرجوع الصريح مع بقاء مخطط ومنفذ 2D. حُفظ دليل L3 في `/backups/staging/inventory-missing-journal-ui-bridge-l3-20260922`.

أُخذ خط أساس Staging مستقل قبل 2D-C، ثم نجحت السيناريوهات السبعة وملف الرجوع داخل معاملات انتهت بـ`ROLLBACK` كامل. بعد ذلك أكد `dry-run` أن `20260922070000_inventory_reconciliation_missing_journal_ui_bridge.sql` هي Migration الوحيدة المنتظرة، فطُبقت على Staging فقط. أثبت فاحص قراءة مستقل تسجيل الإصدار ووجود الدالة والمشغل مرة واحدة، و`SECURITY DEFINER` مع `search_path` آمن، ومنع التنفيذ المباشر عن `PUBLIC` و`anon` و`authenticated`، واعتماد الخطة والبصمة والسطور على الخادم. تطابقت أعداد وبصمات بيانات الأعمال والتشخيص مع خط الأساس؛ والاستثناء الوحيد كان تحديث `updated_at` لفاتورة البيع `95`، وقد أثبت سجل التدقيق تطابق جميع الحقول المحاسبية قبل التحديث وبعده. حُفظت النسخة وتجربة الرجوع وتحقق ما بعد التطبيق وبصماتها في `/backups/staging/inventory-missing-journal-ui-bridge-before-20260922-070950`. لم تتغير أي قاعدة إنتاجية. بوابة 2D التالية هي واجهة عرض الخطة والتنفيذ ثم حالة قبول Staging مضبوطة.

بُنيت واجهة 2D لعرض خطة القيد من الخادم عند إعداد المسودة، وتثبيت التاريخ المحاسبي عند الحاجة، ثم إعادة قراءة الخطة ومطابقة بصمتها قبل التنفيذ. تستخدم نفس منفذ المعالج الذري مع مهلة واضحة ولا ترسل سطور قيد موثوقة من المتصفح. اجتازت الدفعة فحص الأنواع وESLint والبناء والاختبارات، ودُفعت في الالتزام `71394cc`. حُفظت نسخة واجهة Staging في `/opt/backups/accounting-app/staging-inventory-missing-journal-ui-before-20260922-111830`، ثم نُشرت الحزمة المتحققة `assets/index-BwGxL1Th.js` على Staging فقط. أعادت الصفحة والسجل والأصل الرئيسي `200`، وبقيت واجهتا Farida وAlibea دون تغيير، وأكد المستخدم أن شاشة المطابقة تعمل وتعرض بياناتها المعتادة.

قبل أول قبول فعلي أُخذت نسخة حديثة للقراءة فقط وخط أساس بعد اكتمال 2D، وحُفظا في `/backups/staging/inventory-missing-journal-ui-acceptance-before-20260922-114237`. أثبت الخط وجود `613` منتجًا و`1364` حركة و`31` فاتورة شراء و`311` قيدًا و`831` سطر قيد، وثلاث معالجات وحدثي تقريب فقط، مع حالة `rounding_only` وعدم وجود معالجة `create_missing_inventory_journal` نشطة. أُعد مشغل Staging لحالة فاتورة شراء اختبارية ذات حركة صحيحة وقيد مفقود، بأرقام ومعرفات ثابتة لا تستهلك تسلسل المستندات، وحارس هوية وقاعدة، وخطة رجوع ترفض العمل بمجرد وجود معالجة أو قيد مرتبط. نجحت التجربة داخل معاملة انتهت بـ`ROLLBACK` كامل: ظهر المصدر `purchase_invoice:990021` بتصنيف `movement_without_journal`، وأعاد المخطط خطة `create_full_journal` متوازنة بين `1104` مدينًا و`2101` دائنًا بقيمة `100`. نجح الرجوع الصريح وعادت جميع الأعداد والبصمات والتشخيص إلى الخط السابق. حُفظ الدليل في `purchase-fixture-rehearsal` داخل النسخة. لم تُنشأ حالة دائمة ولم يتغير الإنتاج.

كشفت التجربة أن التشخيص القديم يُبقي `can_prepare_repair=false` لحالات `movement_without_journal` حتى عندما تكون خطة 2D مؤهلة؛ لذلك كانت الواجهة المنشورة ستخفي زر المسودة قبل أن تتمكن من طلب الخطة الخادمية. صُحح الحارس محليًا ليتيح مدخل 2D لهذا التصنيف وحده، بينما تبقى الخطة الخادمية والجسر هما الحكم النهائي: الخطة غير المؤهلة تعرض سبب الرفض ولا تنشئ مسودة. أضيف اختبار يغطي الحالة ذات العلم `false`، ونجح فحص الأنواع وESLint و`630` اختبارًا في `64` ملفًا والبناء (`3725` وحدة). لم يُنشر هذا التصحيح بعد. كما أثبتت محاولة ضريبية أن إعداد Staging الحالي لا يربط ضريبة المشتريات بـ`1105` ربطًا صالحًا؛ لم تُغير إعدادات الشركة، وأصبحت أول حالة قبول بلا ضريبة لاختبار خريطة `1104/2101`. تبقى خريطة `1105` بوابة مستقلة إذا فُعّلت الضرائب.

نُشر تصحيح مدخل المسودة على Staging في الحزمة `assets/index-DnNS2Wvi.js` بعد نسخة واجهة مستقلة، وتحققت استجابات `200` وثبات واجهتي الإنتاج. أُنشئت حالة القبول المضبوطة `PUR-990021` بحركة شراء قيمتها `100` بلا قيد، ثم اجتازت من الواجهة دورة المعالجة الكاملة في `IR-0048`: مسودة، انتظار مراجعة، اعتماد، ثم تنفيذ. أنشأ المنفذ القيد المرحل `13ab0c2f-36eb-46f4-8303-7fe4ffd3ed62` بسطر `1104` مدين `100` وسطر `2101` دائن `100`، وربطه بالفاتورة وسجّل أثرًا واحدًا من النوع `missing_inventory_journal_created`. لم ينشئ التنفيذ حركة مخزون جديدة، وبقيت كمية المنتج `2` وتكلفته `50`. بعد التنفيذ اختفت الفاتورة من قائمة الانحرافات، وأعاد المخطط `NO_CORRECTION_REQUIRED` بلا سطور تصحيح، وأكد المستخدم ظهور أثر التنفيذ المرتبط بالقيد. حُفظت أدلة المراحل الأربع وبصماتها داخل `/backups/staging/inventory-missing-journal-ui-acceptance-before-20260922-114237/ir-0048-*-verification`. بهذا اكتمل قبول خريطة فاتورة الشراء بلا ضريبة `1104/2101` فقط؛ لا تُعد خرائط البيع والمرتجعات والتسويات والضريبة مقبولة بعد. لم تتغير أي بيئة إنتاجية.

بدأت بوابة خريطة البيع بتجربة معاملاتية مستقلة على Staging، وليست قبول واجهة نهائيًا. أنشأ المشغّل المؤقت فاتورة بيع بلا ضريبة وحركة بيع واحدة بقيمة دفترية `80` وإيراد `100` دون قيد، فتحقق من تصنيف `movement_without_journal` ومن خطة `create_full_journal` المتوازنة ذات أربعة أسطر: `1103` مدين `100`، و`4101` دائن `100`، و`5101` مدين `80`، و`1104` دائن `80`. اختبر ملف التنظيف الصريح، ثم انتهت المعاملة بـ`ROLLBACK` وتطابق خط أساس Staging قبل التجربة وبعدها. حُفظ التقرير والاستعلامات والمشغّل واختباره وبصماتها في `/backups/staging/inventory-missing-journal-ui-acceptance-before-20260922-114237/sales-fixture-rehearsal`. لم تبق بيانات تجريبية ولم يتغير الإنتاج. كانت الخطوة التالية آنذاك إنشاء حالة بيع مضبوطة دائمة ثم تمريرها من الواجهة عبر دورة المعالجة الكاملة قبل اعتماد الخريطة.

اكتملت بوابة البيع بلا ضريبة بعد إنشاء `INV-990022` وتنفيذ `IR-0049` من الواجهة حتى حالة `executed`. أثبتت القراءة المستقلة اتزان القيد وارتباطه بالمعالجة، وأن الأثر المسجل هو `missing_inventory_journal_created`، وأن التشخيص اللاحق يعيد `NO_CORRECTION_REQUIRED`. حُفظت أدلة المسودة والإرسال والاعتماد والتنفيذ في `/backups/staging/inventory-missing-journal-sales-acceptance-before-20260923-014135/ir-0049-*-verification`.

كشفت هذه البوابة نقص `posted_number` في ثلاثة قيود مرحلة. أضافت Migration ‏`20260923030000_journal_posted_number_invariant.sql` قفلًا ذريًا للترقيم، وفهرسًا فريدًا، وقيد تحقق يمنع الحالة `posted` بلا رقم موجب، وقوّت بوابتي إنشاء القيد وترحيله. بعد L3 والرجوع وتجربة Staging المعاملاتية، طُبقت على Staging ومُلئت الأرقام `312–314`. أثبتت المراجعة أن الأعداد وسطور القيود لم تتغير، وأن سجل التدقيق يحصر التعديل في الرقم وتاريخ التحديث. تُركب الواجهة البادئة من `company_settings.journal_entry_prefix`، ولا توجد بادئة مثبتة في منفذ 2D. حُفظ دليل التطبيق في `/backups/staging/journal-posted-number-before-20260923-033809/post-apply-verification`، ودليل قبول `IR-0049` بعد الترقيم في `/backups/staging/inventory-missing-journal-sales-acceptance-before-20260923-014135/ir-0049-executed-post-numbering-verification`. لم تتغير أي قاعدة إنتاجية.

أثبتت مراجعة بوابة الضريبة أن مسارات الترحيل الأصلية تقرأ `purchase_tax_account_id` و`sales_tax_account_id` من إعدادات الشركة، بينما كان مخطط 2D يفرض الرمزين `1105/2102`. اعتمد العقد المحاسبي بقاء الحسابين قابلين للتهيئة: حساب المشتريات يجب أن يكون أصلًا نشطًا تفصيليًا، وحساب المبيعات التزامًا نشطًا تفصيليًا؛ والرمزان المذكوران افتراضيان لا ثابتان. أضيفت Migration ‏`20260923100000_inventory_reconciliation_configurable_tax_accounts.sql` وحارس إعدادات وحارس لتغيير الحساب المرتبط. نجحت ثمانية سيناريوهات داخل L3 لفواتير الشراء والبيع ومرتجعاتهما، وتغيير الحساب والبصمة، ورفض النوع الخاطئ والحساب غير النشط أو التجميعي. نجح ملف الرجوع الصريح وعادت بيانات ومخطط L3 إلى خط الأساس. حُفظ الدليل وبصماته في `/backups/staging/inventory-configurable-tax-accounts-l3-20260923`.

أُخذ بعد ذلك خط أساس Staging حديث وحُفظ في `/backups/staging/inventory-configurable-tax-before-20260923-055349`. أثبت أن الضريبة معطلة حاليًا وأن الحسابين فارغان، وأن Migrations ‏2D والترقيم موجودة بينما Migration الحسابات القابلة للتهيئة غائبة. شُغلت Migration والسيناريوهات الثمانية داخل معاملة انتهت بـ`ROLLBACK`، ثم اختُبر ملف الرجوع في معاملة مستقلة. طابقت الأعداد والبصمات وإعدادات الضريبة والتشخيص والمخطط خط الأساس قبل التجربة وبعدها. حُفظ الدليل في مجلد `transactional-rehearsal`. لم تُطبق Migration على Staging أو الإنتاج؛ البوابة التالية `dry-run` ثم التطبيق على Staging والتحقق الرسمي بعده.

اكتملت بوابة التطبيق الدائم على Staging بتاريخ 2026-09-23. أكد `dry-run` أن Migration ‏`20260923100000_inventory_reconciliation_configurable_tax_accounts.sql` وحدها منتظرة، ثم طُبقت عبر سجل الترحيلات الرسمي. أثبت تحقق التطبيق وفاحص مستقل لاحق تسجيل الإصدار وصحة دوال المخطط وحارسي إعدادات الضريبة والحساب المرتبط، وصحة الصلاحيات، وتطابق بيانات الأعمال وإعدادات الضريبة مع خط الأساس؛ بقيت الضريبة معطلة والحسابان فارغين، لذلك لم يُنشأ أي أثر محاسبي. حُفظت ملفات التطبيق والتحقق ومصادرها وبصماتها في `/backups/staging/inventory-configurable-tax-before-20260923-055349/post-apply-verification`. لم تتغير Farida أو Alibea، والبوابة التالية هي ربط حسابين صالحين على Staging بتغيير مضبوط ثم قبول فاتورة شراء ضريبية واحدة من الواجهة.

كشف فحص دلالي للقراءة فقط أن `1105` هو حساب ضريبة مدخلات صالح ومحمي، لكن `2102` و`2103` هما «قروض قصيرة الأجل» و«قروض طويلة الأجل» بلا استخدام سابق، وليسا حسابي ضريبة. لذلك مُنع تغيير معنى `2102` واعتمد الرمز المتاح `2104` باسم «ضريبة القيمة المضافة للمخرجات». القرار المعماري هو إنشاء `1105/2104` دائمًا كحسابي نظام محميين، وحفظ الربط الداخلي في `company_settings` مع عرضه للقراءة فقط لاحقًا؛ يبقى للمستخدم تفعيل الضريبة ونسبة واحدة، ولا تُضاف نسبتا شراء وبيع عامتان لأن التعدد المستقبلي الصحيح يكون بتعريفات ضريبية على مستوى المستند أو السطر.

أضيفت Migration ‏`20260923130000_inventory_output_tax_system_account.sql` وملف رجوع صريح. تنشئ `2104` تحت أصل الخصوم، وتملأ الروابط الفارغة فقط بـ`1105/2104` دون تغيير `enable_tax` أو `tax_rate` ودون استبدال أي ربط صحيح موجود. أُضيف الحسابان إلى مصدر التهيئة الافتراضي، وأصبحت إعادة بناء النظام تربطهما تلقائيًا. نجحت ثمانية سيناريوهات L3 في إثبات الهوية والحماية وبقاء `2102` حساب قروض والربط الافتراضي وثبات التفعيل والنسبة، ثم نجح ملف الرجوع في إزالة `2104` وربطه دون مساس ببيانات الأعمال. حُفظ تقرير L3 وتدقيقا الحسابات والمصادر وبصماتها في `/backups/staging/inventory-output-tax-system-account-l3-20260923`.

اكتملت بوابة Staging للحساب بتاريخ 2026-09-23. حُفظت النسخة وخط الأساس في `/backups/staging/inventory-output-tax-before-20260923-101332`، ثم نجحت تجربة التطبيق والتكرار والحماية والرجوع داخل معاملات انتهت بـ`ROLLBACK` وعادت جميع البصمات. أكد `dry-run` أن Migration المطلوبة وحدها منتظرة، ثم طُبقت وسُجلت رسميًا. أثبت التحقق المستقل وجود `2104` بالهوية النظامية المحمية، وبقاء `2102/2103` حسابي قروض، وربط `company_settings` بـ`1105/2104` مع بقاء الضريبة معطلة والنسبة صفرًا، وثبات بيانات الأعمال والتشخيص. حُفظ دليل التطبيق والتحقق في `post-apply-verification-corrected` داخل النسخة. لم تتغير Farida أو Alibea؛ البوابة التالية قبول فاتورة شراء ضريبية مضبوطة من الواجهة.

### قيد بلا حركة

لا يعكس القيد كاملاً تلقائياً؛ فقد يحتوي أطرافاً صحيحة أخرى. يجب تحليل سطور القيد وتحديد هل المطلوب قيد تصحيح جزئي أم مستند مخزون مفقود.

### فرق التقريب

محجوب حتى اعتماد:

- الحد لكل مستند والحد الإجمالي.
- حساب فروق التقريب في `company_settings` أو إعداد محاسبي مكافئ.
- اتجاه القيد وسياسة التاريخ والفترة المقفلة.

فرق Staging الحالي `0.02` يبقى ملاحظة موثقة ولا يحتاج تصحيحاً لمجرد صغره.

## 11. الفهارس الأولية

```text
repairs (status, created_at desc)
repairs (prepared_by, created_at desc)
items (repair_id, line_number)
items (axis, issue_key)
items (product_id) where product_id is not null
items (source_type, source_id) where source_id is not null
effects (repair_id, repair_item_id)
events (repair_id, created_at)
```

لا يضاف فهرس إلى جداول الأعمال قبل إثبات الحاجة بـ`EXPLAIN (ANALYZE, BUFFERS)` على Staging.

## 12. اختبارات 2B قبل Migration

1. رفض البائع و`anon` لكل قراءة أو أمر.
2. نجاح قراءة المدير والمحاسب.
3. المحاسب ينشئ ويعدل مسودة صحيحة.
4. رفض UUID مصدر غير موجود أو لا يطابق `source_type`.
5. رفض منتج أو مصدر حالته `matched`.
6. رفض فرق WAC وحده كبند إصلاح.
7. رفض تكرار `issue_key` في العملية.
8. رفض فتح عملية نشطة ثانية لنفس `axis + issue_key` ولو كانت في رأس مختلف.
9. نجاح الإرسال للمراجعة وقفل البنود.
10. رفض تعديل بند بعد الإرسال.
11. المدير وحده يعتمد.
12. رفض الاعتماد عند تغير `version`.
13. إعادة `request_id` نفسه لا تنشئ حدثاً أو عملية ثانية.
14. رفض التنفيذ في 2B برسالة `REPAIR_TYPE_NOT_ENABLED` وبقاء بيانات الأعمال كما هي.
15. إلغاء المسودة/المعتمدة حسب الصلاحية دون حذف.
16. سلامة القيود الأجنبية وRLS والمنح و`search_path` وتطابق بيانات الأعمال بعد `ROLLBACK`.

## 13. اختبارات 2C الإضافية

1. نجاح إعادة بناء البطاقة عند سلامة الحركات وحدها.
2. رفض التنفيذ إذا أصبحت البطاقة مطابقة بالفعل.
3. رفض التنفيذ إذا تغيرت كمية البطاقة بعد المعاينة.
4. رفض التنفيذ إذا أضيفت حركة للمنتج بعد المعاينة.
5. عدم تأثر العملية بحركة على منتج آخر.
6. منع التنفيذ المكرر بنفس `request_id` أو بطلب جديد للعملية المنفذة.
7. حفظ أثر قبل/بعد وحدث التنفيذ والفاعل.
8. إعادة التشخيص وإثبات صفر فرق الكمية.
9. عدم إنشاء حركة أو قيد أو تعديل قيمة تكلفة.
10. فشل أي تحقق يرجع تحديث المنتج وكل آثار العملية.

## 14. خطة الرجوع

- Migration 2B تضيف جداول ودوال جديدة فقط ولا تعدل مستندات قديمة.
- قبل Staging تؤخذ نسخة schema/data وخط أساس الأعمال.
- يجرب الإنشاء ودورة الحالات داخل معاملة تنتهي بـ`ROLLBACK` على نسخة L3 أولاً.
- ملف الرجوع يحذف الدوال ثم الجداول الجديدة بترتيب العلاقات، ولا يلمس التشخيص أو جداول الأعمال.
- بعد وجود عملية منفذة في أي بيئة لا يسمح بإسقاط الجداول كرجوع عادي؛ يستخدم إصلاح أمامي أو عكس أعمال موثق.
- لا يطبق شيء على Farida أو Alibea ضمن المرحلة الحالية.

## 15. بوابة الانتقال إلى 2B

لا تبدأ Migration الجداول حتى:

- اعتماد هذا العقد.
- حسم أن عمليات المعالجة لا تحذف، بل تلغى.
- اعتماد أن البصمة الموضعية هي حاجز التنفيذ، والبصمة العامة دليل تشخيص فقط.
- قبول أن 2B يبني دورة المستند بلا أي إصلاح فعلي.
- كتابة اختبارات TDD الحمراء للعقد أولاً.

**حالة البوابة:** مكتملة بتاريخ 2026-09-13. اعتمد المستخدم العقد، وثُبتت سياسة الإلغاء وعدم الحذف، والفصل بين البصمة العامة وشرط البداية الموضعي، وحدود 2B دون كتابة على جداول الأعمال. نجح الأمر المعزول:

```text
sudo /usr/bin/node /opt/accounting-app/scripts/tests/rehearse-inventory-repair-lifecycle.mjs --expect-missing
TDD_REPAIR_LIFECYCLE_RED_OK
```

أكد الاختبار غياب الجداول والدوال الجديدة كما هو متوقع قبل التنفيذ، ولم ينفذ سيناريوهات بيانات أو يغير L3 أو Staging أو الإنتاج.

بعد كتابة Migration ‏`20260913234500_inventory_reconciliation_repair_lifecycle.sql`، نجحت السيناريوهات الستة عشر داخل L3 المعزولة مع `ROLLBACK` كامل. أثبت الاختبار دورة الإنشاء والتعديل والإرسال والاعتماد والإلغاء، والصلاحيات وRLS، ومنع التكرار والطلبات القديمة، وبقاء منفذ الإصلاح الفعلي محجوبًا بالرمز `REPAIR_TYPE_NOT_ENABLED`. لم تبق الجداول أو الدوال أو العينات في L3، ولم تتغير Staging أو قواعد الإنتاج.

حُفظ دليل التجربة والتحقق منه في:

```text
/backups/staging/inventory-reconciliation-before-20260913-192242/repair-lifecycle-l3-rehearsal
```

بتاريخ 2026-09-14 أُخذت نسخة حديثة من Staging قبل 2B وحُفظت في:

```text
/backups/staging/inventory-repair-before-20260914-081004
```

أثبت خط الأساس وجود `613` منتجًا و`1354` حركة مخزون، وحالة التشخيص `rounding_only`، وغياب جميع جداول ودوال 2B وسجل إصدارها. بعد ذلك شُغلت Migration والسيناريوهات الستة عشر على مشروع Staging `dunzfxurefzlaamgghys` داخل معاملة واحدة انتهت بـ`ROLLBACK`. أكد فحص مستقل اختفاء كل مكونات 2B وتطابق أعداد وبصمات بيانات الأعمال مع النسخة. حُفظ دليل التجربة في:

```text
/backups/staging/inventory-repair-before-20260914-081004/repair-lifecycle-transactional-rehearsal
```

قبل التطبيق الدائم أُنشئ ملف رجوع صريح مقيد برمز Staging ويرفض العمل إذا وُجد أي سجل معالجة. اختُبر داخل L3، فأزال الدوال والجداول دون `CASCADE` مع بقاء بيانات الأعمال كما هي. حُفظ الدليل في:

```text
/backups/staging/inventory-repair-before-20260914-081004/repair-lifecycle-explicit-rollback-rehearsal
```

أكد `dry-run` الرسمي أن `20260913234500_inventory_reconciliation_repair_lifecycle.sql` هو الترحيل الوحيد المنتظر، دون seeds أو roles. بعد الاعتماد طُبق هذا الملف وحده على Staging، ثم نجح تحقق مستقل أثبت:

- تسجيل الإصدار ووجود الجداول الأربعة والدوال الست العامة.
- تفعيل RLS ومنع DML المباشر ومنع `anon` من تنفيذ RPC.
- إخفاء الدوال الداخلية عن `authenticated`.
- بقاء جداول المعالجة الأربعة فارغة بعد التطبيق.
- تطابق أعداد وبصمات بيانات الأعمال وبصمة التشخيص وحالته `rounding_only` مع النسخة السابقة.
- عدم تعديل Farida أو Alibea.

حُفظ دليل التطبيق والتحقق منه في:

```text
/backups/staging/inventory-repair-before-20260914-081004/repair-lifecycle-post-apply
```

بهذا اكتملت بوابة قاعدة البيانات للمرحلة 2B على Staging. بُنيت بعد ذلك قائمة سجل مستقلة للقراءة على المسار `/reports/inventory-reconciliation/repairs`، تشمل البحث بالرقم أو العنوان وفلتر الحالة والترقيم الخادمي والربط من شاشة التشخيص، ولا تستدعي أي RPC كتابة أو تنفيذ. اجتازت فحص الأنواع والبناء وESLint و`588` اختبارًا، ثم نُشرت على Staging فقط بعد حفظ نسخة الرجوع `/opt/backups/accounting-app/staging-inventory-repair-registry-before-20260914-090014`. تطابقت بصمة `index.html` المنشور مع البناء (`34e85ae3f7fa96988488d58588fa8ee84d395f89a31e197f32bf41bb7ff22201`)، وأعادت الصفحة والمسارات المباشرة والأصل الرئيسي الحالة `200`، وبقيت ملفات Farida وAlibea دون تغيير. أكد المستخدم ظهور الصفحة، فاعتُمدت القائمة بصريًا.

بُنيت بعدها شاشة تفاصيل للقراءة على المسار `/reports/inventory-reconciliation/repairs/:id`، ويفتحها صف السجل. تعرض الرأس والحالة والإصدار ولقطة التشخيص والبنود والحالة قبل/المقترحة وسجل الأحداث وآثار التنفيذ، مع روابط للسجلات المعروفة. لا تحتوي استدعاءات إنشاء أو تعديل أو اعتماد أو تنفيذ. اجتازت فحص الأنواع وESLint والبناء و`592` اختبارًا، ثم نُشرت على Staging فقط بعد حفظ نسخة الرجوع `/opt/backups/accounting-app/staging-inventory-repair-detail-before-20260914-092703`. تطابقت بصمة `index.html` المنشور مع البناء (`e9fd7b60b9191f4369ffb65dffe7aaca1b1debf9cd4f6f455a2bb90d3098b`)، وأعادت مسارات السجل والتفاصيل المباشرة والأصل الرئيسي الحالة `200`، وبقيت ملفات Farida وAlibea دون تغيير. أكد المستخدم أن المعرّف غير الموجود يعرض حالة الفراغ الآمنة دون كشف الفرق بين غياب السجل ومنع الصلاحية. بقي اختبار الرأس والبنود والأحداث الفعلية مع أول مسودة في الدفعة التالية، مع إبقاء التنفيذ الفعلي محجوبًا حتى المرحلة 2C.

أُعدت بعدها واجهة إنشاء أول مسودة من صف التشخيص نفسه. لا يظهر إجراء «إعداد مسودة» إلا عندما تعيد دالة التشخيص `can_prepare_repair=true`، ويُشتق نوع المعالجة آليًا من التصنيف دون اختيار يدوي. يطلب النموذج عنوانًا وسببًا، ويرسل بصمة التشخيص ووقت اللقطة ونطاق المصدر وبندًا واحدًا إلى `create_inventory_reconciliation_repair` مع `request_id` ثابت للمحاولة وإعادتها. لا تستدعي هذه الدفعة التعديل أو الإرسال للمراجعة أو الاعتماد أو التنفيذ، ولا تكتب مباشرة في أي جدول. اجتازت فحص الأنواع والبناء وESLint و`597` اختبارًا، ودُفعت في الالتزام `9c12f26` ثم نُشرت على Staging فقط بعد حفظ نسخة الرجوع `/opt/backups/accounting-app/staging-inventory-repair-draft-before-20260914-134944`. كشف الاختبار الأول أن مفتاح `sb_publishable` المأخوذ من الحزمة السابقة يعيد `401 Invalid API key`؛ فأعيدت نسخة الرجوع فورًا وتأكد تطابق بصمتها وعدم تغير الإنتاج. اختُبر مفتاح `anon JWT` العام من الحزمة السابقة وأعاد `200`، ثم أعيد البناء والنشر به. تطابقت بصمة `index.html` المصحح مع البناء (`88bab8ba20f7d9e0113bac4285f90c68dc7a0d0b480036cc5435f10e31c424e1`)، وقدمت الواجهة الأصل `assets/index-DUBcVR__.js`، وأعادت المسارات الأربعة وREST API الحالة `200`. بقيت بصمتا واجهتي Farida وAlibea دون تغيير، ولم تُطبق Migration أو Edge Function. أنشأ المستخدم `IR-0001` لفاتورة الشراء `PUR-0024` بنجاح. أثبت فاحص Staging داخل معاملة قراءة فقط وجود مسودة واحدة وبند واحد وحدث إنشاء واحد وصفر آثار تنفيذ، وتطابق أعداد وبصمات المنتجات والحركات والفواتير والقيود والتشخيص مع خط الأساس. حُفظ التقرير واستعلامه مع بصماتهما في `/opt/backups/accounting-app/staging-inventory-repair-draft-before-20260914-134944/first-draft-verification`، وبذلك اعتُمد إنشاء المسودة وتفاصيلها وظيفيًا دون أي إصلاح أو ترحيل للأعمال.

بُنيت بعدها دفعة تعديل المسودة فقط. يظهر الإجراء عندما تكون الحالة `draft`، ويعيد إرسال البنود المقروءة دون تغيير هويتها عبر `update_inventory_reconciliation_repair` مع `expected_version` و`request_id` ثابت للمحاولة. يسمح النموذج بتعديل العنوان والسبب فقط، ويرفض الحفظ دون تغيير فعلي حتى لا ينشئ إصدارًا وحدثًا بلا معنى. تتولى قاعدة البيانات رفض تغير الحالة أو الإصدار أو التشخيص. لا تستدعي الدفعة الإرسال للمراجعة أو الاعتماد أو التنفيذ ولا تنفذ DML مباشرًا. اجتازت فحص الأنواع وESLint والبناء و`601` اختبارًا، ودُفعت في الالتزام `158f34f` ثم نُشرت على Staging فقط بعد حفظ نسخة الرجوع `/opt/backups/accounting-app/staging-inventory-repair-edit-before-20260914-142802`. تطابقت بصمة `index.html` المنشور مع البناء (`6ee6c0ce2d4d1b53c6f93e6bcf175df0fdfb2206611ce8133f42b7aefdb1bced`)، وقدمت الواجهة الأصل `assets/index-D4nnrrRH.js`، وأعادت المسارات وREST API الحالة `200` مع ثبات بصمتي الإنتاج. عدّل المستخدم `IR-0001` بنجاح فأصبحت الإصدار 2، وأثبت فحص قراءة وجود بند واحد وحدثي إنشاء وتحديث وصفر آثار، وتطابق بيانات الأعمال والتشخيص مع خط الأساس. حُفظ دليل التحقق في `/opt/backups/accounting-app/staging-inventory-repair-edit-before-20260914-142802/edit-verification`، وبذلك اعتُمد تعديل المسودة وظيفيًا دون إصلاح أو ترحيل.

أظهرت المراجعة البصرية أن سجل الأحداث كان يعرض `actor_id` مختصرًا تحت مسمى «فاعل». بُني تحسين يعرض «نفّذ بواسطة: الاسم — الدور» من `profiles` و`user_roles` بطلبين مجمعين لكل المنفذين، مع احترام RLS وعدم كشف UUID. يُدمج اسم ودور المستخدم الحالي من سياق الجلسة، ويظهر البديل «مستخدم مخوّل» إذا حجبت السياسات بيانات مستخدم آخر. اجتاز التحسين فحص الأنواع وESLint والبناء و`603` اختبارات، ودُفع في الالتزام `3145fea` ثم نُشر على Staging فقط بعد حفظ نسخة الرجوع `/opt/backups/accounting-app/staging-inventory-repair-actor-before-20260914-144504`. تطابقت بصمة `index.html` المنشور مع البناء (`f97ca34e600051839c20724cb1d9c240cffdaa1e1f2c01128a035b394f50595e`)، وقدمت الواجهة الأصل `assets/index-D1GYXl0t.js`، وأعادت المسارات وREST API الحالة `200`، وبقيت بصمتا واجهتي الإنتاج دون تغيير. أكد المستخدم ظهور اسم المنفذ ودوره، فاعتُمد التحسين بصريًا.

بُنيت بعد ذلك دفعة «إرسال المسودة للمراجعة» فقط. يظهر الإجراء للمسودة التي تحتوي بندًا واحدًا على الأقل، ويستدعي `submit_inventory_reconciliation_repair` مع `expected_version` و`request_id` ثابت للمحاولة. بعد نجاح الانتقال إلى `ready_for_review` تختفي إجراءات التعديل والإرسال. لا تستدعي هذه الدفعة إنشاءً أو تعديلًا أو اعتمادًا أو تنفيذًا، ولا تكتب مباشرة في جداول المعالجة أو بيانات الأعمال. اجتازت فحص الأنواع وESLint والبناء وجميع الاختبارات وعددها `606` في `59` ملفًا، ودُفعت في الالتزام `9395bc9` ثم نُشرت على Staging فقط بعد حفظ نسخة الرجوع `/opt/backups/accounting-app/staging-inventory-repair-submit-before-20260914-161209`. تطابقت بصمة `index.html` المنشور مع البناء (`d1b8db0826a664efec8c3e1ddecc9a3a114935c8f638408b0f35e8c945b6990e`)، وقدمت الواجهة الأصل `assets/index-Bu0znVN3.js`، وأعادت المسارات وREST API الحالة `200`، وبقيت بصمتا واجهتي الإنتاج دون تغيير. أرسل المستخدم `IR-0001` للمراجعة بنجاح؛ فأصبحت الحالة `ready_for_review` والإصدار 3، مع بند واحد وثلاثة أحداث بينها حدث إرسال واحد وصفر آثار. أثبت الفاحص المقروء تطابق بيانات الأعمال والتشخيص مع خط الأساس، وحُفظ دليله في مجلد `submit-verification` داخل نسخة الرجوع. بذلك اعتُمد الإرسال للمراجعة وظيفيًا على Staging دون اعتماد أو تنفيذ أو ترحيل.

بُنيت بعد ذلك دفعة «اعتماد المعالجة» فقط. لا يظهر الإجراء إلا للمدير عندما تكون الحالة `ready_for_review`، ويستدعي `approve_inventory_reconciliation_repair` مع `expected_version` و`request_id` ثابت للمحاولة. إذا كان المدير هو نفسه مُعدّ المسودة، تفرض الواجهة سببًا صريحًا لعدم فصل المهام وترسله إلى سجل التدقيق؛ وتبقى قاعدة البيانات هي الحاجز النهائي للصلاحية والسبب والحالة والإصدار. تؤكد الواجهة أن الاعتماد لا ينفذ حركة مخزون أو قيدًا، ولا تحتوي استدعاء تنفيذ أو عكس أو إلغاء أو كتابة مباشرة في الجداول. اجتازت فحص الأنواع وESLint والبناء وجميع الاختبارات وعددها `610` في `60` ملفًا، ودُفعت في الالتزام `48f93bb` ثم نُشرت على Staging فقط بعد حفظ نسخة الرجوع `/opt/backups/accounting-app/staging-inventory-repair-approve-before-20260914-182542`. تطابقت بصمة `index.html` المنشور مع البناء (`07afb1b89baeb6f09dc3230aa963a019e80d338d33dfbe75583ab2ceb90684ac`)، وقدمت الواجهة الأصل `assets/index-CoplrZcx.js`، وأعادت المسارات وREST API الحالة `200`، وبقيت بصمتا واجهتي الإنتاج دون تغيير. اعتمد المستخدم `IR-0001` بنجاح؛ فأصبحت الحالة `approved` والإصدار 4، مع بند واحد وأربعة أحداث بينها حدث اعتماد واحد وصفر آثار، واستوفى توثيق عدم فصل المهام.

كشف فاحص القبول اختلاف البصمة الخام لصفوف فواتير الشراء عن خط الأساس، فرفض النتيجة أولًا. أثبت سجل التدقيق أن الفاتورة `PUR-0024` تلقت بعد الاعتماد بـ46 ثانية معاملة منفصلة غيّرت `updated_at` فقط، دون تغيير مبلغ أو حالة أو سداد، بينما بقيت أعداد الأعمال وبقية البصمات وبصمة التشخيص وحالته مطابقة. عُزز الفاحص بحيث لا يقبل هذا الاستثناء إلا بعد الاعتماد، ولاسم `purchase_invoices` وحده، وعندما يثبت سجل التدقيق عدم تغير أي حقل سوى `updated_at` وأن أول تحديث وقع بعد وقت الاعتماد؛ وأي تغير مالي أو تشخيصي يظل مرفوضًا. نجح الفحص المعزز وحُفظ التقرير والاستعلام والمخرج المحمي في مجلد `approve-verification` داخل نسخة الرجوع. بذلك اكتملت دورة 2B من الإنشاء حتى الاعتماد على Staging دون تنفيذ أو ترحيل، ولم يتغير الإنتاج.

بدأت المرحلة 2C بعقد مستقل لأول منفذ فعلي: `inventory_reconciliation_rebuild_product_card_contract.sql`. يغطي العقد عشرة سيناريوهات: النجاح، البطاقة التي أصبحت مطابقة، تغير البطاقة، إضافة حركة للمنتج، عدم التأثر بحركة منتج آخر، منع التكرار، قصر التنفيذ على المدير، أثر التدقيق والفحص اللاحق، منع إنشاء حركة أو قيد أو تغيير التكلفة، والرجوع الذري عند فشل الفحص اللاحق. أضيف حاجز تشغيل لا يقبل إلا حاوية `l3_public_restore` المعزولة. نجح اختبار TDD الأحمر بعلامة `TDD_REBUILD_PRODUCT_CARD_RED_OK` وأثبت أن منفذ 2B ما زال يعيد `REPAIR_TYPE_NOT_ENABLED` وأن Migration ‏2C غير موجودة، دون تنفيذ عينات أو تغيير L3 أو Staging أو الإنتاج. الخطوة التالية كتابة Migration وملف رجوع 2C ثم تشغيل السيناريوهات داخل معاملة تنتهي بـ`ROLLBACK`.

كُتبت Migration ‏`20260914190000_inventory_reconciliation_rebuild_product_card_executor.sql` لتسمح بنوع `rebuild_product_card` وحده وللمدير فقط. تعيد فحص الشرط الموضعي بعد قفل سجل المعالجة والمنتجات المستهدفة، وترفض أي تغير في بطاقة المنتج أو حركاته أو بيانات المعاينة، ثم تعدل `products.quantity_on_hand` فقط، وتعيد التشخيص قبل حفظ الأثر والحدث والحالة. لا تنشئ حركة مخزون أو قيدًا ولا تغير التكلفة. اجتازت السيناريوهات العشرة داخل L3 المعزولة مع `ROLLBACK` كامل وثبات بيانات الأعمال. كما اجتاز ملف الرجوع الصريح اختبارًا مستقلًا أعاد حاجز 2B، وأبقى جداول الدورة والبيانات دون تغيير، ويرفض الرجوع إذا وجد تنفيذًا فعليًا. حُفظت تقارير التنفيذ والرجوع والعقد والملفان مع بصماتها في `/backups/staging/inventory-repair-before-20260914-081004/rebuild-product-card-l3-rehearsal`. لم تُطبق Migration على Staging أو الإنتاج بعد.

أُخذت بعد ذلك نسخة حديثة من Staging وخط أساس للبيانات والمخطط في `/backups/staging/inventory-rebuild-before-20260914-190747`. أثبت الخط وجود `613` منتجًا و`1354` حركة، ومعالجة واحدة بلا آثار تنفيذ، وحالة تشخيص `rounding_only`، وبقاء منفذ 2B محجوبًا وغياب إصدار 2C. طُبقت Migration والسيناريوهات العشرة داخل معاملة Staging انتهت بـ`ROLLBACK`، ثم جُرّب ملف الرجوع داخل معاملة مستقلة، وأثبت الفحص الخارجي عودة حاجز 2B وتطابق أعداد وبصمات بيانات الأعمال والمعالجات والتشخيص مع الخط. حُفظت ملفات التجربة والتقرير وبصماتها في `transactional-rehearsal` داخل النسخة. لم تسجل Migration ولم يتغير الإنتاج.

أكد `dry-run` الرسمي أن Migration ‏2C وحدها منتظرة بلا Seeds أو Roles. طُبقت بعد ذلك على مشروع Staging وحده وسُجل الإصدار `20260914190000` رسميًا. اجتاز فحص مستقل للقراءة فقط وجود الدالة و`SECURITY DEFINER` و`search_path` الآمن والمنح الصحيحة، وتفعيل `rebuild_product_card` مع بقاء الأنواع الأخرى محجوبة. تطابقت جميع أعداد وبصمات بيانات الأعمال والمعالجات والتشخيص مع النسخة، وبقي عدد آثار التنفيذ صفرًا. حُفظ تقرير ما بعد التطبيق والاستعلام وملفا التنفيذ والرجوع وبصماتها في `post-apply-verification` داخل النسخة. لم تُضف واجهة التنفيذ بعد، ولم يتغير الإنتاج.

بُنيت بعد ذلك واجهة تنفيذ منفصلة لنوع `rebuild_product_card` فقط. لا تظهر إلا للمدير عندما تكون المعالجة `approved`، وبها بند واحد على الأقل، وجميع البنود من محور المنتج وتصنيف فرق الرصيد وبحالة `pending` ولها كمية مقترحة. تتطلب كتابة رقم المعالجة كاملًا قبل التأكيد، وترسل الإصدار الحالي و`request_id` ثابتًا للمحاولة إلى RPC دون أي كتابة مباشرة في الجداول. تعرض بوضوح الكمية قبل وبعد، وتوضح أن قاعدة البيانات ستعيد فحص البطاقة والحركات وترفض العملية ذريًا إذا تغيرت. أما فرق التقريب وبقية الأنواع فتظل بلا زر تنفيذ. اجتازت الواجهة فحص الأنواع وESLint وجميع اختبارات المشروع وعددها `615` في `61` ملفًا، ونجح البناء المحلي بعد تحويل `3722` وحدة. لم تُرفع أو تُنشر بعد، ولم تُنفذ أي معالجة.

دُفعت الواجهة في الالتزام `56fb50f`، ثم حُفظت نسخة رجوع كاملة من Staging في `/opt/backups/accounting-app/staging-inventory-repair-execute-ui-before-20260914-194234`. بُنيت الحزمة بالمفتاح العام الصحيح الموجود في واجهة Staging بعد التحقق منه دون طباعته، واجتازت حواجز غياب عناوين الإنتاج والمشروع القديم. نُشرت على Staging فقط؛ تطابقت بصمة `index.html` مع البناء (`5def0e199fd0292162e0670e04c760cd5d5a2a2038162ade9c8fbc80e4f10fee`)، وقدمت الواجهة `assets/index-CGpajHMm.js`، وأعادت الصفحة والسجل والأصل الرئيسي الحالة `200`، وبقيت واجهتا Farida وAlibea دون تغيير. أكد المستخدم أن `IR-0001` لا تعرض زر التنفيذ لأنها معالجة فرق تقريب، فاجتاز حارس النوع القبول البصري. لم تُنفذ أي معالجة بعد.

بتاريخ 2026-09-15 أُعدت حالة قبول مضبوطة على Staging للمنتج `PRD-002 — قميص قصير زبدة`. ثُبت أولًا أن كمية البطاقة وصافي الحركات كلاهما `1`، وأن للمنتج حركتين بقيمة دفترية صافية `130.00` ولا يرتبط بمعالجة سابقة. حُفظ حارس الإعداد والرجوع قبل الاختبار في `/backups/staging/inventory-rebuild-before-20260914-190747/acceptance-mismatch-before`، ثم رُفعت كمية البطاقة وحدها مؤقتًا إلى `2` داخل معاملة محمية، فصنف التشخيص الحالة `product_balance` وعرض الإجراء «إعداد مسودة» دون تغيير الحركات أو القيود.

أنشأ المستخدم `IR-0012` من الانحراف، ثم مررها عبر الحالات `draft -> ready_for_review -> approved -> executed`. عند الاعتماد ظهر زر التنفيذ للنوع المدعوم فقط، وطلبت النافذة كتابة `IR-0012` كاملًا وعرضت `2.00 -> 1.00`. بعد التنفيذ أصبحت المعالجة بالإصدار `4`، وبندها `applied`، وسجلت أربعة أحداث مستقلة وأثرًا واحدًا من النوع `product_card_rebuilt`. أعادت العملية كمية البطاقة إلى `1` دون إنشاء حركة مخزون أو قيد أو تغيير تكلفة.

اجتاز فاحص مستقل داخل معاملة `READ ONLY` التحقق من رأس المعالجة والبند وتسلسل الأحداث والأثر وحالة المنتج والتشخيص اللاحق. طابقت بصمات `inventory_movements` وفواتير البيع والشراء والقيود وسطورها خط الأساس السابق حرفيًا، وبقي للمنتج حركتان بصافي كمية `1` وقيمة `130.00`، وأصبح فرق الكمية العام صفرًا. تغيرت بصمة التشخيص العامة كما ينبغي لأن تصميمها يشمل `products.updated_at` لمنع إعادة استخدام لقطة قديمة؛ لم يُعامل هذا كتغير مالي، واعتمد الفاحص بدلًا منه تطابق الحقول الثابتة وبصمات الأعمال غير المسموح بتغييرها. حُفظت ملفات الإعداد والاستعلام والتقرير والفاحص مع `SHA256SUMS` في `/backups/staging/inventory-rebuild-before-20260914-190747/acceptance-execution-ir-0012`. بهذا اكتمل قبول التنفيذ الحقيقي الأول لـ2C على Staging، وبقي اختبار رفض اقتراح قديم بعد تغير بياناته بوابة الإغلاق الأخيرة قبل الانتقال إلى 2D. لم تتغير Farida أو Alibea.

بتاريخ 2026-09-20 راجع المستخدم عرض الجداول قبل الانتقال إلى بوابة 2C التالية. فصلت دفعة الواجهة «نوع السجل» عن «السجل المتأثر» في بنود المعالجة، و«الموضوع» عن «سبب المعالجة» في السجل مع اختصار النص الطويل في صف واحد. تستخدم القائمة `DataTable` المشترك، وتستخدم شاشة التشخيص والتفاصيل مكونات `Table` المشتركة؛ لم يُنشأ محرك جدول مكرر. أصبحت تسمية المنتج تستخدم `formatProductDisplay` المشتركة مع الفواتير، مع قراءة الماركة والموديل لمنتجات الصفحة المرئية فقط وبدون أي حقل تكلفة. كشف التدقيق أن التشخيص القديم قد يعيد `can_prepare_repair=true` للمنتج المصنف `matched` لأن العلم يعبر عن سلامة المصدر لا وجود فرق؛ مع ذلك ترفض قاعدة البيانات إنشاء معالجته. أضيف حارس واجهة ومساعد بناء المسودة يمنعان عرض زر «إعداد مسودة» أو إنشاء بند للسجل المطابق.

حُفظت نسخة واجهة Staging قبل النشر في `/opt/backups/accounting-app/staging-inventory-ui-before-LoaFKdo8`، وتحقق `diff` من مطابقتها للمنشور السابق. بُنيت الحزمة بمشروع Staging ومفتاحه العام دون كشف المفتاح، وثبت خلو الأصول من عناوين API الإنتاج. نُشرت الحزمة على Staging فقط وتطابق `index.html` مع البناء ببصمة `ebf9f78d4bc40064928fbed3c64ed4aeaedaa6b15aaff3d161830e1672ae3e0c`، وأصبح الأصل الرئيسي `assets/index-D9zqyORI.js`. بقيت بصمتا `index.html` في Farida وAlibea ثابتتين. أكّد المستخدم بصريًا صحة اسم المنتج واختفاء زر المسودة من الصف المطابق، وانفصال أعمدة السجل وانخفاض ارتفاعها، وانفصال نوع السجل عن السجل المتأثر في تفاصيل `IR-0012`. لم تتغير قاعدة بيانات أو واجهة إنتاجية. تعذر اختبار HTTP الآلي من جلسة العمل بسبب عزل الشبكة؛ عوّضه فحص تطابق الأصول والقبول البصري، ولا ينبغي تسجيل استجابة HTTP ناجحة دون فحص من خارج الجلسة.

بتاريخ 2026-09-21 اكتملت بوابة منع تنفيذ اقتراح قديم على Staging باستخدام `IR-0013`. أُنشئت المعالجة من فرق مضبوط للمنتج `PRD-002` عند البطاقة `2` والحركات `1`، ثم أرسلت واعتمدت. بعد الاعتماد غُيرت البطاقة وحدها إلى `3`. بقي البند محتفظًا بلقطة `2 -> 1`، لذلك رفض المنفذ الاستدعاء بالخطأ `REPAIR_PRECONDITION_CHANGED`. تحقق الاستدعاء المضبوط من بقاء الرأس `approved` بالإصدار `3`، وصفر آثار وصفر أحداث تنفيذ، وبقاء البطاقة `3` والحركات `1`، وتطابق بصمات الحركات وفواتير البيع والشراء والقيود وسطورها. أُلغيت المعالجة بعد الإثبات وأعيدت البطاقة إلى `1` في معاملة محمية، فأصبح التشخيص `matched` ولم تُنشأ حركة أو قيد أو تكلفة أو أثر معالجة. محاولة التنفيذ من واجهة المتصفح بقيت عالقة في حالة التحميل رغم أن قاعدة البيانات لم تنفذ شيئًا؛ لذلك فحارس قاعدة البيانات مقبول، لكن يلزم إصلاح مستقل في الواجهة يضع مهلة للطلب ويعرض رسالة انتهاء مهلة واضحة ولا يعيد عملية الكتابة تلقائيًا.

أُغلق عيب التعليق في الواجهة بتاريخ 2026-09-21. أصبح طلب التنفيذ يستخدم إشارة إلغاء بمهلة `20` ثانية، ويغلق نافذة التنفيذ عند انتهائها ويعرض أن حالة التنفيذ غير مؤكدة مع توجيه المستخدم إلى تحديث الصفحة والتحقق من حالة المعالجة قبل أي محاولة أخرى. لا تعيد الواجهة طلب الكتابة تلقائيًا، كما لم يعد نجاح العملية ينتظر تحديث الاستعلامات في الخلفية. اجتاز المساعد ثلاثة اختبارات مستقلة، واجتاز المكون اختبارات الحراس والمهلة، ثم اجتازت الحزمة كاملة `625` اختبارًا في `63` ملفًا وفحص الأنواع وESLint والبناء. حُفظت واجهة Staging السابقة في `/opt/backups/accounting-app/staging-inventory-execution-timeout-before-20260921-065206`، ونُشر الإصلاح على Staging فقط بالأصل الرئيسي `assets/index-NGcdDbsS.js`. أعادت الصفحة والسجل والأصل الحالة `200`، وثبتت بصمتا واجهتي Farida وAlibea دون تغيير. لا يوجد تنفيذ تلقائي ولا تغيير في قاعدة البيانات ضمن هذه الدفعة.
