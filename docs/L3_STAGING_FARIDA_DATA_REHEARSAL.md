# تجربة بيانات Farida على Staging قبل L3

> التاريخ: 2026-09-08
> الحالة: اكتملت النسخة والاستعادة المعزولة وانتقال السجل وقبول L3 على Staging؛ لم يُطبق انتقال السجل على Farida أو Alibea بعد

## الهدف وحدود الأمان

الهدف هو تجهيز Staging بصورة واقعية من بيانات Farida الحالية قبل تطبيق L3 الخاص بفصل سجل الترحيلات. تظل Farida مصدر قراءة فقط، ويقتصر أي حذف أو استبدال على Staging بعد وجود نسخة رجوع متحققة.

- لا تنقل مخططات Supabase الداخلية من Farida مثل `auth` و`storage` و`realtime`.
- لا تنقل كلمات مرور أو جلسات أو هويات مستخدمي الإنتاج.
- يبقى مستخدم إدارة Staging وسيلة الدخول إلى البيئة التجريبية.
- تنقل بيانات التطبيق من `public` بالحالة الفعلية، بما فيها المشكلات المعروفة.
- ينقل `lovable_schema_migrations` لأنه موضوع اختبار L3، ولا يعاد تسميته قبل اكتمال الاستعادة والتحقق.
- لا يطبق L3 على Farida أو Alibea قبل نجاح Staging واعتماد المستخدم.

## نسخة Farida الحالية

- المسار: `/backups/farida/manual/farida-before-staging-20260908-1425.dump`
- النوع: PostgreSQL custom dump كامل، دون owner أو ACL.
- الحجم: 2,539,981 بايت.
- عناصر فهرس الاستعادة: 1,046.
- الصلاحية: `0600`، والمالك `deploy:deploy`.
- SHA-256: `efb7cd559ee48d7b592d642793cc723f4e5d951b43ce9a487e7d0896638aa778`.
- اجتاز فحص `pg_restore --list`.

## نسخة رجوع Staging قبل النقل

المجلد `/backups/staging/before-farida-copy-20260908-1435` بصلاحية `0700`، وملفاته بصلاحية `0600`:

| الملف | الحجم | SHA-256 |
|---|---:|---|
| `schema.sql` | 414,159 بايت | `0eff5e88407b47ba0fe0bcd14dff1e08771345a8d38662e77661420684121a59` |
| `data.sql` | 136,142 بايت | `e79c786a4525220bceec750ad8ba4a13f10dbb1092b811bdb5955a0e188747b3` |
| `roles.sql` | 370 بايت | `168a95a9c745af5ed4679751f90419ac9dc434240a213b03e32a06d5664c2308` |

أعيد إنشاء `SHA256SUMS` بأسماء ملفات نسبية بعد اكتشاف أنه كان يشير إلى مجلد العمل المؤقت. اجتازت الملفات الثلاثة `sha256sum -c` بعد التصحيح، ولم يتغير محتوى ملفات النسخة.

ظهر عند تفريغ البيانات تحذير علاقات دائرية في `accounts` و`product_categories`. النسخة صالحة، لكن استعادة data-only فوق قيود مفعلة قد تفشل؛ لذلك يجب أن تستخدم الاستعادة ترتيباً مضبوطاً أو تعطيل المشغلات مؤقتاً داخل Staging فقط.

## مقارنة المخططين

- Farida يعمل على PostgreSQL 15، وStaging البعيد على PostgreSQL 17.
- Farida يحتوي 39 جدول بيانات في `public`، وStaging يحتوي 38.
- الجدول الوحيد الموجود في Farida وغير الموجود في Staging هو `lovable_schema_migrations`.
- أسماء دوال `public` متطابقة: 76 في كل بيئة.
- أسماء المشغلات متطابقة: 37 في كل بيئة.
- تسلسلات أرقام القيود وفواتير البيع والشراء موجودة في البيئتين. اختلاف موضع `DEFAULT nextval(...)` في التفريغ ليس اختلافاً وظيفياً.
- لا توجد بيانات تخالف قيود أنواع الحسابات أو إيجابية مبالغ التخصيص الموجودة في Staging.
- أعمدة مبالغ القيود والسطور `numeric` في Farida و`numeric(15,2)` في Staging. لا توجد قيمة تتجاوز السعة.

## خط أساس أعداد صفوف Farida

```text
accounts=37                         audit_log=10934
company_settings=1                  customer_payment_allocations=90
customer_payments=91                customers=6
expense_types=8                     expenses=24
inventory_adjustment_items=77       inventory_adjustments=2
inventory_movements=1354            journal_entries=308
journal_entry_lines=821             lovable_schema_migrations=96
loyalty_transactions=0              product_brands=195
product_categories=56               product_images=193
product_units=4                     products=613
profiles=2                          purchase_invoice_items=602
purchase_invoice_return_settlements=4  purchase_invoices=30
purchase_return_items=19            purchase_return_payment_allocations=1
purchase_returns=5                  sales_invoice_items=747
sales_invoice_return_settlements=10 sales_invoices=97
sales_return_items=16               sales_return_payment_allocations=1
sales_returns=10                    supplier_payment_allocations=39
supplier_payments=40                suppliers=18
telegram_post_log=19                telegram_settings=1
user_roles=2                        auth.users=2
```

## نتائج سلامة البيانات قبل النقل

- لا توجد رؤوس قيود يكون فيها إجمالي المدين مختلفاً عن إجمالي الدائن.
- القيد 152 بتاريخ 2026-08-06 يحتوي مبالغ بأكثر من منزلتين عشريتين: الإجمالي `954.1090909090909`، وسطر مدين وسطر دائن بقيمة `414.1090909090909`. التقريب إلى `954.11` و`414.11` متماثل ويبقي القيد متوازناً، لكنه يعني أن تحميله مباشرة إلى أعمدة Staging الحالية لن يكون نسخة رقمية حرفية.
- القيد `jentry_balanced` على `journal_entries` معرف بحالة `NOT VALID` في Farida.
- القيود المرحلة 114 و118 و137 متوازنة في الرأس، لكن كل واحد منها بلا أي سطر. إجمالياتها على الترتيب 200 و525 و5,000. هذه مشكلة موجودة في إنتاج Farida قبل النسخ وليست ناتجة عن Staging.
- ترتبط القيود الثلاثة تاريخياً بسندات مصروف، لكن سجلات المصروف الحالية التي تحمل الأرقام نفسها لا تطابق اثنين منها؛ لا يجوز إصلاحها تلقائياً أثناء النقل.
- العلاقات الصريحة من بيانات التطبيق إلى المستخدم محصورة في `loyalty_transactions.created_by -> auth.users` و`telegram_post_log.created_by -> profiles`. يجب الحفاظ على مستخدم Staging وعدم استيراد `auth.users` من الإنتاج.

## قرار النقل

يجب أن تكون Staging صورة واقعية وتحافظ على المشكلات المكتشفة كي تستخدم في الاختبار، مع عدم نسخ هويات الإنتاج. لا تستخدم استعادة كاملة للنسخة؛ تُنشأ حزمة `public` مخصصة، ويحدد فيها صراحة التعامل مع `profiles` و`user_roles` ومراجع المستخدم وتسلسلات الأرقام والكسور الزائدة.

## حزمة النقل الجاهزة

- المسار: `/backups/farida/staging-transfer-20260908-1425` بصلاحية `0700`، وجميع الملفات بصلاحية `0600`.
- المصدر هو نسخة Farida المجمدة ذات البصمة الموثقة، وليس اتصالاً جديداً بقاعدة الإنتاج.
- `public-data.sql`: حجمه 14,063,691 بايت، وبصمته `a423137087470dae9a6db631465826143073abd6ac92a27549635ab45cfd30df`.
- `lovable-schema.sql`: حجمه 1,083 بايت، وبصمته `806e6086c470cc1e55229cd01bee2350d3c93070b7e2334a0b2e0b363922bcda`.
- `RESTORE_TABLES.txt`: يسجل 35 جدول بيانات ستدخل الحزمة.
- استُبعدت صراحة جداول `profiles` و`user_roles` و`telegram_post_log` و`telegram_settings`.
- لا تحتوي الحزمة بيانات `auth` أو `storage` أو `realtime`.
- يحتوي ملف البيانات 35 أمر `COPY` و10 أوامر `SELECT` و9 أوامر `SET` فقط، ويشمل بيانات `lovable_schema_migrations`.
- يحتوي ملف المخطط الصغير تعريف `lovable_schema_migrations` ومفتاحه الأساسي فقط.
- تحفظ ملفات TOC والقائمة و`PACKAGE_MANIFEST.txt` داخل الحزمة لإعادة التدقيق.
- اجتازت جميع ملفات الحزمة فحص `SHA256SUMS`.
- أنشئت الحزمة باستخدام حاوية PostgreSQL مؤقتة من النسخة الاحتياطية فقط؛ لم يحدث اتصال أو تعديل على Staging.

## حزمة التنفيذ ونتائج التجربة المعزولة

- ملف التقديم: `/backups/farida/staging-transfer-20260908-1425/forward-execute.sql`، حجمه 1,411,254 بايت، وبصمته `36b5ada9b0a3ca656bf7eeba5b15d93f8bb4640a0e694573785a601fc414a6a8`.
- بيانات التقديم دون سجل التدقيق: `/backups/farida/staging-transfer-20260908-1425/forward-data-inserts-no-audit.sql`، حجمها 1,386,142 بايت، وبصمتها `318576e3b451d4bad5b949ab758545e81785ecde55f943fd4b22dc06e87adc1f`.
- ملف الرجوع: `/backups/staging/before-farida-copy-20260908-1435/rollback-execute.sql`، حجمه 148,452 بايت، وبصمته `370cf4a41f42a7faed478a7dc30e18f30422fd411fd09cf8a91192957f688ff6`.
- بيانات الرجوع: `/backups/staging/before-farida-copy-20260908-1435/rollback-data-inserts.sql`، حجمها 125,097 بايت، وبصمتها `b94f938bee9275bbc664a53894da32a4187c0761793989b80935b43cbd61d045`.
- استبعد التنفيذ `audit_log` من النقل ويفرغه على Staging؛ فالجدول يحتوي 10,934 سجلاً تشغيلياً من الإنتاج وليس مطلوباً لاختبار بيانات الأعمال. تبقى نسخته كاملة في نسخة Farida الأصلية.
- يفرغ التنفيذ `telegram_post_log` ولا ينقل إعدادات Telegram أو سجلاته من الإنتاج.
- يحافظ التنفيذ على مستخدمي Staging الثلاثة، ويحوّل `journal_entries.created_by` إلى `NULL` عندما يشير إلى مستخدم إنتاج غير موجود في `auth.users` التجريبية. لا تتغير بيانات القيد المالية.
- غُيّرت أعمدة مبالغ القيود في Staging التجريبية إلى `numeric` غير مقيد أثناء التقديم كي تحفظ دقة Farida حرفياً، ويعيد ملف الرجوع تعريفها إلى `numeric(15,2)`.
- اختبر ملف التقديم داخل PostgreSQL معزول بلا شبكة ومن نسخة Farida المجمدة فقط. نجح وأعاد: `products=613` و`sales_invoices=97` و`journal_entries=308` و`journal_entry_lines=821` و`lovable_schema_migrations=96`، مع بقاء `auth.users=3` و`profiles=3` و`user_roles=3`.
- اختبر ملف الرجوع في القاعدة المعزولة نفسها. عادت القيم إلى `products=2` و`sales_invoices=5` و`auth.users=3` و`profiles=3` و`user_roles=3`، واختفى `lovable_schema_migrations`، وعاد نوع المبلغ إلى `numeric(15,2)`، وعادت المفاتيح الأجنبية الـ60 والمشغلات الـ37.
- احتاج تحميل نسخة Staging 17 داخل حاوية الاختبار PostgreSQL 15 إلى تجاهل تعليمة `transaction_timeout` غير المدعومة ومتابعة تعريف فهرس `gin_trgm_ops` بعد توفر الامتداد. هذا توافق خاص بالحاوية المؤقتة، ولم يغير ملفات النسخ الأصلية ولا خطة التنفيذ على Staging 17.

## اختبار قناة التنفيذ على Staging

- تحقق الربط محلياً من أن project ref هو `dunzfxurefzlaamgghys` قبل الاختبار.
- نُفذ ملف صغير عبر `supabase@2.116.0 db query --linked --file`، وأنشأ جدول فحص داخل معاملة ثم نفذ `ROLLBACK`.
- أعاد الاستعلام داخل الملف `rollback_left_no_table=true`.
- أكد استعلام مستقل بعد انتهاء الاتصال أن جدول الفحص غير موجود: `probe_table_absent=true`.
- بقي خط الأساس دون تغيير: `products=2` و`sales_invoices=5` و`auth.users=3` و`profiles=3` و`user_roles=3`.
- حُذفت ملفات SQL المؤقتة بعد نجاح التحقق، ولم تُنفذ حزمة بيانات Farida في هذه الخطوة.

## تنفيذ الاستعادة الفعلية على Staging

- نُفذ الملف المتحقق `forward-execute.sql` عبر `supabase@2.116.0 db query --linked --file` بعد اعتماد المستخدم الصريح.
- اقتصر التنفيذ على مشروع Staging ذي المرجع `dunzfxurefzlaamgghys`؛ لم تُنفذ أوامر على Farida أو Alibea الإنتاجيتين.
- انتهت المعاملة دون خطأ، ثم نُفذ استعلام تحقق مستقل في اتصال جديد.
- تحقق الاستعلام من: `products=613` و`sales_invoices=97` و`sales_invoice_items=747` و`journal_entries=308` و`journal_entry_lines=821` و`lovable_schema_migrations=96`.
- بقيت هوية Staging: `auth.users=3` و`profiles=3` و`user_roles=3`، ولا توجد قيمة `journal_entries.created_by` معلقة.
- أصبح `audit_log=0` و`telegram_post_log=0` وفق قرار العزل، وعادت المفاتيح الأجنبية الـ60 والمشغلات الـ37.
- لا توجد رؤوس قيود غير متوازنة، وبقيت القيود الثلاثة المعروفة بلا سطور كما كانت في مصدر Farida.
- أكد المستخدم بعد تسجيل الدخول أن بيانات Farida ظهرت في واجهة Staging.

## اختبار قبول الصلاحيات

- فتح حساب المدير صفحة `/users` وظهرت إدارة المستخدمين والمستخدمون الثلاثة.
- مُنع حساب البائع من `/users` بالاستجابة `403`.
- فتح حساب البائع صفحة `/sales` وظهرت بيانات فواتير Farida.
- أظهرت الفاتورة المرحلة للبائع زر «طباعة» فقط، دون تعديل أو إعادة مسودة أو إلغاء.
- مُنع حساب البائع من `/reports/sales` كما هو متوقع.
- مُنع حساب البائع من `/expenses` بالاستجابة `403`، ولم يظهر نموذج إضافة مصروف.
- النتيجة: اجتاز فصل صلاحيات المدير والبائع اختبارات القبول المحددة.

## اختبار قبول المخزون ومعالجة التقريب

- ظهر 613 منتجاً و1,354 حركة مخزون، وهما مطابقان لقاعدة البيانات المنقولة.
- بتاريخ 2026-09-08 ظهر تقييم المخزون: كمية 3,880، و489 صنفاً ذا رصيد، وقيمة حركات `483,717.12` مقابل رصيد 1104 المدوّر `483,717.14`.
- جميع كميات المنتجات تطابق مجموع الحركات؛ لا توجد حالة اختلاف كمية.
- حُصر فرق السنتين في فاتورتي الشراء 31 و38: مجموع `net_total` وحركات كل فاتورة أقل بسنت واحد من صافي الرأس وقيد 1104.
- السبب هو توزيع الخصم العام نسبياً ثم تقريب كل بند مستقلاً دون تحميل باقي التقريب. لا توجد قيود 1104 غير مرتبطة بمستند.
- عُدلت `distributeNetTotals` محلياً لتسوية باقي التقريب على أكبر بند بطريقة حتمية، بحيث يساوي مجموع البنود الصافي المحسوب دائماً.
- أضيف اختبار يحاكي أرقام فاتورة الشراء 31، ونجح فحص TypeScript وجميع الاختبارات: 50 ملفاً و567 اختباراً.
- حُفظ الإصلاح في الالتزام `774a4bc`، ونجحت جميع الاختبارات ثم بُني باستخدام Node `22.23.2` وnpm `10.9.8`.
- نُشر البناء `assets/index-Dk1thnfw.js` على Staging فقط بعد حفظ نسخة الرجوع `/backups/staging/frontend-before-rounding-20260908-2055`؛ بقيت واجهتا Farida وAlibea دون تغيير.
- أنشئت فاتورة الشراء التجريبية الداخلية #45 بثلاثة بنود قيمة كل منها 1.00 وخصم عام 1.00. وزع الإصلاح الصافي `0.66 + 0.67 + 0.67 = 2.00` دون تغيير ترتيب البنود.
- بعد ترحيلها بالرقم `PUR-0031` تطابق صافي الرأس والبنود وحركات المخزون وقيد 1104 عند `2.00`، وكان فرق الحركة عن القيد وفرق المدين عن الدائن `0.00`.
- اعتُمد الإصلاح على Staging قبل نقله تدريجياً إلى الإنتاج، ولم تُعدّل الفاتورتان التاريخيتان 31 و38.

## نشر إصلاح التقريب على الإنتاج

- نُشر الالتزام `774a4bc` على Farida أولاً بواجهة فقط، مع تجاوز migrations وEdge Functions.
- نسخة رجوع Farida: `/backups/farida/frontend-before-rounding-20260908-2105`، والبناء المنشور `assets/index-CcCL41rl.js`.
- أعادت صفحة Farida ومسارا الدخول والمشتريات HTTP 200، وأكد المستخدم عمل صفحة المشتريات ونموذج الفاتورة الجديدة بعد `Ctrl+F5` دون حفظ بيانات اختبار.
- بعد اعتماد Farida نُشر الالتزام نفسه على Alibea بواجهة فقط، مع تجاوز migrations وEdge Functions.
- انقطعت محاولة Alibea الأولى أثناء البناء وقبل نسخ أي ملفات؛ أثبت فحص الاستعادة أن الواجهة بقيت مطابقة لنسخة الرجوع، ثم أُعيد النشر واكتمل بنجاح.
- نسخة رجوع Alibea: `/backups/alibea/frontend-before-rounding-20260908-2120` بصلاحية `0700`، والبناء المنشور `assets/index-BPnrRyEo.js`.
- أعادت صفحة Alibea ومسارا الدخول والمشتريات HTTP 200، وأكد المستخدم عمل صفحة المشتريات ونموذج الفاتورة الجديدة بعد `Ctrl+F5` دون حفظ بيانات اختبار.
- تحققت كل عملية من بقاء الواجهتين الأخريين دون تغيير. لم تتغير أي قاعدة بيانات إنتاجية أو دالة طرفية بسبب النشر.
- الفاتورتان التاريخيتان 31 و38 بقيتا دون تعديل؛ يمنع الإصلاح تكرار فرق التقريب في المستندات الجديدة.

## بدء L3-A وخط الأساس الحديث — 2026-09-09

- نُفذ فحص قراءة فقط على مشروع Staging المرتبط `dunzfxurefzlaamgghys`؛ لم تتغير قاعدة أو واجهة.
- `public.lovable_schema_migrations` ما زال جدولاً فعلياً، وعدد صفوفه `96`، وبصمة محتواه المرتبة `634e981f2f7363bcdc1b546a1e9c37c0`.
- `public.app_schema_migrations` غير موجود بعد، وهو الوضع المتوقع قبل تطبيق L3.
- خط أعداد بيانات الأعمال: `products=613`، و`customers=6`، و`suppliers=18`، و`sales_invoices=98`، و`sales_invoice_items=748`، و`purchase_invoices=31`، و`purchase_invoice_items=605`، و`sales_returns=10`، و`purchase_returns=5`، و`inventory_movements=1358`، و`journal_entries=310`، و`journal_entry_lines=827`.
- الخط المالي: `posted_sales=94` بإجمالي `196,801.00`، و`posted_purchases=31` بإجمالي `626,171.00`، وإجمالي كميات الحركات `6,138.000`. تكلفة الحركات للعرض بمنزلتين `775,736.17`؛ مجموعها الدقيق من النسخة `775736.1690909090909`، ويجب استخدام الأخير للمطابقة الدقيقة لا الرقم المدوّر.
- لا توجد رؤوس قيود غير متوازنة. بقيت القيود الثلاثة المرحلة بلا سطور كما في عيب المصدر المعروف والموثق سابقاً، ولم ينشئ فحص L3 أثراً جديداً.

## لقطة Staging الحديثة قبل L3

- المسار: `/backups/staging/l3-before-tracking-20260909-093540` بصلاحية `0700`، وجميع الملفات بصلاحية `0600`.
- `public-schema.sql`: الحجم `316,364` بايت، وSHA-256 هو `6ddcae21151b94852b8be20380492a5f59e62e11760bde42e06ede045c96817a`.
- `public-data.sql`: الحجم `1,249,266` بايت، وSHA-256 هو `5db02fcde72325d77576d897057ac0f06f643e7bc160d6ac2c19641126708d6d`.
- يحتوي `SHA256SUMS` الاسمين النسبيين، ونجح `sha256sum -c` للملفين.
- النسخة مخصصة لمخطط وبيانات `public` التي يقع فيها سجل الترحيلات وبيانات الأعمال. لا تشمل مخططات Supabase المدارة مثل `auth` و`storage`، لأن L3 لا يغيرها.
- تحقق وجود تعريفات الجداول في ملف المخطط وأمر `COPY` واحد لكل من `products` وسجل الترحيلات القديم في ملف البيانات.
- أظهر تفريغ البيانات تحذيري العلاقات الدائرية المعروفين في `accounts` و`product_categories`. الملفات سليمة، لكن لا تعتبر خطة الرجوع معتمدة قبل نجاح تجربة استعادة معزولة بترتيب مضبوط للمشغلات والقيود.
- استُخدم وضع `db dump --dry-run` مرة في الفحص الأولي، واتضح أنه يعرض اعتماد اتصال CLI مؤقتاً في المخرجات؛ لم تُحفظ القيمة في Git أو ملفات النسخة، ويُمنع استخدام هذا الوضع مجدداً في سجل ظاهر.

## الخطوة التالية

### فحص ما قبل تجربة الاستعادة — L3-A.3

- أُعيد حساب SHA-256 لملفي النسخة، وطابق القيم الموثقة أعلاه.
- قُرئت كتل COPY محلياً وحُسبت أعداد صفوفها دون طباعة بيانات العملاء أو المستخدمين. طابقت نتائج استعلام Staging السابق: 6 عملاء، 18 مورداً، 31 فاتورة شراء، 605 بنود شراء، 10 مرتجعات بيع، 5 مرتجعات شراء، 1358 حركة، و827 سطر قيد.
- أُعيد حساب بصمة سجل الترحيلات من النسخة: MD5 لتجميع الحقول `version` و`filename` و`checksum` بفاصل chr(31)، وتجميع الصفوف المرتبة حسب `version` بفاصل chr(30). النتيجة `634e981f2f7363bcdc1b546a1e9c37c0` مطابقة للاستعلام السابق.
- صُححت أخطاء نقل الأعداد والبصمة في توثيق L3-A السابق؛ لم تتغير النسخة أو البيانات بسبب هذا التصحيح.
- لم يبدأ اختبار الاستعادة: يتطلب Docker صلاحيات إضافية، وأعاد `sudo -n` رسالة تتطلب كلمة مرور. لا توجد أدوات PostgreSQL محلية متاحة عبر PATH.
- أكد إخراج المستخدم وجود الصورة المحلية `public.ecr.aws/supabase/postgres:17.6.1.166`، إلى جانب صور PostgreSQL 15. الخطوة التحضيرية التالية هي قراءة `Entrypoint` و`Cmd` و`User` للصورة 17 قبل تجهيز الحاوية المعزولة. محاولة هذا الفحص آلياً بـ`sudo -n` طلبت كلمة مرور ولم تنفذ الفحص؛ يلزم تنفيذه من جلسة المستخدم. لم تُنشأ حاوية ولم تبدأ استعادة أو تتغير بيئة إنتاجية.
- أرسل المستخدم نتيجة فحص إعداد الصورة: `Entrypoint=["docker-entrypoint.sh"]` و`Cmd=["postgres","-D","/etc/postgresql"]` و`User=""`. لذلك سيُتجاوز إعداد التشغيل المخصص، ويُهيأ عنقود مؤقت كمستخدم `postgres` داخل `/tmp` بالحاوية فقط؛ دون شبكة أو منافذ منشورة أو ربط مجلدات المضيف، وبحدود للذاكرة والمعالج. هذه خطة تشغيل وليست نتيجة اختبار: لم يُؤكد إنشاء الحاوية أو نجاح تهيئتها بعد.
- نجاح البصمات وعدّ COPY لا يثبت قابلية الاستعادة؛ تبقى L3-A.3 غير مكتملة إلى أن تُستعاد النسخة وتُفحص داخل PostgreSQL.

### إنشاء حاوية تجربة الاستعادة

- أعاد أمر المستخدم لإنشاء `accounting-l3-restore-20260909` المعرّف `854444943e9790aab117a5e4639e8b23e448441673d45340b259abd9ba532a22`.
- الأمر المحدد يستخدم الصورة المحلية 17 دون سحب، وبلا شبكة أو مجلدات مضيف مربوطة، وجذر للقراءة فقط، وبيانات مؤقتة في `/tmp`، وحد ذاكرة 512MiB دون swap إضافي، ونصف معالج. لم تُرسل بيانات النسخة إلى الحاوية بعد.
- ظهور معرّف الحاوية لا يثبت استمرار تشغيلها أو نجاح `initdb`. الخطوة التالية فحص اتصال PostgreSQL عبر مقبس `/tmp` داخل الحاوية؛ لم تُعتمد جاهزيتها أو نجاح الاستعادة بعد.

### جاهزية PostgreSQL وأداة الاستعادة — L3-A.3

- أكد ناتج المستخدم `READY|17.6|` نجاح الاتصال عبر مقبس `/tmp`، وإصدار PostgreSQL 17.6، وخلو `listen_addresses`. لم يُنفذ اختبار استعادة البيانات بعد هذا الفحص.
- جُهزت الأداة `scripts/tests/rehearse-public-restore.mjs` واختباراتها المجاورة. الأداة مخصصة لهذه اللقطة ومعرّف الحاوية أعلاه فقط، وليست أداة استعادة للإنتاج.
- يفحص `--check` البصمتين، وتطابق جداول المخطط وكتل COPY، وتسلسلات الأرقام وبصمة سجل الترحيلات دون استخدام Docker. النتيجة الفعلية: `SNAPSHOT_CHECK_OK tables=39 sequences=9 tracking_rows=96`.
- نجحت اختبارات Node الخمسة للمحلل وبصمة الصفوف وحاجز العزل وتوليد فحوص SQL، ونجح فحص صيغة JavaScript و`git diff --check` وحاجز سلامة المستودع. هذه ليست اختبارات تنفيذ SQL داخل PostgreSQL.
- يفحص `--restore` هوية الحاوية وحالتها والعزل وحدود الموارد قبل إنشاء قاعدة جديدة باسم `l3_public_restore`؛ يرفض الكتابة إذا كانت موجودة. يستعيد مخطط وبيانات `public` ضمن معاملة، دون إعادة تشغيل migrations أو الاتصال ببيئة فعلية.
- لأن النسخة لا تشمل `auth`، يُنشأ داخل قاعدة الاختبار فقط جدول `auth.users(id)` بمعرّفات المراجع الموجودة في النسخة، ودالتا `auth.uid()` و`auth.role()` تعيدان NULL. هذه تعريفات مساندة وليست استعادة لحسابات الدخول أو اختباراً للمصادقة/RLS أو دليلاً على سلامة مستخدمي مصدر النسخة.
- بعد الاستيراد بوضع `replica` يعود الوضع إلى `origin`، وتُعاد إضافة المفاتيح الأجنبية لفرض فحص الصفوف، بدلاً من الاكتفاء بعلامة `convalidated`. يتحقق الاختبار من 60 علاقة و37 مشغلاً و9 تسلسلات والمجاميع المرجعية، ويبقي عيب القيود الثلاثة المعروفة بلا سطور دون إصلاح ضمني.
- بعد COMMIT تُقرأ كل الجداول من جديد بصيغة COPY وتُقارن بصمات جميع الصفوف والأعمدة بالنسخة مع تجاهل ترتيب الصفوف فقط. لا تُطبع بيانات الصفوف أو معرّفات المستخدمين؛ تحفظ نتيجة التنفيذ أو أخطاؤه في مجلد خاص داخل `/tmp` بصلاحية `0700` وملفات `0600`.
- الأمر التالي المقترح للمستخدم: `sudo /usr/bin/node /opt/accounting-app/scripts/tests/rehearse-public-restore.mjs --restore`. لم يُنفذ بعد؛ تبقى L3-A.3 مفتوحة حتى ظهور نتيجة الاستعادة وفحصها. تبقى الحاوية للاختبار المعزول التالي، ولا تُحذف تلقائياً عند الخطأ.

### محاولة الاستعادة الأولى — لم تنجح

- نفذ المستخدم الأداة وأرسل `ISOLATION_OK` ثم `Docker/PostgreSQL step failed`، مع مسار التشخيص `/tmp/accounting-l3-restore-report-67VA3g/run.log`.
- وصلت الأداة إلى خطوة استعادة SQL بعد إنشاء قاعدة الاختبار وفحص العزل. لم يظهر `RESTORE_OK`، ولم تُعتمد الاستعادة أو تبدأ مرحلة فصل سجل الترحيلات.
- تعذرت قراءة السجل من جلسة المساعد لصلاحيات المجلد، وطلب `sudo -n` كلمة مرور. السبب التفصيلي غير معروف بعد؛ الخطوة التالية قراءة أسطر ERROR/FATAL فقط من جلسة المستخدم، دون مشاركة ملف السجل كاملاً أو صفوف النسخة.
- لا يُعاد تشغيل `--restore` مباشرة؛ الأداة ترفض استبدال قاعدة الاختبار الموجودة. يلزم تشخيص الفشل والتحقق من حالتها قبل أي قرار إعادة محاولة. يبقى نطاق العمل الحاوية المعزولة فقط.

### تشخيص المحاولة الأولى وتجهيز إعادة المحاولة

- أرسل المستخدم الخطأ: `operator class "public.gin_trgm_ops" does not exist for access method "gin"` عند تنفيذ مخطط النسخة. أظهر فحص ملف المخطط سبعة فهارس تعتمد هذا الصنف، دون أمر `CREATE EXTENSION` في النسخة. الخلل في تجهيز متطلبات قاعدة الاختبار، ولا يثبت تلف النسخة أو مشكلة في الإنتاج.
- أُضيف `CREATE EXTENSION pg_trgm WITH SCHEMA public` إلى تهيئة قاعدة الاختبار قبل استيراد المخطط، داخل معاملة الاستعادة نفسها. لم تتغير ملفات النسخة أو جداول الإنتاج أو Staging.
- أُضيف وضع صريح `--resume-empty` لاستخدام قاعدة الاختبار الموجودة دون حذفها. قبل أي إنشاء، يتحقق SQL من اسمها ومالكها وخلو `public` من العلاقات والدوال والأنواع، وعدم وجود مخطط `auth` أو أدوار التهيئة أو إضافات غير `plpgsql` أو اتصال عميل آخر. إذا بقيت آثار للمحاولة السابقة يرفض التنفيذ دون مسحها. يتحقق كذلك من توفر إضافة `pg_trgm` في الصورة المحلية.
- يفحص هذا الحاجز فعلياً فرضية رجوع المعاملة الفاشلة؛ لم يُفترض نجاح الرجوع بناء على رسالة الخطأ وحدها. لا يوجد `DROP DATABASE` أو تفريغ جداول لتسهيل إعادة المحاولة.
- نجحت اختبارات Node السبعة وفحص JavaScript والفحص المحلي للنسخة (`39` جدولاً و`9` تسلسلات و`96` سجل ترحيل). يبقى اختبار SQL الحقيقي وإعادة الاستعادة غير منفذين بعد التعديل.
- الخطوة التالية للمستخدم: `sudo /usr/bin/node /opt/accounting-app/scripts/tests/rehearse-public-restore.mjs --resume-empty`، ثم مراجعة النتيجة قبل الانتقال إلى تجهيز رجوع L3 المحدود.

### محاولة الاستعادة الثانية — بانتظار التشخيص

- أرسل المستخدم `ISOLATION_OK; verifying empty test database before transactional restore...` ثم فشل خطوة Docker/PostgreSQL، مع السجل `/tmp/accounting-l3-restore-report-ssxr6i/run.log`.
- الرسالة تؤكد نجاح فحص هوية الحاوية وعزلها فقط؛ تسبق تنفيذ حاجز خلو القاعدة، فلا تثبت نجاحه أو تثبيت الإضافة أو تحميل المخطط. سبب الفشل الثاني غير معروف حتى قراءة سجل الخطأ.
- لم يظهر `RESTORE_OK`. لا تُعاد المحاولة ولا يتغير الكود بناءً على التخمين؛ الخطوة التالية طلب أسطر ERROR/FATAL من السجل الجديد دون مشاركة كامل السجل أو بيانات النسخة.

### تشخيص المحاولة الثانية وتصحيح شرط القبول

- أرسل المستخدم `ERROR: Foreign key/constraint verification failed` من السجل الثاني. هذا استثناء من فحص الأداة بعد تحميل المخطط والبيانات وإعادة إضافة المفاتيح الأجنبية، وليس رسالة PostgreSQL عن صف ذي مرجع مكسور.
- أثبتت قراءة ملف المخطط المجمد وجود `public.journal_entries.jentry_balanced` كقيد CHECK بحالة `NOT VALID` (السطر 5384)، إلى جانب تعريفات المفاتيح الأجنبية الستين. كان شرط الأداة يخلط فحص العلاقات الأجنبية مع رفض جميع القيود غير المعتمدة، فرفض حالة موجودة في المصدر نفسه.
- صُحح الفحص ليشترط عدد العلاقات الأجنبية الستين واعتمادها جميعاً، وليطابق وجود قيد CHECK غير معتمد واحد فقط بالجدول والاسم والنوع المحددين أعلاه. لا يقبل أي قيد إضافي غير معتمد، ولا ينفذ `VALIDATE` لتغيير حالة قيد المصدر.
- أُضيف فحص مستقل ودقيق دون تقريب لتساوي `total_debit` و`total_credit` لكل رأس قيد. تبقى مقارنة جميع صفوف النسخة، والمجاميع، وحالات المشغلات والتسلسلات كما هي؛ ولم تُعدّل بيانات النسخة أو البيئة الفعلية لتجاوز الفحص.
- أُضيف اختبار انحدار للفصل بين FK وCHECK. نجحت اختبارات Node الثمانية وفحص النسخة والصيغة وسلامة المستودع. لم تُنفذ إعادة المحاولة المعدّلة بعد؛ لا يُعد نجاح الاختبارات المحلية اعتماداً للاستعادة.
- الخطوة التالية: تشغيل `--resume-empty` مجدداً؛ يبقى حاجز العزل والتحقق من خلو قاعدة الاختبار قبل الكتابة إلزامياً، ثم مراجعة نتيجة الاستعادة الفعلية.

### محاولة الاستعادة الثالثة — بانتظار التشخيص

- نفذ المستخدم `--resume-empty` بعد تصحيح شرط القيود، وظهرت رسالة نجاح فحص العزل ثم فشل خطوة Docker/PostgreSQL. السجل الجديد: `/tmp/accounting-l3-restore-report-zUVtdg/run.log`.
- لا يتضمن الناتج سبب الخطأ أو إثبات اجتياز الفحوص التالية للعزل. لم يظهر `RESTORE_OK`، ولا يُفترض نجاح معالجة الفشل السابق أو رجوع المعاملة دون تحقق.
- الخطوة التالية قراءة أسطر ERROR/FATAL من هذا السجل فقط قبل أي تعديل أو إعادة محاولة. لا تزال L3-A.3 مفتوحة، ولا ينتقل العمل إلى تغيير سجل الترحيلات.

### تشخيص المحاولة الثالثة — دقة المرجع المالي

- أرسل المستخدم `ERROR: Financial baseline mismatch` من سجل المحاولة الثالثة. الأداة كانت تقارن مجموع `inventory_movements.total_cost` الدقيق بالرقم المرجعي المدوّر للعرض `775736.17`، ولذلك رفضت المقارنة. هذا خطأ في إعداد مرجع الاختبار، وليس دليلاً على فقد أو تغيير أموال أثناء الاستعادة.
- أُعيد جمع الحقول من كتل COPY للنسخة المجمدة بحساب عشري باستخدام BigInt دون أعداد عائمة. النتائج: 94 فاتورة بيع مرحلة بمجموع `196801`، و31 فاتورة شراء مرحلة بمجموع `626171`، ومجموع كميات الحركات `6138`، ومجموع تكلفة الحركات `775736.1690909090909`. القيم المرجعية الأخرى في الشرط السابق مطابقة، والاختلاف هو تقريب تكلفة الحركات فقط.
- أصبحت الأداة تستخرج جميع المجاميع المالية المرجعية مباشرة من النسخة التي تتحقق بصمتها، وتقارنها في PostgreSQL كأعداد `numeric` بدقتها الكاملة. لا تستخدم سماحية أو تقريباً لتجاوز الاختلاف، وتبقى مقارنة كل صف وكل عمود بالنسخة بعد COMMIT إلزامية.
- أصبحت رسائل الفشل تفصل اسم المؤشر وقيمتي المتوقع والفعلي، ويسجل تقرير النجاح المجاميع الدقيقة. لا تُعرض بيانات مستندات أو بيانات عملاء ضمن هذه الرسائل.
- نجح فحص النسخة وظهرت المجاميع الدقيقة أعلاه، ونجحت اختبارات Node الإحدى عشرة، بما فيها الكسور السالبة والصفر والقيم الأكبر من دقة Number وفلترة المستندات المرحلة. نجح فحص الصيغة وحاجز المستودع و`git diff --check`. لم تُنفذ محاولة SQL جديدة بعد هذا التعديل.
- الخطوة التالية: تشغيل `--resume-empty` مع جميع حواجز العزل وخلو القاعدة كما هي، ثم مراجعة نتيجة الاستعادة. لم تتغير أي بيانات على الإنتاج أو مشروع Staging الفعلي.

### المحاولة الرابعة — نجاح الاستعادة المعزولة

- أرسل المستخدم نجاح التنفيذ باستخدام `--resume-empty`: `RESTORE_OK tables=39 foreign_keys=60 triggers=37 sequences=9 tracking_rows=96`.
- أكد التنفيذ `ALL_COPY_ROWS_MATCH` بعد COMMIT: تطابقت جميع الصفوف والأعمدة مع النسخة المجمدة، مع تجاهل ترتيب الصفوف فقط. وأكد `FINANCIAL_BASELINE_OK` بالمجاميع الدقيقة المستخرجة من النسخة.
- أُعيد فحص العلاقات الأجنبية الستين وحالات المشغلات والتسلسلات. بقي قيد CHECK `jentry_balanced` بحالة `NOT VALID` كما في المصدر، مع نجاح فحص التوازن الدقيق للرؤوس. بقيت القيود المرحلة الثلاثة المعروفة بلا سطور؛ لم تُصلح تلقائياً.
- التقرير الناتج: `/tmp/accounting-l3-restore-report-Es8ZAl/report.json`. الاعتماد هنا يستند إلى ناتج الأداة الذي أرسله المستخدم، وليس إلى قراءة مستقلة لهذا الملف من المساعد. يُحفظ التقرير لاحقاً بجوار النسخة باسم `restore-report.json`؛ لم تُؤكد هذه النسخة الدائمة بعد.
- الحاوية `accounting-l3-restore-20260909` وقاعدتها `l3_public_restore` باقية لاختبار الرجوع المحدود. لا تُعد تشغيل `--restore` أو `--resume-empty` على قاعدة الاختبار الناجحة، ولا تُفرغها؛ العمل التالي يستخدم البيانات المستعادة كما هي.
- اكتملت L3-A.3. لا تزال L3-A ككل مفتوحة لتجهيز واختبار رجوع تغيير سجل الترحيلات والفحص الجاف. لم تُطبق migration سجل الترحيلات على الحاوية أو Staging أو الإنتاج في هذه الخطوة.
- حدود النتيجة: اختبار استعادة `public` مع تعريفات `auth` مساندة فقط؛ ليس اختبار دخول أو RLS أو استعادة كاملة لمخططات Supabase المدارة.

### حفظ تقرير النجاح وتجهيز اختبار تغيير السجل والرجوع

- أكد المستخدم حفظ التقرير. قرأه المساعد من `/backups/staging/l3-before-tracking-20260909-093540/restore-report.json`، وتحقق من نتيجته وهوية الحاوية والمجاميع وأعلام المطابقة. الحجم `1275` بايت والصلاحيات `0600` والمالك `deploy:deploy`؛ SHA-256: `1a4ab50d21ec7ea7900e7ceeea09d48b635afe6e93d6a3b52084dd054ad6d4ef`.
- جُهزت `scripts/tests/rehearse-tracking-transition.mjs` واختباراتها. تشترك في حاجز الحاوية والنسخة مع أداة الاستعادة، ولا تنشئ قاعدة جديدة ولا تعيد استيراد البيانات. تستخدم ملف انتقال واحداً فقط ببصمة `9a6eb211ec127cb8b125d08f71c2aafe4fcd93f721460cd0f0329fa0d9e2ed70`، دون تعديله أو تشغيل بقية migrations.
- تتحقق قبل أي تغيير من التقرير المحفوظ وجميع صفوف الجداول الـ39 وحالة السجل القديم، وتلتقط مخطط `public` ومالك جدول السجل وصلاحياته ومفتاحه الأساسي.
- يولّد الاختبار ملف رجوع محمياً خاصاً بالحاوية، مقيداً باسم قاعدة الاختبار وOID جدول السجل. يتحقق من View التوافق واعتمادها على الجدول ومحتوى الإصدارات قبل إسقاط View فقط بـ`RESTRICT` وإعادة اسم الجدول والفهرس. لا يوجد حذف جدول أعمال أو بيانات أو `CASCADE`. هذا الملف ليس أمر رجوع جاهزاً لـStaging أو الإنتاج لاختلاف هوية القاعدة.
- المرحلة الأولى المخططة: تقديم التغيير واختبار تكراره وكتابة صف اصطناعي عبر View ضمن SAVEPOINT يُلغى، ثم تنفيذ الرجوع الصريح، وإنهاء المعاملة كلها بـROLLBACK ومطابقة خط الأساس.
- المرحلة الثانية المخططة: تقديم التغيير مع COMMIT، ثم قراءة مستقلة لجميع الصفوف عبر الاسم المحايد واسم التوافق وفحص المالك والصلاحيات. بعد تأكد تنفيذ COMMIT، تحاول الأداة الرجوع المحدود في `finally` حتى إن فشل فحص التقديم. لا يُعتمد الرجوع إلا بعد نجاح COMMIT الخاص به ومطابقة الصفوف ومخطط `public` وبيانات ملكية السجل مستقلاً.
- يحتفظ تقرير النجاح ببصمة المخطط ونتائج التقديم والرجوع ومسار ملف الرجوع. تقارن لقطات `pg_dump` حرفياً مع استثناء رموز `\\restrict` العشوائية فقط، ولا يُطبع محتوى البيانات أو المخطط في الطرفية.
- نُفذ `--check` محلياً ونجح: `TRACKING_PLAN_CHECK_OK`، دون تنفيذ SQL. نجحت اختبارات أداة الاستعادة الإحدى عشرة واختبارات أداة الانتقال الأربعة، وفحص الصيغة وحاجز المستودع. الاختبار الفعلي للتقديم والرجوع لم يُنفذ بعد.
- أمر المستخدم التالي الوحيد: `sudo /usr/bin/node /opt/accounting-app/scripts/tests/rehearse-tracking-transition.mjs --run`. يظل Staging الفعلي والإنتاج خارج النطاق. الفحص الجاف لمسار النشر وقناة تنفيذ Staging الفعلية يبقيان مرحلتين منفصلتين بعد نجاح هذا الاختبار.

### المحاولة الأولى لاختبار انتقال السجل — توقف الفحص بعد التقديم

- أرسل المستخدم `ISOLATION_AND_BASELINE_OK` و`TRANSACTIONAL_PREFLIGHT_OK`: نجح اختبار التقديم والتكرار والرجوع الصريح داخل المعاملة الملغاة، ثم مطابقة خط الأساس.
- توقفت الأداة في `forward-verification` بعد مرحلة التقديم المثبت. سجل الخطأ `/tmp/accounting-l3-tracking-report-g8D4MQ/run.log` وملف الرجوع المقيد `/tmp/accounting-l3-tracking-report-g8D4MQ/rollback-isolated-only.sql`.
- يحاول مسار `finally` في الأداة الرجوع بعد تأكد تثبيت التقديم حتى عند فشل فحصه، لكن لم تظهر نتيجة التحقق المستقل من الحالة النهائية؛ لا نعتمد نجاح الرجوع اعتماداً على وجود هذا المسار في الكود وحده.
- أظهرت المراجعة المحلية أن مطابقة الصفوف تستخدم COPY المباشر أيضاً لاسم التوافق بعد تحوله إلى View؛ هذا موضع محتمل للخطأ ويحتاج تأكيد سجل PostgreSQL قبل نسبة الفشل إليه.
- الخطوة التالية: قراءة أسطر ERROR/FATAL وفحص نوع علاقتي سجل الترحيلات وعدد سجلاته قراءة فقط في قاعدة الاختبار. لا تُعد تشغيل الاختبار ولا تنفذ ملف الرجوع يدوياً قبل تحديد الحالة الراهنة.

### تشخيص فحص View التوافق وتصحيح أداة المطابقة

- أكد سجل PostgreSQL الذي أرسله المستخدم السبب: `cannot copy from view` لاسم سجل التوافق. استُخدمت صيغة COPY المباشر على علاقة أصبحت View، ففشل فحص القراءة بعد التقديم؛ ليس الخطأ من تغيير البيانات في migration.
- أكد الاستعلام المستقل الذي نفذه المستخدم `legacy_kind=r` و`neutral_kind=null` و`history_rows=96`. هذا يؤكد عودة شكل علاقتي السجل وعدد صفوفه بعد الرجوع التلقائي، لكنه لا يغني عن مطابقة جميع الصفوف والمخطط والملكية في دورة الاختبار التالية.
- تغيرت القراءة في أداة انتقال السجل إلى `COPY (SELECT <explicit columns> FROM <relation>) TO STDOUT` لتدعم الجدول وView التوافق بنفس ترتيب الأعمدة والإعدادات. لا توجد تصفية أو حدود صفوف أو DISTINCT، وتبقى مقارنة بصمات جميع الصفوف كما هي.
- أُضيف اختباران لمنع الرجوع إلى COPY المباشر على View، ولرفض الأعمدة الفارغة أو معرّفات SQL غير الصالحة. نجحت اختبارات الانتقال الستة واختبارات الاستعادة الإحدى عشرة، وفحص `--check` دون SQL وفحص الصيغة وحاجز المستودع.
- لم تُنفذ دورة التقديم والرجوع المعدّلة بعد. الأمر التالي هو إعادة تشغيل أداة انتقال السجل بـ`--run`؛ تبدأ بمطابقة جميع بيانات النسخة وحالة السجل قبل أي كتابة، وتبقى مقيدة بالحاوية المعزولة.

### نجاح اختبار التقديم والرجوع المحدود

- أرسل المستخدم من المحاولة الثانية لأداة الانتقال النتائج بالترتيب: `ISOLATION_AND_BASELINE_OK`، ثم `TRANSACTIONAL_PREFLIGHT_OK`، ثم `FORWARD_OK`، ثم `TRACKING_REHEARSAL_OK; FORWARD_AND_ROLLBACK_OK; ALL_ROWS_AND_PUBLIC_SCHEMA_RESTORED`.
- اجتاز الاختبار المعاملة الملغاة (بما فيها التكرار الآمن وكتابة View التجريبية الملغاة)، ثم تقديم التغيير في معاملة مثبتة وقراءة مستقلة، ثم الرجوع الصريح في معاملة مثبتة وقراءة مستقلة.
- وفق ناتج الأداة تطابقت جميع صفوف وأعمدة الجداول الـ39 مع النسخة، وبقيت الإصدارات الـ96 دون تغيير. تطابق مخطط public قبل التجربة وبعدها، وعادت هوية جدول السجل ومفتاحه ومالكه وصلاحياته. لم تُنفذ migrations تاريخية أو تغييرات أعمال ضمن الاختبار.
- التقرير: `/tmp/accounting-l3-tracking-report-SpetC9/report.json`. ملف الرجوع: `/tmp/accounting-l3-tracking-report-SpetC9/rollback-isolated-only.sql`. الاعتماد هنا يستند إلى ناتج التنفيذ الذي أرسله المستخدم؛ قراءة الملفين بعد حفظهما الدائم لم تتم بعد.
- الخطوة التشغيلية التالية حفظ الملفين في `/backups/staging/l3-before-tracking-20260909-093540/tracking-rehearsal/` بمجلد `0700` وملفين `0600` مملوكين لـdeploy، دون نقل ملفات البيانات أو حذف مخرجات الاختبار الأصلية. تحقق المساعد من عدم وجود مجلد الوجهة قبل اقتراح أمر الحفظ.
- اكتمل اختبار الرجوع المحدود في الحاوية، وليس تطبيق L3 على Staging. يظل ملف الرجوع الناتج محصوراً في قاعدة الاختبار وOID الجدول المستعاد؛ يلزم إعداد نظيره المقيد ببيئة Staging وفحص قناة التنفيذ والفحص الجاف قبل L3-B. لا تعدل Farida أو Alibea.

### التحقق من الأرشفة الدائمة

- عاد أمر الحفظ دون إخراج، وهو السلوك الطبيعي لـ`install` عند النجاح. تحقق المساعد مباشرة من وجود مجلد `tracking-rehearsal` وملفيه بجوار النسخة وقرأ التقرير المحفوظ.
- المجلد بصلاحية `0700`، والملفان `0600`، والملكية `deploy:deploy`. حجم `report.json` هو `1172` بايت، وحجم `rollback-isolated-only.sql` هو `1956` بايت.
- SHA-256 للتقرير: `7cbe78de5d6da9f8c469f7483c8000ff5baff91540760862735d56ade04d8f91`، ولملف SQL الكامل: `10461effcc3d9534d2febe4ed4ce6bed52a82fc8a410024db4a1ff5f8efd875c`.
- طابقت بصمة جسم SQL (بعد فصل BEGIN/COMMIT الخارجيين فقط) القيمة المسجلة في تقرير الاختبار: `3b43cc0faff2acd5f48b5628e233f4d65e5d4ae9ebffab5c63ae99aa3b19ceb8`. نتيجة الفحص المحلي `ARCHIVE_VERIFIED`.
- أكد التقرير المحفوظ نجاح التقديم والرجوع مع مطابقة الصفوف والمخطط وبيانات السجل، وعدم تشغيل migrations تاريخية أو تعديل الإنتاج أو Staging الفعلي. اكتملت أرشفة نتيجة الاختبار؛ لا حاجة لإعادة أمر الحفظ.

### الفحص الجاف لسجل الترحيلات

- أضيف فحص قراءة فقط `scripts/tests/audit-migration-history.mjs` يقارن ملفات migrations الحالية بكتلة سجل الترحيلات في نسخة Staging المجمدة المتحققة، ولا يتصل بقاعدة ولا ينفذ SQL.
- النتيجة الفعلية: 97 ملفاً محلياً، و96 إصداراً مسجلاً، وملف واحد فقط غير مسجل هو `20260907091000_neutral_migration_tracking.sql`، وعدد migrations التاريخية التي سيعاد تشغيلها وفق مطابقة الإصدار هو صفر: `historical_replay=0`.
- لا توجد إصدارات مسجلة بلا ملف، ولا اختلاف أسماء لإصدار موجود. توجد 12 بصمة لملفات تاريخية لا تطابق البصمة التي سجلتها Staging. يعاملها الفحص كـ`HISTORICAL_CHECKSUM_DRIFT_WARNING` لا كـmigration جديدة؛ سكربت التشغيل يعتمد رقم الإصدار في `is_applied` ولذلك لن يعيد تنفيذها.
- الملفات ذات اختلاف البصمة: `20260217190521_fba4abbe-a888-46af-a926-06e8966ebf66.sql`، و`20260225211237_94750922-e2f7-4aa8-ac8d-05102e415ac0.sql`، و`20260317210134_53ab8d91-16db-4b55-b7e5-4c0d7ac3b560.sql`، و`20260331131936_c9931fce-fe1e-485b-93cd-4b0d876256ed.sql`، و`20260405172325_c215ad74-1899-4cc6-9048-391d4de1d0cc.sql`، و`20260406203534_508ebdef-37c4-4158-85fd-ef130fb9022a.sql`، و`20260420124655_03ceddcd-ac2c-4e18-92bd-6ce8707d1693.sql`، و`20260525102718_41b925eb-77f8-4b85-9978-c8c960b9e7bf.sql`، و`20260831190000_inventory_reports_catchup.sql`، و`20260905204000_allow_finance_guard_invocation.sql`، و`20260905205000_fix_average_cost_guard_wrapper.sql`، و`20260905213000_sales_product_catalog.sql`.
- لا تُعدّل migrations التاريخية ولا تُغيّر سجلاتها في L3 لمعالجة التحذير؛ سيُفتح لاحقاً تدقيق مستقل لمصدر هذه الفروق وحفظ سلامة migrations الجديدة. بوابة L3 الحالية تعتمد عدم إعادة التشغيل، وقد تحقق ذلك، مع بقاء التحذير موثقاً لا مخفياً.
- نجحت اختبارات الفحص الجاف الأربعة: فصل الملف المعلق، وتمييز اختلاف البصمة عن migration معلقة، وكشف الملف المفقود/الاسم المتغير، ورفض الإصدارات المكررة أو الأسماء غير الصالحة.

### تجهيز رجوع Staging المقيد

- جُهز `/tmp/l3-staging-tracking-rollback.sql`، بحجم 104 أسطر وبصمة SHA-256 `f96ad1a4994c090492e606e603aab5519688dae6c46f9b93e8aa27623fedc001`. لم يُنفذ أو يُحفظ في النسخ الدائمة بعد.
- الملف يرفض التنفيذ ما لم تكن القاعدة والدور المتوقعان، والاسم المحايد جدولاً والاسم القديم View تعتمد عليه، والمالك والمفتاح الأساسي وحالة السجل وتوقيعه مطابقين. كما يشترط خط أعداد الأعمال والمجاميع الدقيقة للّقطة الحالية، حتى لا يرجع تلقائياً بعد حدوث معاملات جديدة تستلزم مراجعة بشرية.
- ينفذ في معاملة SERIALIZABLE بمهلة قفل 5 ثوانٍ ووقت أقصى 30 ثانية، ويقفل جدول السجل فقط. التغيير الوحيد في مسار النجاح هو إسقاط View التوافق بـRESTRICT وإعادة اسم جدول السجل وفهرس مفتاحه؛ لا يحتوي `DROP TABLE` أو `DELETE` أو `TRUNCATE` أو `CASCADE`.
- التعليق داخل الملف يربطه بمشروع Staging ref `dunzfxurefzlaamgghys`، لكن SQL لا يستطيع التحقق من project ref المستضاف بذاته؛ يجب أن يتحقق غلاف التنفيذ من ملف الربط قبل الإرسال. لا يجوز استخدامه مباشرة على Farida أو Alibea.
- حاول المساعد تنفيذ استعلام النظام القراءة فقط على Staging عبر CLI، لكن جلسة المساعد لا تحتوي اعتماد Supabase وأعادت `Access token not provided`. لم يتغير Staging بسبب المحاولة. خط الأساس الحالي مستند إلى الاستعلام السابق والنسخة المجمدة؛ يلزم تجديد تحقق القراءة فقط عبر القناة المرتبطة قبل التطبيق الفعلي.

### حفظ رجوع Staging وتجهيز اختباره

- حفظ المستخدم الملف في `tracking-rehearsal/staging-rollback.sql`. تحقق المساعد من الصلاحية `0600` والمالك `deploy:deploy` والحجم `4483` بايت والبصمة `f96ad1a4994c090492e606e603aab5519688dae6c46f9b93e8aa27623fedc001`، ومن التطابق الحرفي مع الملف المجهز في `/tmp`.
- أضيف وضع `--test-staging-rollback` إلى أداة تجربة الانتقال. يتحقق من بصمة ملف الرجوع المحفوظ والتقرير والـmigration، ومن خط الأساس الكامل في الحاوية، ثم يثبت التقديم ويشغل ملف رجوع Staging نفسه ويطابق جميع الصفوف ومخطط public بعده.
- إذا فشل ملف Staging بعد تثبيت التقديم، يحاول الاختبار في `finally` الرجوع المقيد بالـOID الذي سبق إثباته، حتى لا يترك قاعدة الاختبار في حالة انتقالية. يظل أي فشل مسجلاً ولا يتحول إلى نجاح بسبب رجوع الطوارئ.
- أضيف اختبار بنيوي للملف المحفوظ وحواجزه ومنع أوامر حذف البيانات، ونجحت اختبارات الانتقال السبعة وفحص `--check` وبصمات الملفات وحاجز المستودع. لم يُنفذ وضع الاختبار الجديد داخل PostgreSQL بعد.
- محاولة التشغيل الأولى لم تبدأ بسبب خطأ كتابي في المسار المرسل للمستخدم (`scriptsалам` بدلاً من `scripts`). أعاد Node الخطأ `MODULE_NOT_FOUND` قبل تحميل الأداة؛ لم يتصل Docker ولم تُقرأ أو تُعدّل أي قاعدة. الأمر الصحيح يستخدم `/opt/accounting-app/scripts/tests/rehearse-tracking-transition.mjs`.
- نفذ المستخدم الأمر الصحيح، واجتاز `ISOLATION_AND_BASELINE_OK` ثم فشل في مرحلة `staging-rollback-file`. سجل التشخيص `/tmp/accounting-l3-tracking-report-WFIqOy/run.log`، وملف رجوع الطوارئ المعزول في المجلد نفسه.
- بدأت الأداة تقديم migration داخل الحاوية قبل تشغيل ملف الرجوع، ثم دخلت مسار `finally` المصمم لمحاولة الرجوع المقيد بالـOID عند فشل ملف Staging. لا نعتمد نجاح رجوع الطوارئ دون فحص مستقل لنوعي علاقتي السجل وعدد الصفوف.
- الخطوة التالية قراءة أسطر الخطأ من السجل وفحص الحالة النهائية قراءة فقط. لا يعاد تشغيل الاختبار ولا ينفذ أي ملف رجوع يدوياً قبل معرفة النتيجة.

### تشخيص حارس قاعدة الرجوع

- أرسل المستخدم الخطأ `Unexpected database or role; rollback refused`. أكد فحص الحالة بعدها `legacy_kind=r` و`neutral_kind=null` و`history_rows=96`، أي نجح رجوع الطوارئ وأعاد حالة السجل الأساسية.
- سبب الرفض متوقع من ربط الملف بالبيئة: ملف Staging يشترط `current_database()='postgres'`، بينما قاعدة الاختبار المعزولة اسمها `l3_public_restore`. لم يصل الملف إلى أوامر إسقاط View أو إعادة التسمية؛ الحارس عمل كما صُمم.
- بقي ملف Staging المحفوظ وبصمته دون تعديل. تغيرت أداة الاختبار فقط لتنشئ في الذاكرة نسخة اختبار تستبدل شرط اسم القاعدة مرة واحدة من `postgres` إلى `l3_public_restore`، بعد التحقق من وجود الشرط مرة واحدة بالضبط. لا تحفظ النسخة المعدلة ولا تستخدم خارج الحاوية.
- يسجل تقرير الاختبار بصمة ملف Staging الأصلي وبصمة نسخة الاختبار، ليكون الفرق قابلاً للتدقيق. أضيف اختبار يثبت أن إسقاط تبديل اسم القاعدة يعيد النص حرفياً إلى الملف الأصلي، ويرفض غياب الشرط أو إعادة إسقاط نسخة معدلة.
- نجحت اختبارات الانتقال الثمانية وفحص الصيغة و`--check` وحاجز المستودع. إعادة اختبار ملف الرجوع بعد إسقاط حارس اسم القاعدة لم تُنفذ بعد.

### نجاح اختبار ملف رجوع Staging

- أعيد الاختبار باستخدام اسم قاعدة الاختبار في نسخة الذاكرة فقط؛ بقي ملف Staging الأصلي دون تعديل.
- أرسل المستخدم `STAGING_ROLLBACK_REHEARSAL_OK; ALL_ROWS_AND_PUBLIC_SCHEMA_RESTORED`. عاد كل صف ومخطط public إلى خط الأساس بعد الرجوع.
- التقرير المؤقت `/tmp/accounting-l3-tracking-report-CE6tRj/report.json` محمي وغير مقروء من جلسة المساعد. ينتظر الحفظ بجوار النسخة ثم التحقق منه.
- اكتمل اختبار منطق رجوع Staging في الحاوية فقط. لم يتغير Staging المستضاف أو Farida أو Alibea.
- احتوى أمر الحفظ الأول المرسل للمستخدم خطأً على كلمة `Trap` وانقطع قبل مسار الوجهة. لم يصل التقرير إلى مجلد النسخة، وأنشأ الأمر ملفاً عرضياً باسم `/opt/accounting-app/Trap` مطابقاً للتقرير. تحقق المساعد من الهدفين ثم حذف الملف العرضي المحدد من مساحة العمل. لا توجد قاعدة بيانات أو نسخة احتياطية حُذفت أو تغيرت؛ ما زال التقرير الأصلي محفوظاً في `/tmp`.
- أُعيد الحفظ بالأمر المصحح إلى `tracking-rehearsal/staging-rollback-report.json`. أكد فحص SHA-256 الذي أرسله المستخدم تطابق الملف المحفوظ مع التقرير المؤقت حرفياً؛ بصمة كليهما `5277751b4aeff125d74db198b583dcb4d6411407529cf89871ae633673f5e104`.
- اكتملت بذلك أرشفة دليل اختبار رجوع Staging. الخطوة التالية هي تجديد فحص Staging القراءة فقط عبر القناة المرتبطة قبل أي تطبيق لـ`L3-B`؛ لا يزال Staging المستضاف وFarida وAlibea دون تغيير.

### تجديد خط أساس Staging قبل L3-B

- جُدد تسجيل Supabase CLI بتدفق التحقق الرسمي، ثم تحقق الغلاف محلياً أن project ref المرتبط هو `dunzfxurefzlaamgghys` قبل إرسال الاستعلام.
- نفذ المساعد `/tmp/l3-staging-tracking-readonly.sql` عبر `supabase@2.116.0 db query --linked --file`؛ الاستعلام قراءة فقط ولم ينفذ DDL أو DML.
- النتيجة الحية: قاعدة `postgres` بإصدار PostgreSQL `17.6`، وسجل الترحيلات القديم جدول عادي OID `19002` مملوك لـ`postgres` ومفتاحه `lovable_schema_migrations_pkey`، والسجل المحايد غير موجود.
- بقي سجل الترحيلات `96` صفاً ببصمة `634e981f2f7363bcdc1b546a1e9c37c0`. الأعداد الحية: المنتجات `613`، فواتير البيع `98`، فواتير الشراء `31`، القيود `310`، وسطـور القيود `827`؛ تطابق حواجز ملف الرجوع المحفوظ.
- نجح استعلام المجاميع الحي: فواتير البيع المرحلة `94` بإجمالي `196801`، وفواتير الشراء المرحلة `31` بإجمالي `626171`، ومجموع كميات حركات المخزون `6138`، ومجموع تكلفتها الدقيق `775736.1690909091`. تتطابق جميعها مع النسخة المتحققة وحارس الرجوع.
- اكتملت بوابة خط الأساس الحي دون كتابة. نجح `supabase@2.116.0 db push --linked --dry-run` وأعاد migration واحدة فقط: `20260907091000_neutral_migration_tracking.sql`؛ لم توجد seeds أو roles معلقة ولم تُكتب تغييرات.
- أعيد فحص ملف migration قبل الاعتماد: SHA-256 هو `9a6eb211ec127cb8b125d08f71c2aafe4fcd93f721460cd0f0329fa0d9e2ed70`، واجتاز حاجز سلامة المستودع. الخطوة التالية تحتاج اعتماد المستخدم الصريح لتطبيق هذه migration الواحدة على Staging، يتبعها فوراً تحقق قراءة فقط؛ لا يشمل الإذن Farida أو Alibea.

### L3-B — تطبيق سجل الترحيلات المحايد على Staging

- اعتمد المستخدم التطبيق صراحة. تحقق غلاف التنفيذ مرة أخرى من project ref ثم نفذ `supabase@2.116.0 db push --linked`؛ طُبق الملف الوحيد `20260907091000_neutral_migration_tracking.sql` بنجاح، دون seeds أو roles ودون لمس الإنتاج.
- أكد فحص مستقل بعد التطبيق أن `public.app_schema_migrations` جدول عادي بنفس OID الأصلي `19002` ومالك `postgres` ومفتاح `app_schema_migrations_pkey`، وأن الاسم القديم أصبح View OID `19601` تعتمد فعلاً على الجدول المحايد.
- يقرأ الاسمان الـ`96` صفاً نفسها وبالبصمة نفسها `634e981f2f7363bcdc1b546a1e9c37c0`. بقيت أعداد الأعمال `613/98/31/310/827` والمجاميع المالية والمخزنية `94/196801` و`31/626171` و`6138/775736.1690909091` دون تغيير.
- أعاد `db push --linked --dry-run` بعد التطبيق `upToDate=true` وقائمة migrations فارغة؛ لا يوجد أي migration أو seed أو role معلق في قناة Supabase الخاصة بـStaging.
- اكتملت بوابة L3-B رقمياً. يبدأ L3-C بفحص وصول الواجهة ثم الدخول والصلاحيات والصفحات والتقارير على Staging، مع بقاء ملف الرجوع المحفوظ صالحاً لهذا الخط الأساسي.
- نجح فحص HTTPS لصفحة `https://staging.alibea2020.com/auth`: الحالة `200` ونوع المحتوى `text/html` بزمن يقارب `0.092` ثانية. الخطوة التالية اختبار دخول المستخدم بحساب المدير على Staging فقط.
- أكد المستخدم نجاح الدخول بحساب المدير، ثم نجاح فتح قوائم فواتير المبيعات والمشتريات والمصروفات وظهور بياناتها دون إنشاء أو تعديل مستند. الخطوة التالية مطابقة تقرير المبيعات على فلتر «هذا الربع» مع نتيجة القبول السابقة.
- عرض تقرير المبيعات على «هذا الربع» القيم: صافي `191401` من `94` فاتورة، ربح `64287.25`، هامش `33.6%`، مرتجعات `5400`، وتحصيل `191335` بنسبة `97.2%`. أما نتيجة `25251/9591.75/38%/200/25085` السابقة فهي لفلتر «هذا الشهر»؛ كان وصف المساعد السابق لها بأنها ربع سنوية خطأً وتم تصحيحه.
- طابق المساعد أرقام الربع مباشرة بدالة التقرير الداخلية عبر استعلام Staging للقراءة فقط للفترة `2026-07-01` إلى `2026-09-30`: أعادت `94/196801/191401/64287.25/33.59/5400/191335/97.22`، والفترة السابقة صفراً. تتطابق واجهة التقرير مع قاعدة البيانات بعد التقريب المعروض.
- الخطوة التالية في L3-C فحص صفحة صحة النظام لسلامة القيود والمخزون دون تنفيذ إصلاحات أو تعديلات.
- أكد المستخدم أن صفحة صحة النظام تفتح وتعرض ثلاثة انحرافات في رؤوس القيود ذات أرقام الترحيل `104` و`109` و`128`.
- أثبت استعلام Staging مستقل للقراءة فقط أنها بالضبط القيود المرحلة الثلاثة بلا سطور المعروفة في خط الأساس قبل L3: القيد `104` رأسه `200/200`، و`109` رأسه `525/525`، و`128` رأسه `5000/5000`، وكل منها `line_count=0` ومجموع سطوره `0/0`.
- هذه ليست قيوداً غير متوازنة بين المدين والدائن؛ الانحراف هو أن إجمالي الرأس لا يطابق مجموع السطور المعدوم. لم تنشئ migration انتقال السجل هذه الحالات، وبقي العدد المعروف ثلاثة. لا تُصلح ضمن L3 حتى لا يُخلط إصلاح بيانات تاريخية مع انتقال سجل الترحيلات؛ تُفتح لاحقاً كمهمة بيانات مستقلة.
- بقي فحص تطابق قيمة المخزون مع حساب `1104` هو خطوة صحة النظام التالية في L3-C.
- أرسل المستخدم نتيجة المطابقة: قيمة تقييم المخزون `483321.64` ورصيد حساب `1104` الفعلي `483629.03`، بفارق موجب `307.39` في الدفتر. طابق استعلام مستقل داخل معاملة `READ ONLY` هذه القيم حرفياً؛ استخدم هوية `service_role` مؤقتة عبر GUC لأن حاجز تكلفة الدالة رفض جلسة CLI غير الحاملة لـJWT، ثم انتهت المعاملة بـ`ROLLBACK`.
- الفرق يعادل نحو `0.064%` ويصنفه النظام تحذيراً محدوداً، لكنه يبقى مهمة تسوية تكلفة مستقلة. Migration الانتقال لا تقرأ أو تعدل المنتجات أو الحركات أو القيود، وبقيت خطوط الأساس السابقة كما هي؛ لا يُنسب الفرق إلى L3 ولا يُصحح ضمنها.
- أكد فحص مستقل لسطور القيود أن إجمالي المدين والدائن للقيود المرحلة متطابقان تماماً عند `2401407.649090909`، وأن عدد القيود ذات السطور غير المتوازنة صفر. الحالات الوحيدة هي القيود التاريخية الثلاثة بلا سطور.
- اكتمل فحص صحة البيانات المطلوب لـL3-C مع توثيق الاستثناءين التاريخيين. الخطوة التالية فحص تقرير المشتريات وظيفياً على Staging دون تعديل.
- فتح المستخدم تقرير المشتريات بفلتر «هذا الربع» وأرسل الملخص: `31` فاتورة، إجمالي `626171`، مرتجعات `15444`، صافي `610727`، مدفوع `592008`، نسبة `96.9%`، متوسط `20199.06`، متأخرات `0`، خصم `1446`، وضريبة `0`.
- طابق استعلام Staging مستقل للقراءة فقط القيم حرفياً، بما فيها نسبة السداد الدقيقة `96.93%` التي تقربها الواجهة إلى `96.9%`. اكتملت المطابقة الرقمية لتقرير المشتريات.
- الخطوة التالية اختبار منع حساب البائع من الوصول إلى التقرير المالي الكامل، دون إنشاء أو تعديل بيانات.
- أكد المستخدم أن حساب البائع مُنع من فتح `/reports/sales` وظهرت نتيجة عدم التصريح المتوقعة. نجح اختبار الحد الأدنى للصلاحيات بعد الانتقال.
- اكتملت بوابة L3-C: الوصول والدخول واستمرار الجلسة، قوائم المبيعات والمشتريات والمصروفات، تقريرا المبيعات والمشتريات مع مطابقة SQL، صحة القيود والمخزون، ومنع البائع من التقرير المالي. لم تُنشأ أو تُعدل بيانات أثناء القبول.
- الاستثناءات المقبولة هي نفسها خط الأساس قبل L3: ثلاثة قيود مرحلة بلا سطور وتحذير مخزون `307.39`. لا يوجد فرق جديد من migration الانتقال.
- بعد إغلاق القبول نجح `git diff --check`، واختبارات أدوات الاستعادة وانتقال السجل وتدقيق التاريخ، وحاجز سلامة المستودع دون أسرار أو أسماء محظورة جديدة. أصبحت تغييرات L3 المحلية جاهزة للتثبيت في Git قبل تجهيز الإنتاج.

الخطة التنفيذية الملزمة في `docs/MASTER_EXECUTION_PLAN.md`. الخطوة التالية فحوص المستودع وتثبيت توثيق L3-A/B/C قبل تجهيز L3-D؛ لا يُمس الإنتاج دون بوابة مستقلة.

## الانتقال إلى L3-D

- ثُبتت نتائج L3-A/B/C في الالتزام `30025ca` ورُفعت إلى المستودع الجديد.
- بدأ فحص Farida قراءة فقط بخط أساس منفصل موثق في الخطة الرئيسية. لا تُستخدم أعداد Staging أو ملف رجوعها عند تنفيذ Farida.
- أُنشئت نسخة Farida custom أولية في `/backups/farida/l3-before-tracking-20260909-200452` بحجم `2687204` بايت و`1378` بنداً وبصمة `47ef37ec6d567e7d82832517ca90785b6c3e626f1d59cdd7f35cb9da5da0cfd3`. تنتظر التحقق المستقل قبل اعتمادها.
- نجح التحقق المستقل من بصمة النسخة وصلاحياتها وإعادة قراءة عناصرها الـ`1378`. اعتمدت النسخة لبوابة Farida، بينما يبقى رجوع Staging غير صالح للاستخدام عليها.
- اكتمل خط أساس Farida التفصيلي، وجُهز رجوع مستقل مرتبط بـOID `20617` وأرقام Farida الحالية. رفض فحص القراءة الأول بأمان لأن ACL المستعمل كان بصيغة PostgreSQL 17 ويحمل `m`، بينما Farida على PostgreSQL 15.8 وصلاحيتها الفعلية `arwdDxt`. لم تتغير القاعدة. صُحح ملف الفحص وملف الرجوع، وأصبحت بصمة الرجوع الجديدة `6affe1dfac773f422adaa1faed366b5f504b092dd0d3d34ff28fddf87c2ea5a4` وحجمه `5029` بايت، ولا يحتوي أوامر حذف بيانات.
- حُفظ رجوع Farida داخل مجلد النسخة وتطابقت بصمتاه. نجحت فحوص سكربت `--prepare-tracking --only farida` والـmigration وحاجز المستودع. قبل التطبيق يلزم توقف معاملات Farida لدقائق قليلة ثم تجديد خط الأساس.
- كشف التجديد أن معاملات إنتاج حقيقية حدثت بعد النسخة الأولى، فرفض الحارس الخط القديم قبل أي تعديل. أُخذت نسخة جديدة في `/backups/farida/l3-before-tracking-20260909-234013` بحجم `2721235` بايت و`1378` بنداً وبصمة dump هي `68bff8dba028fb01a9b5907dcad65c13cb6d043d582d9895d05edc472c974bb8`.
- أصبح خط Farida الحديث `612/102/31/329/881` للمنتجات وفواتير البيع والشراء والقيود وسطـورها؛ والمجاميع `102/215135.00` للبيع المرحل، و`31/629599.0` للشراء المرحل، و`6261/794294.5890909090909` لكمية وتكلفة حركات المخزون.
- حُفظ فحص ما قبل التطبيق ببصمة `4bcd7da5fdb2f51381b248da834003190294db416ca6ed2132378dc7c0a50f2b` ورجوع Farida الحديث ببصمة `9ae53ae27417419a6161b18a95091146f6217d04fa1656c045abf0a656cb2bf1`، ونجح `FARIDA_PREFLIGHT_OK`.
- طُبق انتقال السجل على Farida وحدها. أثبت `FARIDA_TRACKING_TRANSITION_OK` أن الجدول المحايد احتفظ بـOID `20617` وبالـ`96` إصداراً، وأن الاسم القديم أصبح View توافق، وأن أعداد الأعمال والمجاميع لم تتغير. لم تُمس Alibea.
- نجح قبول Farida الوظيفي بحساب المدير في فواتير المبيعات والمشتريات والمصروفات وتقريري المبيعات والمشتريات. نجح دخول البائع وظهرت له فواتير المبيعات فقط. لم تُنشأ بيانات اختبار.
- لم يتكرر خطأ `VM... reportAllChanges/startTime` في التصفح الخفي، كما أنه غير موجود في مصدر المشروع أو بنائه؛ مصدره إضافة متصفح محلية وليس انتقال السجل أو التطبيق.
- اكتملت L3-D. تبدأ L3-E بفحص ونسخة وخط أساس مستقل لـAlibea؛ لا يُعاد استخدام ملف رجوع Farida عليها.
- اختلاف PostgreSQL 17 في Staging عن PostgreSQL 15.8 في الإنتاج موثق كمرحلة ترقية بنية مستقلة بعد L3، ولا يُعالج داخل انتقال سجل الترحيلات.
