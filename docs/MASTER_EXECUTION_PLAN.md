# الخطة التنفيذية الرئيسية للنظام

> تاريخ الإنشاء: 2026-09-09
> الحالة: اكتملت L3-A وL3-B وL3-C على Staging — التالي تثبيت الأدلة في Git ثم تجهيز L3-D لـFarida
> المستودع المعتمد: `https://github.com/MohammedIsmail36/accounting-stores-app.git`
> نسخة العمل المعتمدة: `/opt/accounting-app`

## سلطة هذه الوثيقة

هذه الوثيقة هي المرجع التنفيذي الأول لتحديد المرحلة الحالية والخطوة التالية. تبقى الملفات الأخرى سجلات فنية وأدلة تحقق، لكن لا يجوز اختيار مهمة جديدة منها بما يتعارض مع ترتيب هذه الخطة.

عند وجود تعارض بين حالة مكتوبة هنا وحالة قديمة في مستند آخر، يُوقف التنفيذ وتُراجع الأدلة ثم تُحدَّث الوثيقتان قبل أي تغيير تشغيلي.

## الهدف الحالي

إكمال الانفصال التشغيلي والبنائي عن Lovable بأمان، مع الحفاظ على استقرار Staging وFarida وAlibea، ثم إغلاق آثار الانتقال المؤقتة. بعد ذلك فقط يبدأ مشروع تحسين تقرير المشتريات.

معيار الإغلاق:

- صفر اعتماد تشغيلي أو بنائي أو اتصال أو حزمة أو إعداد نشر تابع للمنصة السابقة.
- استخدام `public.app_schema_migrations` بوصفه سجل الترحيلات التشغيلي في القواعد الثلاث.
- إزالة واجهة التوافق القديمة بعد انتهاء نافذة الرجوع واعتماد البيئتين الإنتاجيتين.
- السماح فقط بإشارات تاريخية ضرورية داخل السجلات وملفات migrations التي لا يجوز إعادة كتابتها.

## حدود البيئات

| البيئة | الاستخدام | قاعدة الأمان |
|---|---|---|
| Staging | الاختبار والتجارب والكتابة | يبدأ بها كل تغيير قاعدة أو ميزة |
| Farida | إنتاج مستقل | لا تتغير قبل نجاح Staging ونسخة رجوع واعتماد صريح |
| Alibea | إنتاج مستقل | لا تتغير قبل نجاح Farida واعتماد صريح مستقل |

لا تُنشر الشركتان في خطوة واحدة، ولا تُجمع migration قاعدة بيانات مع تعديل واجهة غير ضروري في الدفعة نفسها.

## الحالة المثبتة

### مراحل الانفصال المكتملة

- `L0`: مساحة تنظيف وخط أساس وقوائم منع الأسرار.
- `L1`: فصل مصادر الحزم عن المنصة السابقة واستخدام npm الرسمي.
- `L2`: إزالة وسيط مصادقة المعاينة التابع للمنصة.
- `L4`: تنظيف السكربتات والوثائق والهوية والملفات العامة.
- `L5`: بوابات الجودة والأمان واعتماد Node 22.
- `L6`: إنشاء المستودع الجديد بتاريخ نظيف وحماية `main`.
- `L7`: اعتماد Staging ثم Farida ثم Alibea من المستودع الجديد، وجعل `/opt/accounting-app` نسخة العمل الوحيدة.

### الجزء غير المكتمل

- `L3`: كوده واختباراته المعزولة جاهزة، لكن انتقال سجل الترحيلات لم يُطبق على Staging أو Farida أو Alibea.
- ما زال الاسم القديم موجوداً في قواعد البيانات حتى تنفيذ الانتقال الذري إلى `public.app_schema_migrations` ثم انتهاء نافذة التوافق.

### قبول Staging الواقعي المكتمل قبل L3

- نُقلت نسخة واقعية من بيانات Farida إلى Staging من دون هويات مستخدمي الإنتاج.
- نجحت اختبارات الدخول والصلاحيات والمنتجات والمخزون.
- عولج باقي تقريب خصم الفواتير، واختُبر على `PUR-0031` ثم نُشر تدريجياً على Farida وAlibea.
- نجحت دورة فاتورة البيع الداخلية `#99`: إنشاء مسودة، تعديلها، ترحيلها إلى `INV-0094`، إعادتها لمسودة، تعديل السعر، وإعادة ترحيلها مع الاحتفاظ برقم الترحيل.
- تطابق قيد `INV-0094` بعد التعديل عند مدين ودائن `256.11` بفارق `0.00`، وظهرت الفاتورة بقيمة `166.00` في تقرير المبيعات.
- تطابق ملخص المبيعات الشهري مع قاعدة Staging، وظهرت `PUR-0031` بقيمة `2.00` في تقرير المشتريات.

## الانحرافات التي يجب إغلاقها قبل L3

1. بعض عناوين الحالة في المستندات القديمة لا تعكس اكتمال L7 أو اختبارات Staging الأخيرة.
2. حاجز `scripts/check-repository-safety.sh` يرفض حالياً مستندي L3 لأنهما يذكران اسم جدول الترحيلات القديم لأغراض التوثيق؛ يجب تصنيفهما صراحة كسجلين مسموحين أو نقل التفاصيل التاريخية إلى سجل واحد.
3. لا توجد حتى الآن وثيقة واحدة كانت تجمع حالة الانفصال، L3، واختبارات Staging؛ هذه الوثيقة تعالج ذلك من الآن.
4. تقرير المشتريات ملف كبير ويحتاج خطة تحسين مستقلة، لكنه خارج النطاق حتى إغلاق L3.

## ترتيب التنفيذ الإلزامي

### M0 — توحيد الحوكمة وحواجز المستودع — مكتملة

- اعتماد هذه الوثيقة مرجعاً رئيسياً.
- تصحيح عناوين الحالة المتقادمة في مستندات الانفصال وL3 وسجل تقرير المبيعات.
- إصلاح قائمة السماح في حاجز السلامة بحيث يقبل السجلات التاريخية المحددة فقط ويرفض أي مرجع جديد.
- تشغيل فحص الأسرار وفرق Git، ثم التوثيق والرفع دون نشر أو تغيير قاعدة.

بوابة القبول: مستودع نظيف، حاجز السلامة ناجح، والحالة نفسها مكتوبة في كل الوثائق الحية.

### L3-A — لقطة Staging وما قبل التنفيذ

- الحالة: اكتمل خط الأساس واللقطة والاستعادة واختبار التقديم والرجوع المعزول وحفظ تقاريره. نجح الفحص الجاف: 97 ملفاً محلياً، 96 إصداراً مسجلاً، والمعلق الوحيد migration الانتقال، وصفر إعادة تشغيل تاريخية. جُهز رجوع Staging المقيد مؤقتاً، وبقي حفظه واختباره وتجديد فحص Staging القراءة فقط قبل التطبيق.
- أخذ نسخة قاعدة حديثة من Staging بعد معاملات القبول الأخيرة، مع بصمة وحجم وصلاحيات محمية.
- تسجيل نوع وجدول وعدد وبصمات سجل الترحيلات القديم، وأعداد بيانات الأعمال والمؤشرات المحاسبية المرجعية.
- إعداد رجوع خاص بانتقال سجل الترحيلات واختباره بمعزل عن الإنتاج.
- تنفيذ dry-run يثبت أن العملية لن تعيد تشغيل migrations التاريخية.

بوابة القبول: نسخة رجوع متحققة، أرقام مرجعية محفوظة، وصفر migration أعمال غير مقصودة.

### L3-B — تطبيق سجل الترحيلات المحايد على Staging

- تطبيق `20260907091000_neutral_migration_tracking.sql` على Staging فقط وفي معاملة مستقلة.
- التحقق من أن `public.app_schema_migrations` جدول فعلي وأن الاسم القديم أصبح View توافق مؤقتاً.
- مطابقة عدد الإصدارات والأسماء والبصمات قبل الانتقال وبعده.
- تشغيل أداة الترحيلات في وضع الفحص والتأكد من عدم وجود migrations قديمة معلقة أو معاد تنفيذها.

بوابة القبول: تطابق سجل الترحيلات بالكامل وعدم تغير أي جدول أعمال أو رصيد أو عدد مستندات.

### L3-C — قبول Staging بعد الانتقال

- فحص الدخول واستمرار الجلسة والصلاحيات.
- فحص صفحة المبيعات والمشتريات والمصروفات والتقارير دون إنشاء بيانات جديدة غير ضرورية.
- إعادة التحقق من القيد والمخزون وملخص المبيعات المرجعي.
- اختبار الرجوع إذا ظهر أي فرق، وإيقاف الخطة قبل الإنتاج.

بوابة القبول: Staging سليمة وظيفياً ورقمياً واعتماد المستخدم صريح.

### L3-D — Farida وحدها

- أخذ نسخة قاعدة حديثة ومتحققة قبل التغيير.
- تنفيذ فحص ما قبل الانتقال ومطابقة سجل Farida.
- تطبيق انتقال سجل الترحيلات فقط، دون تعديل واجهة أو Edge Functions.
- إعادة فحص البيانات والخدمات والدخول والتقارير.

بوابة القبول: Farida مستقرة واعتماد المستخدم قبل لمس Alibea.

### L3-E — Alibea وحدها

- تكرار بوابات النسخة والفحص والتطبيق والمطابقة على Alibea بصورة مستقلة.
- منع استخدام أرقام أو نسخة رجوع Farida عند التحقق من Alibea.

بوابة القبول: Alibea مستقرة، والقواعد الثلاث تستخدم السجل المحايد.

### L3-F — نافذة التوافق والإزالة النهائية

- إبقاء View الاسم القديم خلال مدة رجوع يعتمدها المستخدم.
- إثبات أن نسخة العمل وسكربتات النشر لا تقرأ أو تكتب إلا `public.app_schema_migrations`.
- بعد انتهاء المدة، إنشاء migration مستقل لحذف View التوافق، واختباره على Staging ثم Farida ثم Alibea بالتتابع.
- إعادة فحص المستودع والبناء والقواعد، مع بقاء الإشارات التاريخية الضرورية فقط.

بوابة القبول: لا كائن قاعدة أو اعتماد تشغيلي يحمل الاسم القديم، وسجل الإغلاق مرفوع إلى Git.

### S0 — إغلاق الانتقال وفحص الاستقرار

- فحص Nginx وDocker والحاويات والذاكرة والقرص وواجهات API.
- جرد ملفات الاختبار المؤقتة وحذف الأهداف المثبت عدم استخدامها فقط.
- اعتماد سياسة الاحتفاظ بنسخ الرجوع وعدم حذف أي نسخة قاعدة لازمة قبل القرار.
- تحديث جميع المستندات إلى حالة «مكتمل» ورفع الالتزام النهائي.

## المشروع التالي المؤجل: تحسين تقرير المشتريات

لا يبدأ هذا المشروع قبل إغلاق `L3-F` و`S0`. حجمه الحالي يستحق معالجة مرحلية مستقلة؛ فالملف `src/pages/reports/PurchasesReport.tsx` يتجاوز ألفي سطر.

### P0 — التدقيق والسياسة المحاسبية

- تعريف المؤشرات المحاسبية للمشتريات: المشتريات قبل الضريبة، مردودات المشتريات، صافي التكلفة، المدفوع، وتسويات المرتجعات.
- مطابقة كل مؤشر مع حسابات قاعدة البيانات والقيود وحركات المخزون.
- تسجيل التناقضات دون تعديل في أول دفعة.

### P1 — طبقة البيانات والأداء

- إزالة أي حد صفوف غير معلن واستخدام pagination آمنة.
- نقل الملخصات الثقيلة إلى RPC محمية إذا أثبت القياس الحاجة.
- توثيق حالات التحميل والخطأ والبيانات الناقصة.

### P2 — تفكيك الكود

- فصل الاستعلامات والحسابات والتفضيلات والتصدير والجداول والرسوم إلى وحدات مختبرة.
- إبقاء الصفحة منسقاً صغيراً بدلاً من ملف واحد ضخم.
- تنفيذ التفكيك على دفعات صغيرة لا تغير الأرقام أو التصميم معاً.

### P3 — المؤشرات والتناقضات المحاسبية

- تثبيت صيغ البطاقات ومقارنتها بأرقام قاعدة البيانات.
- فصل المستندات المرحلة عن المسودات والملغاة.
- توضيح أثر مردودات المشتريات والتسويات والضريبة والخصم على كل مؤشر.

### P4 — تبسيط UI/UX

- تقليل ارتفاع نطاق التقرير وتجميع عناصر التحكم حسب الوظيفة.
- توحيد الأزرار والفلاتر والخيارات النشطة مع هوية النظام.
- جعل الملخص الأساسي مقروءاً قبل المؤشرات الإضافية والتحليل البصري.

### P5 — الجداول والفلاتر والتصدير

- توحيد الأعمدة الأساسية والإضافية والبحث والفرز.
- ضمان اتساق الأرقام بين الشاشة وPDF وExcel.
- منع قص قائمة التصدير أو تداخلها مع الحاويات.

### P6 — الرسوم والهاتف والأداء

- تصميم رسوم RTL بلا تداخل بين المحاور والتسميات.
- تحسين الهاتف بعد تثبيت الأرقام وبنية الصفحة.
- قياس زمن التحميل وحجم الحزمة وعدد الاستعلامات قبل وبعد.

### P7 — الاعتماد والنشر

- اختبارات آلية ومطابقة مستقلة على Staging.
- اعتماد المستخدم لكل دفعة قبل التالية.
- نشر Farida وحدها ثم Alibea وحدها مع نسخة رجوع وفحص بعد كل نشر.

## بروتوكول كل خطوة لاحقة

1. إعلان اسم المرحلة ورقم الخطوة ونطاقها قبل التنفيذ.
2. إجراء فحص قراءة فقط وتسجيل خط الأساس.
3. تنفيذ تغيير واحد محدود.
4. تشغيل الاختبارات المناسبة ومقارنة النتائج بخط الأساس.
5. تحديث هذه الوثيقة والسجل التفصيلي المرتبط.
6. الالتزام والرفع إلى Git بعد المراجعة.
7. Staging أولاً، ثم Farida، ثم Alibea فقط عند الحاجة وباعتماد منفصل.

لا يُفتح عمل طارئ خارج المرحلة إلا إذا كان عطل إنتاج أو خطر بيانات. عندها يُسجل كمسار طوارئ، ويُغلق قبل العودة إلى الخطة.

## الموضع الحالي والخطوة التالية

اكتملت `M0`: اعتُمدت هذه الوثيقة، ووُحدت حالات المستندات الحية، وأُصلحت قائمة السماح المغلقة في حاجز المستودع. لم يُطبق أي تغيير على قاعدة أو واجهة بسببها.

اكتملت `L3-A.3` — اختبار الاستعادة المعزول بناءً على ناتج التنفيذ الذي أرسله المستخدم. جدول المصدر يحتوي `96` إصداراً ببصمة `634e981f2f7363bcdc1b546a1e9c37c0`، والسجل المحايد غير موجود بعد. بقيت ثلاثة قيود مرحلة بلا سطور وحالة CHECK التاريخية دون تعديل؛ ليست هذه نتيجة إصلاح للعيوب التاريخية.

نجحت المحاولة الرابعة: `RESTORE_OK` لـ39 جدولاً و60 علاقة أجنبية و37 مشغلاً و9 تسلسلات و96 سجل ترحيل. أكد التنفيذ `ALL_COPY_ROWS_MATCH` و`FINANCIAL_BASELINE_OK` وفحص التوازن الدقيق للرؤوس، مع إبقاء `jentry_balanced NOT VALID` كما في المصدر. حُفظ التقرير بجوار النسخة في `/backups/staging/l3-before-tracking-20260909-093540/restore-report.json`، وقرأه المساعد وتحقق من صلاحياته وبصمته. تفاصيل العزل وحدود تعريفات `auth` المساندة في وثيقة تجربة L3؛ هذه ليست استعادة أو اختباراً لنظام المصادقة.

عولج محلياً نقص تهيئة `pg_trgm`، ثم فُصل فحص العلاقات الأجنبية الستين عن حالة CHECK التاريخية `jentry_balanced NOT VALID` مع التحقق من تطابق حالتها وفحص التوازن الدقيق للصفوف. لم تُغيّر بيانات المصدر أو حالته لتجاوز الفحوص.

صُححت قبل النجاح ثلاثة عيوب في تجهيز أداة الاختبار: نقص `pg_trgm`، والخلط بين حالة FK وCHECK التاريخية، واستخدام تكلفة مدوّرة بدلاً من مجموعها الدقيق `775736.1690909090909`. نجحت الاختبارات المحلية الإحدى عشرة، ثم أكد التنفيذ الفعلي سلامة الاستعادة. لا تزال الحاوية متاحة لاختبار الرجوع المحدود، ولم يتغير الإنتاج أو مشروع Staging الفعلي.

نجح اختبار `scripts/tests/rehearse-tracking-transition.mjs` بعد تصحيح صيغة قراءة View: تقديم ورجوع داخل معاملة تُلغى، ثم تقديم ورجوع في معاملتين مثبتتين مع قراءات مستقلة. نجحت اختبارات الأداة الستة مع اختبارات الاستعادة الإحدى عشرة. ملف الرجوع الناتج مقيد بقاعدة الاختبار، ولا يُستخدم مباشرة على Staging أو الإنتاج.

آخر تنفيذ أرسله المستخدم: `TRANSACTIONAL_PREFLIGHT_OK` ثم `FORWARD_OK` ثم `TRACKING_REHEARSAL_OK; FORWARD_AND_ROLLBACK_OK; ALL_ROWS_AND_PUBLIC_SCHEMA_RESTORED`. عادت بيانات الجداول الـ39 ومخطط public وملكية السجل وصلاحياته ومفتاحه إلى خط الأساس، وبقيت الإصدارات الـ96 دون تغيير. أُرشف التقرير وملف الرجوع في `/backups/staging/l3-before-tracking-20260909-093540/tracking-rehearsal/`، وقرأهما المساعد وتحقق من الصلاحيات وتطابق بصمة جسم الرجوع مع التقرير (`ARCHIVE_VERIFIED`).

نجح الفحص الجاف المحلي بـ`historical_replay=0`، وكشف 12 اختلاف بصمة لملفات تاريخية مسجلة لا تتحول إلى migrations معلقة لأن المطابقة بالإصدار. الفروق موثقة تفصيلياً ولا تُعدل في L3. جُهز `/tmp/l3-staging-tracking-rollback.sql` ببصمة `f96ad1a4994c090492e606e603aab5519688dae6c46f9b93e8aa27623fedc001` وحواجز حالة وبيانات ومهلات، ولم يُنفذ أو يُحفظ بعد. تعذر على جلسة المساعد تجديد استعلام Staging القراءة فقط لأن Supabase CLI لا يملك access token؛ لم يحدث أي تعديل.

حُفظ ملف رجوع Staging وتحقق المساعد من مطابقته وبصمته وصلاحياته. أضيف وضع آمن لاختباره ضد البيانات المستعادة مع رجوع طوارئ مقيد بالـOID عند الفشل؛ نجحت اختبارات الانتقال السبعة والفحص المحلي، ولم ينفذ اختبار PostgreSQL الجديد بعد.

لم تبدأ محاولة التشغيل الأولى بسبب خطأ كتابي في مسار الأمر المرسل (`MODULE_NOT_FOUND`)؛ لم تصل إلى الأداة أو Docker، ولا يوجد أثر على قاعدة الاختبار أو البيئات الفعلية.

نفذ المستخدم الأمر المصحح؛ نجح خط الأساس ثم فشل ملف رجوع Staging في الحاوية عند `staging-rollback-file`. حاولت الأداة رجوع الطوارئ في `finally`، لكن حالته لم تتحقق مستقلاً بعد. سجل التشخيص `/tmp/accounting-l3-tracking-report-WFIqOy/run.log`.

أثبت التشخيص أن ملف Staging رفض قاعدة الاختبار لأن اسمه يقيد البيئة إلى قاعدة `postgres`. أكد الفحص المستقل نجاح رجوع الطوارئ وعودة الجدول القديم وغياب المحايد وبقاء 96 صفاً. بقي الملف الأصلي دون تعديل، وأصبحت أداة الاختبار تسقط شرط اسم القاعدة فقط في الذاكرة إلى اسم قاعدة الاختبار، مع اختبار يثبت أن هذا هو الفرق الوحيد. نجحت اختبارات الانتقال الثمانية؛ التنفيذ المعدل ينتظر التشغيل.

الخطوة التالية: إعادة اختبار ملف Staging في الحاوية بإسقاط اسم قاعدة الاختبار فقط، ثم تجديد استعلام Staging عبر القناة المرتبطة قبل L3-B.

تحديث النتيجة: نجح الاختبار المعدل برسالة `STAGING_ROLLBACK_REHEARSAL_OK`، وعادت جميع الصفوف ومخطط public إلى خط الأساس. حُفظ التقرير في `tracking-rehearsal/staging-rollback-report.json`، وتطابقت بصمته مع الأصل المؤقت: `5277751b4aeff125d74db198b583dcb4d6411407529cf89871ae633673f5e104`. حُذف الملف العرضي `Trap` الذي نتج عن خطأ في أمر الحفظ الأول، ولم تمس أي قاعدة أو نسخة احتياطية.

الخطوة الحالية: تجديد استعلام Staging القراءة فقط عبر القناة المرتبطة، ومراجعة هوية المشروع وحالة السجل وخط الأساس قبل L3-B. لا تطبيق فعلي قبل نجاح هذه البوابة.

تحديث الفحص الحي: جُددت جلسة Supabase CLI وتحقق project ref `dunzfxurefzlaamgghys`. أعاد استعلام القراءة فقط قاعدة `postgres`/PostgreSQL `17.6`، وجدول السجل القديم OID `19002` مملوكاً لـ`postgres`، وغياب الاسم المحايد، و`96` إصداراً بالبصمة المتوقعة. تطابقت أعداد المنتجات وفواتير البيع والشراء والقيود وسطـورها (`613/98/31/310/827`) مع حارس الرجوع. كما تطابقت المجاميع الحية: بيع مرحل `94/196801`، شراء مرحل `31/626171`، وكمية/تكلفة حركة المخزون `6138/775736.1690909091`. اكتملت بوابة القراءة فقط؛ التالي dry-run رسمي قبل اعتماد التطبيق.

نجح dry-run الرسمي وأثبت أن التغيير الوحيد المعلق على Staging هو `20260907091000_neutral_migration_tracking.sql` ببصمته المعتمدة؛ لا seeds أو roles معلقة، ولم يحدث تطبيق. الخطوة الحالية طلب اعتماد صريح لتطبيق هذه migration الواحدة على Staging فقط، ثم إجراء تحقق مستقل فوراً. الإنتاج خارج الإذن.

اكتملت `L3-B` رقمياً بعد اعتماد المستخدم: طُبقت migration الانتقال الوحيدة على Staging. أصبح `app_schema_migrations` الجدول الفعلي بنفس OID والمالك، وأصبح الاسم القديم View توافق مرتبطة به. تطابقت صفوف السجل وبصمته وكل أعداد الأعمال والمجاميع المالية والمخزنية مع خط الأساس. أكد dry-run اللاحق أن القاعدة محدثة ولا توجد migrations أو seeds أو roles معلقة. لم تتغير Farida أو Alibea.

اكتملت `L3-C`: نجح الوصول والدخول واستمرار الجلسة وقوائم المبيعات والمشتريات والمصروفات. طابق تقريرا المبيعات والمشتريات استعلامات Staging المباشرة. بقيت ثلاثة قيود تاريخية بلا سطور وتحذير مخزون `307.39` فقط، مع توازن دفتر الأستاذ وصفر قيود غير متوازنة لها سطور. أكد المستخدم أن حساب البائع يُمنع من تقرير المبيعات المالي. لم تُنشأ أو تُعدل بيانات في القبول.

نجحت فحوص إغلاق L3-A/B/C محلياً: `git diff --check`، وثلاث حزم اختبارات لأدوات الاستعادة وانتقال السجل وتدقيق التاريخ، وحاجز سلامة المستودع. الخطوة الحالية تثبيت هذه الأدوات والتوثيق في Git ورفعها، ثم تجهيز بوابة L3-D الخاصة بـFarida دون لمس الإنتاج قبل نسخة وفحص واعتماد مستقل.

رُفع التزام L3-A/B/C إلى المستودع الجديد عند `30025ca`. بدأ تجهيز L3-D بفحص Farida قراءة فقط: قاعدة `postgres` على PostgreSQL `15.8` بحجم `42808111` بايت، والسجل القديم جدول، والمحايد غير موجود، وعدد التاريخ `96` وبصمته `634e981f2f7363bcdc1b546a1e9c37c0`. خط أعداد الأعمال الحالي `products=612` و`sales=100` و`purchases=30` و`entries=308` و`lines=821`؛ بيع مرحل `196635` وشراء مرحل `626169.0` ومجموع كمية/تكلفة الحركات `6134/778524.0590909090909`. تختلف هذه الأرقام عن Staging لأنها بيئة إنتاج مستقلة، وستكون مرجع Farida الخاص دون استعارة مرجع Staging.

الخطوة الحالية: إنشاء نسخة custom كاملة ومحمية لقاعدة Farida، والتحقق من قراءتها وبصمتها قبل أي migration.

أنشأ المستخدم نسخة Farida الكاملة في `/backups/farida/l3-before-tracking-20260909-200452/farida-postgres.dump`. حجم الأرشيف `2687204` بايت، ونجح `pg_restore --list` في قراءة `1378` بنداً، وبصمة SHA-256 هي `47ef37ec6d567e7d82832517ca90785b6c3e626f1d59cdd7f35cb9da5da0cfd3`. لم تُطبق migration. يلزم تحقق مستقل من ملف البصمة والصلاحيات وإعادة قراءة قائمة الأرشيف قبل اعتماد النسخة.

نجح التحقق المستقل: `sha256sum -c` أعاد `OK`، وبقيت صلاحية المجلد `0700` والملفات الثلاثة `0600`، وأعاد `pg_restore --list` العدد نفسه `1378`. أصبحت النسخة معتمدة كبوابة رجوع L3-D. الخطوة التالية قراءة هوية سجل الترحيلات وخط الأساس الدقيق في Farida لتوليد رجوع خاص بها؛ لا يُعاد استخدام ملف Staging.

اكتمل خط أساس الرجوع الخاص بـFarida: الجدول القديم OID `20617` ومالكه `postgres` ومفتاحه الأساسي القديم وصلاحياته محفوظة، والمحايد غائب. سجل التاريخ `96` صفاً ببصمة `634e981f2f7363bcdc1b546a1e9c37c0`. ثُبتت أعداد الأعمال `612/100/30/308/821` والمجاميع `93/196635` و`30/626169.0` و`6134/778524.0590909090909`.

جُهز `/tmp/l3-farida-tracking-rollback.sql` لرجوع تهيئة السجل فقط: يقيد قاعدة ودور وOID ومالك وصلاحيات ومفتاح الجدول، واعتماد View، وتاريخ الترحيلات، وخط أعمال Farida. لا يحتوي حذف جدول أو صفوف أو TRUNCATE أو CASCADE. نسخته المصححة حجمها `5029` بايت و`116` سطراً وبصمتها `6affe1dfac773f422adaa1faed366b5f504b092dd0d3d34ff28fddf87c2ea5a4`.

حُفظت النسخة الأولى من ملف الرجوع في مجلد النسخة، ثم رفض فحص ما قبل التنفيذ بأمان بسبب اختلاف تمثيل ACL بين PostgreSQL 17 في Staging وPostgreSQL 15.8 في Farida: استخدمت النسخة الأولى الحرف `m` غير الموجود في ACL الفعلي لـFarida. لم يحدث أي تغيير؛ انتهت معاملة `READ ONLY` عند الحارس. صُحح فحص Farida وملف رجوعها إلى ACL المثبت `arwdDxt`، واجتازا الفحص البنيوي. يجب استبدال الملف المحفوظ بالنسخة المصححة والتحقق من بصمتها الجديدة قبل إعادة الفحص.

طريقة L3-D المعتمدة هي `--prepare-tracking --only farida`: تنفذ migration انتقال السجل وحدها ولا تمر على migrations الأعمال ولا تضيف صفاً جديداً إلى سجل التطبيق؛ لذلك يبقى خط الـ96 صفاً مطابقاً لاختبار Staging وملف الرجوع. يلزم إيقاف إدخال معاملات Farida مؤقتاً خلال الفحص والتطبيق والتحقق حتى لا يصبح حارس خط الأعمال قديماً.

عند تجديد الفحص قبل التطبيق رفض حارس الأعمال الخط القديم بأمان: كانت Farida قد استقبلت معاملات حقيقية بعد النسخة الأولى، فأصبحت الأعداد `products/sales/purchases/entries/lines = 612/102/31/329/881`، والمجاميع `posted sales=102/215135.00` و`posted purchases=31/629599.0` و`inventory quantity/cost=6261/794294.5890909090909`. لم ينفذ الفحص أي تغيير. أُنشئت نسخة أحدث مستقلة في `/backups/farida/l3-before-tracking-20260909-234013/farida-postgres.dump` بحجم `2721235` بايت و`1378` بنداً وبصمة `68bff8dba028fb01a9b5907dcad65c13cb6d043d582d9895d05edc472c974bb8`، ثم جُددت حواجز Farida وفق هذا الخط.

حُفظ مع النسخة الحديثة فحص ما قبل التطبيق ببصمة `4bcd7da5fdb2f51381b248da834003190294db416ca6ed2132378dc7c0a50f2b` وملف رجوع مستقل ببصمة `9ae53ae27417419a6161b18a95091146f6217d04fa1656c045abf0a656cb2bf1`، وأعيد توليد `SHA256SUMS` والتحقق منه. اجتاز الفحص الحي `FARIDA_PREFLIGHT_OK` داخل معاملة قراءة فقط.

اكتمل تطبيق L3-D على Farida وحدها باستخدام `--prepare-tracking --only farida`. أثبت التحقق المستقل بعد التطبيق أن `public.app_schema_migrations` هو الجدول الفعلي بنفس OID `20617`، والاسم القديم View توافق، وبقي سجل التاريخ `96` صفاً بالبصمة نفسها. بقيت أعداد الأعمال `612/102/31/329/881` وجميع المجاميع السابقة دون تغيير. النتيجة: `FARIDA_TRACKING_TRANSITION_OK`، ولم تُمس Alibea.

اكتمل قبول Farida الوظيفي بعد الانتقال: فتح المدير فواتير المبيعات والمشتريات والمصروفات وتقريري المبيعات والمشتريات بفلتر «هذا الشهر» دون أخطاء. نجح دخول البائع ولم تظهر له إلا فواتير المبيعات وفق صلاحياته. خطأ الكونسول `VM... reportAllChanges/startTime` لم يظهر في التصفح الخفي، ولا توجد رموزه في المصدر أو البناء؛ صُنّف ككود محقون من إضافة المتصفح وليس خطأً في التطبيق. أُغلقت L3-D بنجاح، والخطوة التالية بوابة L3-E مستقلة لـAlibea.

توحيد PostgreSQL مؤجل إلى مرحلة بنية مستقلة بعد اكتمال L3: Staging على PostgreSQL 17، بينما Farida وAlibea على PostgreSQL 15.8. الانتقال إلى 17 مفيد لتقليل اختلافات النقل، لكنه ترقية رئيسية لـSupabase ذات نسخة مستقلة وتجربة ترقية ورجوع وفحص إضافات؛ لا يُدمج مع انتقال سجل الترحيلات.

بدأت L3-E بخط أساس Alibea مستقل: PostgreSQL `15.8`، والسجل القديم جدول فعلي OID `20544` مملوك لـ`postgres` بصلاحية `arwdDxt` ومفتاح `lovable_schema_migrations_pkey`، والاسم المحايد غائب. بقي التاريخ `96` صفاً بالبصمة `634e981f2f7363bcdc1b546a1e9c37c0`. خط الأعمال: `products/sales/purchases/entries/lines = 317/78/25/247/665`؛ والمجاميع `posted sales=76/134025.00` و`posted purchases=25/285958` و`inventory quantity/cost=2798/378307.7725`.

حُفظت نسخة Alibea المستقلة في `/backups/alibea/l3-before-tracking-20260909-235936/alibea-postgres.dump` بحجم `1497140` بايت و`1392` بنداً وبصمة `f3fa2083ca7d27916b8c4286a4d009fa9d0a50ad89e1efff0d1b981c5d04e0ce`. حُفظ معها فحص ما قبل التطبيق ببصمة `2d5f0e00814c16f6fa770d00f9182908c27b083280c6395a1a6c1526195a7ea9` ورجوع Alibea الخاص ببصمة `164a9268af1fe80abfbd75ca033ddec352b57f3024f2408854ff1b4366dec504`، وأعيد توليد `SHA256SUMS` والتحقق منه. نجح `ALIBEA_PREFLIGHT_OK` داخل معاملة قراءة فقط.

طُبق انتقال السجل على Alibea وحدها باستخدام `--prepare-tracking --only alibea`. أثبت فحص مستقل `ALIBEA_TRACKING_TRANSITION_OK`: أصبح `public.app_schema_migrations` الجدول الفعلي بنفس OID `20544`، وأصبح الاسم القديم View توافق، وبقي التاريخ `96` صفاً. بقيت أعداد الأعمال `317/78/25/247/665` وكل المجاميع السابقة دون تغيير. لم تُمس Farida؛ قبول الواجهة الوظيفي لـAlibea هو الخطوة التالية قبل إغلاق L3-E.

اكتمل قبول Alibea الوظيفي: فتح المدير فواتير المبيعات والمشتريات والمصروفات وتقريري المبيعات والمشتريات بفلتر «هذا الشهر» دون أخطاء. نجح دخول البائع، واقتصر وصوله على فواتير المبيعات دون التقارير المالية. لم تُنشأ بيانات اختبار. أُغلقت L3-E بنجاح، وأصبحت Staging وFarida وAlibea تستخدم سجل الترحيلات المحايد مع View توافق مؤقتة للاسم القديم.
